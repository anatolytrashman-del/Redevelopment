# Источники аналитики рынка (БЦ, офисы и вообще)

Копилка внешних источников рыночных данных, которые мы можем цитировать на публичных
страницах аналитики (`/minsk/analytics/*`), в гайдах и в питчах в СМИ. Пополняется по
мере находок; реализация (парсинг, занесение в `external_metrics`, вывод на страницу) —
отдельная работа, наличие источника в этом файле её НЕ означает.

Соседние файлы: `docs/sklady-sources.md` — уже разобранные источники по складам (там же
формат разбора «источник → что даёт → периметр»); `SEO_PLAN.md` — где эти данные
используются в SEO-контуре.

**Правила проекта, которые действуют на любой источник отсюда:**

- Фактом считается только **дословно процитированный текст** источника. Значение,
  снятое глазами с графика или диаграммы, не записывается никуда (так в 2026-09-08 был
  отклонён NAI Belarus). Если числа лежат в исходнике страницы как текст (JSON серии
  графика) — это текст, а не картинка, и цитировать их можно.
- Числа для публичных страниц живут в таблице `external_metrics`
  (`source` = ключ из `SOURCE_LABELS` в `src/data/marketSnapshots.ts`), не в коде.
- Атрибуция обязательна («по данным <источник>, <дата публикации>»). Легальность
  цитирования данных аналитики владелец подтвердил 2026-09-16 (см. `SEO_PLAN.md`).

---

## Список источников

| Источник | Что даёт | Статус |
| --- | --- | --- |
| **«Твоя столица» — `t-s.by/analytics/`** (см. разбор ниже) | помесячные ряды ставок аренды и цен продажи офисов в БЦ Минска с 2018 года, по классам; классификация БЦ Минска; обзоры и исследования рынка | найден 2026-09-16, **не подключён** |
| «Твоя столица» — годовой отчёт через prometr.by | склады: ставки класса A/B, сток, прогноз ввода | подключён, `source = tvoya-stolitsa` (см. `docs/sklady-sources.md`) |
| NAI Belarus (через probusiness.io) | склады: качественный сток, ввод, вакансия | подключён, `source = nai-belarus` |
| Госкомимущество (через СМИ-перепечатки) | число зарегистрированных сделок | подключён, `source = goskomimushchestvo` |
| НКА — `nca.by/services/real-estate-market-analytics/` | госорган (Национальное кадастровое агентство), услуга «Аналитика рынка недвижимости» | найден, не разобран |
| Исследовательский центр ИПМ — `ipmconsult.by` | независимый think tank, статьи о рынке недвижимости Беларуси | найден, не разобран |
| `realt.by/news/monitoring/` | собственный мониторинг цен площадки | найден, не разобран |
| `dolevoe.by` | блог с аналитикой рынка 2025–2026 | найден, не разобран |

---

## «Твоя столица» (t-s.by) — разбор 2026-09-16

Агентство недвижимости с многолетним собственным мониторингом. Корень раздела —
[`www.t-s.by/analytics/`](https://www.t-s.by/analytics/). Домен открыт из окружения
сессии обычным `curl`, страницы отдаются статическим HTML (Bitrix).

Что внутри:

| Страница | Что там |
| --- | --- |
| `/analytics/monitoring/comm_offer/bc_rent/` | **«Динамика ставок аренды офисов в БЦ Минска»** — линейный график, 4 ряда: класс A, B+, B, C, в **BYN за м²/мес**, 89 помесячных точек (фев 2018 — авг 2026) |
| `/analytics/monitoring/comm_offer/bc_sell/` | **«Динамика цен на продажу офисов в БЦ Минска»** — 2 ряда: класс B и класс C, в **USD за м²**, те же 89 точек |
| `/analytics/monitoring/bc.php` | «Классификация бизнес-центров, разработанная для г. Минска» — их методика отнесения БЦ к классам (текст, не числа) |
| `/analytics/monitoring/build/`, `/rent_price/`, `/cotteges_offer/` | жилая недвижимость и стройка — для нас периферия |
| `/analytics/issledovaniya-rynkov-nedvizhimosti/`, `/analytics/projects/`, `/analytics/zhilaya-nedvizhimost/` | исследования под заказ, проекты, обзоры жилого рынка |

**Главное техническое наблюдение:** числа графиков лежат прямо в HTML страницы как
JSON (amCharts, поле `dataProvider`), а не рисуются картинкой:

```
"dataProvider": [{"category":"фев 2018","column-1":"50.7","column-2":"33", ...}, ...]
"graphs": [{"title":"Класс A","valueField":"column-1"}, ...]
```

То есть это цитируемый текст источника, а не снятое с диаграммы значение — под правило
проекта подходит, и весь ряд с 2018 года снимается парсером в один заход. Достаточно
`curl` + разбор `dataProvider`/`graphs` (в исходнике кириллица в `\uXXXX`).

Оговорка самого источника, которую при цитировании надо переносить: «Ставки, указанные
на графике, являются справочными и основаны на предложениях собственников **с учётом
НДС**. Реальные ставки по заключенным сделкам могут быть ниже на величину до 10% в
зависимости от характеристик объекта… и текущей ситуации на рынке». То есть это **цены
предложения**, сопоставимые с нашим собственным сбором с Kufar/Realt, а не цены сделок.

Чем ценно для нас: это единственный найденный публичный ряд ставок **по классам БЦ и
помесячно на 8+ лет** — им закрывается то, чего наш собственный сбор дать не может
(история до начала нашего мониторинга и разрез по классу здания).

Что сделать, когда дойдут руки (не сделано):

1. Скрипт-парсер обеих страниц → `external_metrics`, ключ `source = tvoya-stolitsa-bc`
   (отдельно от складского `tvoya-stolitsa` — другой периметр и другая методика).
2. Ключ в `SOURCE_LABELS` (`src/data/marketSnapshots.ts`) с подписью «Твоя столица,
   мониторинг БЦ Минска» и обязательной оговоркой про НДС и цены предложения.
3. Решить периодичность обновления (у них ряд помесячный — хватит крона раз в месяц,
   не по часам, как синки спроса).

---

## Площадки объявлений: разбор топ-20 выдачи (2026-09-20)

Владелец прислал список из 20 сайтов, чаще всего попадающих в топ по запросам об
аренде офисов в БЦ (столбцы: запросов, в топ-10, в топ-3, лучшая и средняя позиция).
Это НЕ аналитика рынка, а источники самих объявлений — то, из чего собираются
`citywide_offers` и `business_center_offers`. Разбор сведён сюда, чтобы следующая
сессия не проверяла те же сайты заново.

| # | Домен | Вердикт |
| --- | --- | --- |
| 1 | realt.by | **подключён** — оба контура |
| 2 | re.kufar.by | **подключён** — оба контура |
| 3 | megapolis-real.by | **подключён**: каталог БЦ с 2026-09-19, городские сегменты с 2026-09-20 |
| 4 | domovita.by | **подключён**: каталог БЦ с 2026-09-19, городские сегменты с 2026-09-20 |
| 5 | t-s.by | отклонён как источник объявлений (2026-09-19): выдача — архивные карточки 2020–2023, пометка видна только в теле страницы. Но как источник АНАЛИТИКИ подключать стоит — см. разбор выше |
| 6 | gohome.by | отклонён (2026-09-19): 820 объявлений, в спот-проверке 5 из 5 — перепечатка Kufar, своих лотов 7 |
| 7 | minsk.etagi.com | **не проверен** — домен закрыт egress-прокси песочницы (403 на CONNECT) |
| 8 | btm.by | **не проверен** — домен закрыт |
| 9 | arendaofisa.by | отклонён (2026-09-19): 170 объявлений на всю Беларусь, цена неоднозначна («3000 р.» — и за м², и за месяц) |
| 10 | iqproperty.by | **не проверен** — домен закрыт |
| 11 | avenue.by | **не проверен** — домен закрыт |
| 12 | realty.by | **не проверен** — домен закрыт |
| 13 | garantiruem.by | **не проверен** — домен закрыт |
| 14 | realting.com | отклонён (2026-09-19): в листинге нет адреса, у трети карточек нет номера дома, машинный перевод адреса уже дал ложное совпадение |
| 15 | bizshop.by | **не проверен** — домен закрыт |
| 16 | irr.by | **не проверен** — домен закрыт |
| 17 | hata.by | отклонён (2026-09-20): на весь Минск 18 коммерческих лотов (14 аренда + 4 продажа), из них 5 — внутренние дубли одного объекта; адреса нет структурным полем, только в свободном тексте описания. Открывается ТОЛЬКО как `www.hata.by`, без `www` домен закрыт |
| 18 | pro-n.by | **не проверен** — домен закрыт |
| 19 | bir.by | используется, но по другому поводу — первичный рынок новостроек (`scripts/sync-bir-primary-market.mjs`), не аренда офисов |
| 20 | belest.by | **не проверен** — домен закрыт |

**Что мешает доразобрать остальные девять.** `minsk.etagi.com`, `btm.by`,
`iqproperty.by`, `avenue.by`, `realty.by`, `garantiruem.by`, `bizshop.by`, `irr.by`,
`pro-n.by`, `belest.by` не входят в Allowed domains окружения сессии — прокси отбивает
их на стадии CONNECT (HTTP 403), ни `curl`, ни `fetch` до сайта не доходят. Разведать
их можно сразу, как владелец добавит домены в настройках окружения; отдельная ловушка
в том, что разрешение выдаётся на конкретный хост (`hata.by` закрыт, а `www.hata.by`
открыт), поэтому добавлять стоит и апекс, и `www`-вариант.

**Мерка, по которой площадку стоит подключать** (выведена по четырём подключённым):
в листинге, без захода внутрь объявления, должны быть адрес с номером дома, площадь
одним числом и цена за м²; лотов по Минску — сотни, а не десятки; и своя доля лотов,
которых нет у Kufar/Realt, — хотя бы 10–15% (спот-проверка на 5 карточках показывает
это за минуту). Площадка, у которой адрес живёт только в тексте описания, не годится:
привязать лот к зданию нечем, а именно привязка и нужна.
