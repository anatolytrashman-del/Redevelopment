# Очередь релиза

Накопитель готовой, но ещё не опубликованной работы. Владелец ведёт несколько
диалогов параллельно, каждый доводит свою фичу до готовности и **останавливается
перед публикацией**; потом всё накопленное уезжает в прод **одним мерджем = одним
билдом Vercel**.

Правила работы с этим файлом — в `CLAUDE.md`, раздел "Очередь релиза". Коротко:

- сессия дописывает СВОЮ запись в "Ждут публикации" сверху, на своей ветке,
  и на этом заканчивает — в `oodobu` ничего не пушит;
- публикующая сессия собирает все перечисленные ветки в одну релизную,
  прогоняет сборку и открывает ОДИН PR в `oodobu`;
- после публикации записи переезжают в "Опубликовано" (внизу), не удаляются.

Конфликт в этом файле при мердже (две сессии дописали сверху) разрешается как
в журнале — **сохранять обе записи**, не выбирая сторону.

---

## Ждут публикации

Формат записи:

```
### <ветка>
- **Что:** одна строка, что меняется с точки зрения владельца
- **Проверено:** чем (сборка, мок-тест, скриншоты, ручной прогон)
- **Миграции SQL:** применены / нет; какие именно
- **Риск при задержке:** есть/нет и какой
```

### claude/festive-cerf-ceexef
- **Что:** каталог поставщиков (`SupplierCatalog.tsx`) считает и показывает
  только верифицированных поставщиков (`SupplierOffer.verified`) — и в
  счётчике плитки хаба (например «Отделочные материалы»), и в счётчике
  каждой плитки подкатегории, и в самом списке компаний при переходе внутрь,
  и в поиске по имени поставщика сверху. Дедуп поставщика в рамках одной
  плитки хаба (чтобы один и тот же поставщик, встречающийся сразу в
  нескольких подкатегориях, не считался в общем итоге хаба несколько раз)
  уже был реализован раньше через `Set` — не трогал; подкатегории внутри
  хаба по-прежнему считают независимо друг от друга (один поставщик может
  входить сразу в несколько подкатегорий) — так и было задумано, тоже не
  менял.
- **Проверено:** `npm run build:app` — чисто.
- **Миграции SQL:** нет.
- **Риск при задержке:** нет — чисто фронтовая правка отображения, к базе не
  обращается.

### claude/wonderful-einstein-vdg945 — ОТЛОЖЕНА ОСОЗНАННО
- **Что:** очередь повторной отправки одиночных писем (отказ Resend больше не
  теряет письмо): новая таблица `outgoing_email_jobs`, колонки `send_status`/
  `send_error` у `supplier_offer_emails` и `purchase_emails`, Edge Function
  `process-outgoing-emails`.
- **Почему не в релизе 2026-09-12:** в шапке её миграции
  (`supabase/migrations/20260912-outgoing-email-queue.sql`) прямым текстом:
  «эта миграция ещё НЕ применена к проду (владелец попросил не выкатывать до
  обновления тарифа)». Проверено запросом к базе — таблицы
  `outgoing_email_jobs` там действительно нет, то есть код бы поехал в прод
  и обращался к несуществующей таблице.
- **Что нужно для публикации (все три шага, по порядку):**
  1. обновить тариф Resend (ради этого и откладывалось);
  2. применить миграцию, затем задеплоить Edge Function
     `process-outgoing-emails`;
  3. только ПОСЛЕ деплоя функции раскомментировать и выполнить блок
     `cron.schedule` в конце той же миграции — иначе крон каждую минуту будет
     стучаться в несуществующий эндпоинт.
- **Риск при задержке:** нет, ровно наоборот — выкатывать раньше нельзя.
- **УСТАРЕЛО (2026-09-12, сборка релиза №4):** содержимое ветки уже в проде —
  таблица `outgoing_email_jobs`, колонки `send_status`/`send_error`, Edge
  Function `process-outgoing-emails` и её крон в базе есть (см. запись
  «опубликована раньше, вне очереди» в «Опубликовано»). Эту запись держим
  до отдельного разбора, публиковать по ней нечего.

---

## Не в очереди (разобрать отдельно)

### claude/modular-platform-client-sales-r6fgw4
31 коммит от 2026-09-09, 38 файлов, ~7.5к строк. Верхний коммит — "Add plan
for the client-license prototype (separate repo + Supabase project)", то есть
похоже на заготовку под ОТДЕЛЬНЫЙ репозиторий, а не под этот прод. В очередь
намеренно не включена: слишком большой и непонятный объём, чтобы вливать
заодно. Разобрать отдельной сессией — выяснить у владельца, живая это работа
или брошенная.

---

## Опубликовано

### claude/affectionate-allen-fyu7s7 (пятый заход) — Верификация: убрать «вторую очередь» из UI, сайт поставщика — отдельным окном
По прямой команде владельца "когда закончишь, мерджи сразу" — опубликовано
сразу, без ожидания в общей очереди.

- **Что:** 1) Секция "Вторая очередь — не хватает данных" (появилась
  прошлым заходом, см. запись ниже) полностью убрана из интерфейса на
  всех экранах вкладки "Верификация" — владелец счёл список лишним.
  Данные (name/email/categories.length) остаются как есть в базе,
  `isReadyForVerification` по-прежнему фильтрует основную очередь, просто
  без отдельного видимого списка "внизу" — "храним только в базе
  данных". 2) Встроенный iframe-блок с сайтом поставщика рядом с
  карточкой заменён на отдельное позиционированное окно (половина
  экрана справа) — владелец: "точно как в верификации объявлений с
  рынка" — тот же приём `openAdWindow`, что уже в
  `MarketOffersReview.tsx` (см. `openSupplierSiteWindow` в
  `SupplierVerificationTab.tsx`). Окно переиспользуется при переходе к
  следующему поставщику (одно имя окна `supplier-site-check`) и при
  клике на ссылку "Сайт" в карточке.
- **Проверено:** `npx tsc -b` и `npm run build:app` чистые; мок-тест
  через временный `__ComponentTest.tsx` + Playwright (4 тестовых
  поставщика) — скриншоты подтвердили отсутствие текста "Вторая
  очередь" в интерфейсе (до и во время верификации), и что при переходе
  к цели верификации реально открывается отдельное окно
  (`context.waitForEvent('page')`), а не встроенный iframe; временные
  файлы удалены, `App.tsx` после отката идентичен бэкапу.
- **Опубликовано:** PR
  [#96](https://github.com/anatolytrashman-del/Redevelopment/pull/96) из
  `claude/affectionate-allen-fyu7s7` в `oodobu`, мердж-коммит `51b6e38`.
  Vercel-деплой `dpl_LdvPRcvpC8pxxSFgemZSYKBHpVj1` — `READY`, быстрый
  режим пререндера (43с, 0 непригодных копий с прода). Прод проверен
  напрямую: JS-чанк `assets/Suppliers-C7BaW_8m.js` на `redevelopment.pro`
  не содержит "Вторая очередь", содержит новое имя окна
  `supplier-site-check`, старый заголовок встроенного браузера "Сайт
  поставщика" пропал.

### claude/affectionate-allen-fyu7s7 (четвёртый заход) — Верификация: очередь только по полным данным + починка брака товарных групп у 100 поставщиков
Две устаревшие записи этой же ветки из "Ждут публикации" (чек-листовая
версия вкладки — заменена раньше в проде другим дизайном, и эта запись)
убраны отсюда, публиковать по ним больше нечего.

- **Что:** 1) SQL/данные (без кода, применено напрямую через Supabase
  Management API, отдельного релиза не потребовало): технический сбой
  одного прогона классификатора 2026-09-13 оставил 100 доменов (включая
  169.ru) с пустыми `categories`, хотя сайты были разобраны полностью
  (`sections` не пусты) — переклассифицировал через
  `scripts/supply-categories/{export,import}.mjs`, 254 домена записаны с
  категориями (подробности — `docs/session-journal.md`, запись
  2026-09-13). 2) Код: по требованию владельца ("на верификацию мне нужны
  только те, где есть название, почта и все категории каталога, остальных
  — во вторую очередь") в `SupplierVerificationTab.tsx` очередь домена
  считается готовой к верификации только при названии, email и хотя бы
  одной категории у снимка сайта; остальные хосты выводятся отдельным
  списком "Вторая очередь" (без кнопки "Верифицировать", только
  "Редактировать"). Бейдж на вкладке (`pendingVerificationHostCount`)
  считает только основную очередь.
- **Проверено:** `npx tsc -b` и `npm run build:app` чистые; мок-тест через
  временный `__ComponentTest.tsx` + Playwright — 4 тестовых поставщика,
  скриншоты подтвердили разбивку 2/2.
- **Опубликовано:** PR
  [#94](https://github.com/anatolytrashman-del/Redevelopment/pull/94) из
  `claude/release-2026-09-13-verify` в `oodobu`, мердж-коммит `df9369c`.
  Vercel-деплой `dpl_ACPBpKbpfj4hCZ7ofbv44CN6MbVV` — `READY`, быстрый
  режим пререндера (45с, 0 непригодных копий с прода). Прод проверен
  напрямую: JS-чанк `assets/Suppliers-Bv5rvf3c.js` на `redevelopment.pro`
  содержит строку "Вторая очередь" и новый текст причин ("категории не
  распознаны").

### claude/affectionate-allen-fyu7s7 (третий заход) — верификация: просмотр + встроенный сайт вместо чек-листа
По команде владельца «Это сразу мерджи в прод, как только сделаешь». PR
[#92](https://github.com/anatolytrashman-del/Redevelopment/pull/92) из
`claude/release-2026-09-13-9` в `oodobu`, мердж-коммит `682d3d8`, Vercel
`dpl_HtrLUMyKF9CDwCseoQwNmmNbn3Sb` — READY (в очереди билдов Vercel позади
двух параллельных релизов, но не мешал), законно быстрый режим (публичный
код не менялся): 286 путей, 284 скопировано с прода, 2 отрендерено, 0
из-за непригодной копии, ошибок в логе нет.
- **Что:** редизайн вкладки «Верификация» (Закупки) по прямой правке
  владельца поверх предыдущего захода (см. запись выше про PR #88) — убран
  редактируемый чек-лист категорий («не будем отмечать их вручную»).
  Новый макет очереди: карточка поставщика в РЕЖИМЕ ПРОСМОТРА слева (имя,
  сайт, email, телефон, мессенджеры, категории — только для контекста) +
  встроенный `iframe` с главной страницей сайта справа (плюс ссылка
  «Открыть в новой вкладке» на случай блокировки встраивания). Кнопки:
  «Верифицировать» / «Редактировать» / «Удалить» (корзина).
  «Верифицировать» переиспользует уже существующий `SupplierOffer.verified`
  (тот же признак, что у сохранения формы предложения) и подтверждает
  разом все карточки-предложения одного домена — «если поставщика
  апрувнули в одной категории, значит он апрувнулся и во всех». После
  верификации следующая карточка подставляется сама (реактивно, без
  window.open — в отличие от MarketOffersReview, куда сайт поставщика
  почти всегда можно встроить, в отличие от Kufar/Realt). Добавлен фильтр
  по стране (флаги Россия/Беларусь), по умолчанию Россия — «белорусские
  поставщики пока не нужны».
- **Проверено:** `npm run build:app` чистый в исходной ветке и повторно в
  релизном worktree после мерджа (конфликтов не было); мок-тест через
  временный `__ComponentTest.tsx` + Playwright — карточка со всеми полями,
  iframe, авто-переход после удаления/верификации, колбэки
  Редактировать/Удалить. После публикации — живой прод: JS-чанк
  `Suppliers-BMHZqUTV.js` отдаётся 200, содержит «Верифицировать», «Начать
  верификацию», «Открыть в новой вкладке»; строк предыдущей версии
  («Одобрить», «Снять отметку») в бандле больше нет.
- **Миграции SQL:** нет новых. Колонки `supplier_site_snapshots.categories_verified`/
  `categories_verified_at` (миграция из PR #88, тот же день) остаются в
  базе неиспользуемыми — код их больше не читает и не пишет; DROP COLUMN
  осознанно не делали (риск рассинхронизации с уже опубликованным кодом).
- Ветки `claude/affectionate-allen-fyu7s7` и `claude/release-2026-09-13-9`
  можно удалять.

### claude/ecstatic-lovelace-s84oc8 — соглашение о намерениях на аренду
По команде владельца «да» (в ответ на «Публиковать сейчас?»). PR
[#90](https://github.com/anatolytrashman-del/Redevelopment/pull/90) из
`claude/release-2026-09-13-8` в `oodobu`, мердж-коммит `147dc65`, Vercel
`dpl_CxiBg7y6PpDvouPh6NZQzxCoY2jv` — см. журнал за 2026-09-13 итоговый статус
деплоя. Оба гугл-шаблона для аренды владелец подготовил и прислал ссылки,
обе строки заведены в `document_templates`
(`1a969194-8b7f-477f-b1dc-26939749107a` — кабинет,
`b9b9d09b-ed25-4070-8e5a-709875f69bfb` — рабочее место), обе переменные
(`RENT_AGREEMENT_TEMPLATE_ID`/`RENT_WORKSTATION_AGREEMENT_TEMPLATE_ID`)
владелец добавил в Vercel — реальное подписание аренды должно работать
полностью после этого деплоя.
- **Что:** по аналогии с существующим флоу подписания соглашения о намерениях
  на покупку кабинета/рабочего места сделан параллельный флоу для аренды
  (ставка $20/м²/мес для кабинетов, $100/мес для фиксированных рабочих мест).
  Код полностью готов и покрывает весь путь:
  - `agreement_signatures` получила колонку `deal_mode` ('sale'/'rent',
    NOT NULL DEFAULT 'sale' — старые строки не ломаются), `objects` —
    `rent_intent_agreement_file` (аналог `intent_agreement_file`, но для
    превью-PDF аренды);
  - `AgreementSigningFlow`/`PublicPlanAndUnits` передают `dealMode` в
    `api/agreement-otp-request.js`, тот пишет его в базу и добавляет
    "· аренда" в письмо с кодом;
  - `api/agreement-otp-verify.js` выбирает один из ЧЕТЫРЁХ шаблонов
    (кабинет/место × покупка/аренда) по `deal_mode`×`is_workstation`;
  - `IntentAgreementDocument.tsx` (превью текста соглашения на сайте)
    получил `dealMode` — текст для аренды переведён (Арендодатель/
    Арендатор, ставка $20/м²/мес вместо $2100/м² разово, формулировки про
    готовность помещения к передаче в аренду вместо оформления права
    собственности);
  - в админке (`ObjectFormModal.tsx`) появилось отдельное поле загрузки
    "Шаблон соглашения о намерениях — аренда" (PDF-превью для сайта),
    параллельно существующему полю для покупки;
  - `Documents.tsx` в списке подписанных соглашений показывает
    "Аренда"/"Покупка"; бронь с сайта в режиме аренды помечает лида
    строкой "Формат сделки: аренда" в поле requirement.
- **Проверено:** `npx tsc -b` и `npm run build:app` чистые (и на релизной
  ветке тоже, отдельно). Мок-тест на локальном dev-сервере (Playwright +
  `page.route()`): превью соглашения корректно переключается между
  "Аренда"/"Покупка" без утечки формулировок в обратную сторону; полный
  флоу брони кабинета в режиме "Аренда" — тело запроса на
  `/api/agreement-otp-request` подтверждённо несёт `dealMode: "rent"`.
  Реальную (неотмоканную) отправку OTP-письма и генерацию PDF на проде для
  аренды после деплоя владелец ещё не проверял сам — стоит один раз
  пройти флоу до конца на реальных данных, особенно для рабочих мест.
- **Миграции SQL:** применены заранее, аддитивно, обратно совместимы:
  `alter table agreement_signatures add column deal_mode text not null
  default 'sale'`; `alter table objects add column
  rent_intent_agreement_file jsonb`; `NOTIFY pgrst, 'reload schema'`.

### claude/affectionate-allen-fyu7s7 (второй заход) — вкладка «Верификация» на странице Закупки
По команде владельца «мерджи, готов смотреть». PR
[#88](https://github.com/anatolytrashman-del/Redevelopment/pull/88) из
`claude/release-2026-09-13-6` в `oodobu`, мердж-коммит `fd3c0d6`, Vercel
`dpl_2D9897C5LvxUi8RBV3xksaNSmMTi` — READY, законно быстрый режим
(публичный код не менялся, только админка): 286 путей, 284 скопировано с
прода, 2 отрендерено (ровно как запланировано), 0 из-за непригодной копии,
ошибок в логе нет.
- **Что:** новая вкладка «Верификация» на странице Закупки (после
  «Поставщики»), по аналогии с верификацией объявлений для Светланы
  (`MarketOffersReview.tsx`): карточка с чек-листом товарных категорий
  (справочник `data/supplyCategories.ts`) + отдельное позиционированное
  окно с сайтом поставщика (iframe-embed невозможен — те же
  X-Frame-Options/CSP, что у Kufar/Realt). Режим «Начать верификацию» —
  очередь-без-индекса как в `MarketOffersReview`: карточка и окно с сайтом
  открываются сами, «Пропустить»/«Завершить проверку» в шапке. Одобрение —
  на весь снимок сайта разом (одна колонка `categories` на хост), поэтому
  очередь и список считают уникальных поставщиков (доменов), а не карточки-
  предложения — «если поставщика апрувнули в одной категории, значит он
  апрувнулся и во всех» реализовано структурно. Бейдж числа непроверенных —
  на самой вкладке.
- **Проверено:** `npm run build:app` чистый в исходной ветке и повторно в
  релизном worktree после мерджа (конфликтов при мердже не было); мок-тест
  через временный `__ComponentTest.tsx` + Playwright — список (фильтры/
  поиск), карточка с чек-листом и заметкой классификатора, очередь («Начать
  верификацию» → карточка+счётчик → «Одобрить»), временные файлы удалены.
  После публикации — живой прод: JS-чанк `Suppliers-lDG9PFqG.js` отдаётся
  200 и содержит «Верификация», «Начать верификацию»,
  `supplier-site-verification` (имя позиционированного окна).
- **Миграции SQL:** ПРИМЕНЕНА до мерджа кода, напрямую через Supabase
  Management API —
  `supabase/migrations/20260913-supplier-site-snapshots-verification.sql`
  (колонки `supplier_site_snapshots.categories_verified` bool default false,
  `categories_verified_at`). Аддитивная, старый код их не читал.
- Ветки `claude/affectionate-allen-fyu7s7` и `claude/release-2026-09-13-6`
  можно удалять.

### claude/zealous-volta-ahvsoi — период «Вчера», сброс истории посещаемости, синк в 2:00 МСК
По прямой команде владельца «пушь на прод и мерджи после готовности» —
публикация без очереди, сразу в этой же сессии. PR
[#86](https://github.com/anatolytrashman-del/Redevelopment/pull/86) из
`claude/zealous-volta-ahvsoi` в `oodobu`, мердж-коммит `b925435`, Vercel
`dpl_9e3oK11jGTnLq2upCjZtSk56qhPo` — READY за ~1 мин, законно быстрый режим
(публичный код не менялся, только админка/скрипты): 286 путей, 284
скопировано с прода, 2 отрендерено (ровно как запланировано), 0 из-за
непригодной копии.
- **Что:** на `/admin/site-metrics` добавлен период «Вчера» (ровно один
  календарный день — предпоследняя строка дневного ряда, а не скользящее
  окно, как у 7/30/90); `scripts/sync-yandex-metrika.mjs` сузил окно запроса
  к Метрике для дневных рядов (визиты по дням, достижения цели) до
  «вчера+сегодня» (`TREND_WINDOW_DAYS=2`) вместо полных 90 дней — иначе
  следующий же прогон заново перезатянул бы всю историю поверх очищенных
  данных; источники трафика/топ страниц (не дневной снимок) не тронуты.
  Cron синка сдвинут с 04:00 на 23:00 UTC = 02:00 по Москве/Минску.
- **Проверено:** `npx tsc -b` и `npm run build:app` чистые; мок-тест через
  временный роут + Playwright с замоканными ответами Supabase (реальный
  Supabase из песочницы сессии оказался недоступен через прокси для
  headless-браузера — `ws_closed_mid_exchange`/`ERR_CONNECTION_RESET`,
  замена не по коду, а по среде) — период «Вчера» показывает ровно
  вчерашний день, 7/30/90 работают как раньше.
- **CI:** штатная проверка `ci` в GitHub Actions красная
  (`browserType.launch: ... /opt/pw-browsers/chromium` не существует) —
  это не связано с диффом, `scripts/prerender.mjs` жёстко использует путь
  Chromium из песочницы Claude Code, когда `VERCEL` не задан, поэтому
  `npm run build` в обычном раннере ci.yml всегда падает на этом шаге
  (см. идентичный сбой на только что влитом #82). Оставлен комментарий на
  PR с объяснением, отдельная задача — не в рамках этой правки.
- **Миграции SQL:** DDL нет. Данные: таблица `yandex_metrika_daily_stats` в
  Supabase очищена до вчера+сегодня (`DELETE ... WHERE date < (SELECT
  MAX(date) ...) - INTERVAL '1 day'`) напрямую через Supabase Management
  API в этой же сессии, до мерджа кода — было 22 строки (22 авг — 12 сен),
  осталось 2 (11–12 сен).
- Ветку `claude/zealous-volta-ahvsoi` можно удалять после того, как
  владелец подтвердит на живом проде.

### claude/wizardly-babbage-ijfnwn — фильтр ботов из счётчика "онлайн"
По команде владельца «мерджи». PR
[#84](https://github.com/anatolytrashman-del/Redevelopment/pull/84) из
`claude/release-2026-09-13-5` в `oodobu`, мердж-коммит `2889895`, Vercel
`dpl_7UPWxwuoeErAiL5VH8wxJ1nWaRCR` — READY за 6 мин, законно ПОЛНЫЙ режим
(отпечаток публичного кода изменился — `onlinePresence.ts` в графе
`App.tsx`, общего для публичного и админского бандла), ошибок в логе нет.
- **Что:** `src/lib/onlinePresence.ts` — presence-трекер публичных страниц
  (`useOnlinePresenceTracker`, зовётся из `App.tsx` для всего, что не
  `/admin`) больше не джойнится в канал `online-visitors`, если браузер
  палится как автоматизированный: `navigator.webdriver === true` (так
  Playwright/Puppeteer/Selenium — то, на чём работает большинство ИИ-агентов
  и скрейперов, выполняющих JS страницы) или `User-Agent` совпадает с
  известными ботами/краулерами (GPTBot, ClaudeBot, PerplexityBot, ahrefs,
  semrush, facebookexternalhit и т.п.). Повод: владелец видел стабильные
  3-4 "онлайн" в сайдбаре на страницах без реальных посетителей.
- **Проверено:** `npm run build:app` чистый (tsc + vite build) — в исходной
  сессии и повторно в релизном worktree перед мерджем. Ручного прогона с
  реальным Playwright-визитом на прод не делал — полагаюсь на
  `navigator.webdriver`/UA-эвристику, без гарантии против продвинутого
  агента, который её подделывает.
- **Миграции SQL:** нет.
- **Конфликты при сборке:** не было — единственный тронутый файл
  (`onlinePresence.ts`) ни в одной другой ветке очереди не встречался.

### claude/cool-clarke-hlft8j — новый раздел «Коллаборации» в меню Маркетинг
По команде владельца «публикуй очередь», затем «мерджи». PR
[#82](https://github.com/anatolytrashman-del/Redevelopment/pull/82) из
`claude/release-2026-09-13` в `oodobu`, мердж-коммит `2ea0b0b`, Vercel
`dpl_AZNrgwPf9LhGm3ADK7b4wPaqLQwV` — READY за 6 мин, законно ПОЛНЫЙ режим
(отпечаток публичного кода изменился, `b61c10d7...` → `a00b797b...` —
правки задели `src/App.tsx`, общий файл для публичного и админского
бандла, хотя сам новый роут админский).
- **Что:** новая страница `/admin/collaborations` (пункт «Коллаборации» в
  группе меню «Маркетинг», после «Аналитика рынка»). Карточки партнёров:
  партнёр, способ связи (растущий список, старт Telegram/Email) + сам
  контакт, ссылка, «о чём договариваемся», статус (растущий список, старт
  Обсуждаем/Договорились/В работе/Приостановлено/Завершено). Паттерн 1-в-1
  как у «Финансирование» (карточка + модалка), без загрузки логотипа.
  Новый `PageKey: 'collaborations'` — виден всем профилям с `pages: 'all'`,
  остальным по умолчанию недоступен, пока не добавлен явно в /admin/settings.
- **Проверено:** `npm run build:app` чистый (tsc + vite build) — и в
  исходной сессии, и повторно в релизном worktree перед мерджем; мок-тест
  через временный `<Route>` на `/__test-collab` + Playwright — карточки,
  модалка добавления, `AddableSelect` для статуса открываются и выглядят
  корректно. ПОСЛЕ публикации — живой прод: `curl https://redevelopment.pro/
  admin/collaborations` → 200, `<title>Коллаборации — админка
  Redevelopment</title>` (свой шелл сработал). Реального сохранения записи
  в БД через форму на живых данных не делал — Supabase был недоступен из
  песочницы исходной сессии; полагаюсь на то, что API-слой (`collaborationsApi.ts`)
  один в один повторяет уже рабочий `financingApi.ts`. Стоит проверить
  живьём при случае.
- **Миграции SQL:** применена. Новая таблица `public.collaborations`
  (partner, contact_method, contact, link, agreement, status, created_at)
  + RLS `authenticated_all` (как у `financing_offers`) — см.
  `supabase/migrations/20260913-collaborations.sql`. Аддитивная, старому
  коду не мешала до публикации.
- Ветку `claude/cool-clarke-hlft8j` (и служебную `claude/release-2026-09-13`)
  можно удалять.

### claude/affectionate-allen-fyu7s7 — категория «Лестницы, перила и ограждения» + починка каталога поставщиков
По команде владельца «замерджи все, я гляну, как все это выглядит». PR
[#80](https://github.com/anatolytrashman-del/Redevelopment/pull/80) из
`claude/release-2026-09-13-4` в `oodobu`, мердж-коммит `83d5dce`, Vercel
`dpl_FC4qEFGtnVCnEPY56rzniSD6EzNH` — READY, быстрый режим (публичный код не
менялся, только админка): 286 путей, 284 скопировано с прода, 2 отрендерено
(ровно как запланировано), 0 из-за непригодной копии, ошибок в логе нет.
- **Что:** страница «Поставщики» → «Каталог поставщиков» — новая плитка
  «Лестницы и ограждения» в хабе «Двери, окна и перегородки» (30 профильных
  держателей по собранному ассортименту — керамогранит/сантехника и т.п.
  уже были, этой группы не было совсем). Заодно почищен баг: у 1198
  карточек поставщиков поле `items` (в приложении — позиции КП, не
  ассортимент) по ошибке содержало текст ассортимента вместо позиций счёта
  и ломало бы таблицу «Список материалов» в карточке — очищено.
- **Проверено:** `npm run build:app` чистый в релизном worktree; после
  публикации — живой прод отвечает (`curl https://redevelopment.pro/` →
  307, алиас `redevelopment.pro` привязан к этому деплою, `target:
  production`).
- **Миграции SQL:** нет — все правки данных (категории в
  `supplier_site_snapshots`, починка `items` в `supplier_research_offers`)
  применены напрямую через Supabase Management API в этой же сессии, до
  мерджа кода.
- Ветки `claude/affectionate-allen-fyu7s7` и служебную
  `claude/release-2026-09-13-4` можно удалять.

### claude/ecstatic-lovelace-s84oc8 (второй заход) — правки после первой публикации переключателя Покупка/Аренда
По команде владельца «мерджи, посмотрю все сам». PR
[#79](https://github.com/anatolytrashman-del/Redevelopment/pull/79) из
`claude/release-2026-09-13-3` (собрана из `claude/ecstatic-lovelace-s84oc8`) в
`oodobu`, мердж-коммит `ee08fff`, Vercel `dpl_AfKBDXkBC429TZdCrU8QjJZ42Ph7` —
READY за 6 мин, законно ПОЛНЫЙ режим (публичный код снова изменился,
`5b9ee2e5...` → `b61c10d7...`).
- **Что:** владелец посмотрел опубликованный переключатель "Покупка"/"Аренда"
  на `/minsk/one` (первый заход — запись выше) и попросил 4 правки: (1)
  "Аренда" — новый дефолт вместо "Покупка"; (2) тумблер в шапке — рядом с
  логотипом в один ряд, было отдельной строкой под ним; (3) все суммы в
  режиме "Аренда" — с суффиксом "/мес" (заголовок, обе карточки цен, таблица
  кабинетов, карточка конкретного кабинета); (4) на скриншоте владельца
  внизу страницы в режиме "Аренда" было огромное пустое пространство, а под
  ним как будто оторванные от карточки 3 ссылки блока "Онлайн-бронирование".
  Заодно, отдельным сообщением, — кнопка в шапке "Написать собственнику" →
  "Задать вопрос".
- **Про пункт (4):** живьём воспроизвести на проде из песочницы сессии не
  удалось (исходящий прокси рвёт Chromium до `redevelopment.pro`,
  `ERR_CONNECTION_RESET`, при этом `curl` к тому же URL проходит нормально) —
  исправлены оба технически обоснованных источника проблемы: явные `key` у
  всех блоков ниже hero-секции (без них React при переключении режима
  пересоздавал `PublicPlanAndUnits`/`BookingTermsCard`/`FaqCard` целиком,
  сверяя соседей по смещённой позиции, а не по key) и убран принудительный
  `min-h-svh` (в "Аренде" на 2 блока меньше контента, чем в "Покупке" —
  раньше это растягивалось пустым фоном до высоты экрана).
- **Проверено:** `npx tsc -b`/`npm run build:app` чистые в релизном
  worktree; Playwright + мок-Supabase — переключение Аренда→Покупка→Аренда
  чистое, без артефактов, `scrollHeight` короче в аренде, как и ожидается.
  ПОСЛЕ публикации — живой прод: `curl https://redevelopment.pro/minsk/one`
  → 200, в HTML дефолт "Аренда" (`от $100/мес`, `от $200/мес`, "Помещение с
  чистовой отделкой"), кнопка "Задать вопрос" на месте, `min-h-svh` в
  разметке больше не встречается.
- **Миграции SQL:** нет.
- **Что владелец сказал сам проверить:** живьём покликать тумблер туда-
  обратно на реальных данных несколько раз подряд — баг №4 подтверждён
  только косвенно (устранены вероятные причины), не воспроизведён напрямую
  в этой сессии.
- Отдельно, не блокирует: JSON-LD FAQ (`setFaqJsonLd` в
  `ObjectLandingPage.tsx`) по-прежнему не зависит от `dealMode` и всегда
  про покупку — теперь, когда дефолт страницы "Аренда", это расхождение
  между тем, что видит пользователь, и тем, что читают краулеры, стало
  заметнее. Осознанно не трогалось (не просили) — отдельная задача при
  желании.
- Ветку `claude/ecstatic-lovelace-s84oc8` (и служебные
  `claude/release-2026-09-13-2`, `claude/release-2026-09-13-3`) можно
  удалять.

### claude/ecstatic-lovelace-s84oc8 — переключатель "Покупка"/"Аренда" на лендинге Red One
По команде владельца «мерджи». PR
[#78](https://github.com/anatolytrashman-del/Redevelopment/pull/78) из
`claude/release-2026-09-13-2` (собрана из `claude/ecstatic-lovelace-s84oc8`) в
`oodobu`, мердж-коммит `614b8ff`, Vercel `dpl_C8Hm94iuFmXGtYg3dAZBcyswdGe8` —
READY за 6 мин. Первый прод-билд после публикации законно ПОЛНЫЙ — отпечаток
публичного кода изменился (`aa22ed4f...` → `5b9ee2e5...`, лог: «публичный код
изменился — полный режим», «ПОЛНЫЙ режим (код публичных страниц изменился) —
рендерю каждый путь headless-браузером»): правки задели общие публичные
компоненты объектной страницы (`ObjectLandingPage.tsx`,
`AvailableUnitsTable.tsx`, `PublicPlanAndUnits.tsx`, `BookingTermsCard.tsx`,
`FaqCard.tsx`, `data/buildingPlans.ts`). 291 og-обложка отрендерена заново
(0 скопировано с прода) — тоже ожидаемо при полном режиме. Ошибок в логе
сборки нет.
- **Что:** на лендинге Red One (`/minsk/one`) в шапке — переключатель
  "Покупка"/"Аренда" (`ToggleGroup`, состояние компонента, не роут — SEO/URL
  не меняются). "Покупка" — прежняя страница без изменений, по умолчанию.
  "Аренда" — та же вёрстка, другие цены и часть блоков скрыта: заголовок
  "от $100" вместо "от $12 000"; карточки цен — "Кабинеты 11–40 м² — от
  $200" (подпись "Помещение с чистовой отделкой" вместо условий рассрочки) и
  "Фиксированное рабочее место — от $100"; таблица доступных кабинетов и
  карточка конкретного кабинета считают цену по новым ставкам — офисы
  $20/м², места $100 (`priceForDeal`/`workstationPriceForDeal`/
  `pricePerMeterForDeal` в `src/data/buildingPlans.ts`), а строка "Первый
  взнос" в карточке кабинета скрыта (у аренды нет рассрочки/лизинга/
  кредита); блоки "3 варианта покупки, если нет полной суммы" и "Помещения
  с арендаторами для инвесторов" показываются только в режиме "Покупка";
  FAQ прячет вопросы про покупку в режиме "Аренда" (`FaqCard.tsx`, поле
  `purchaseOnly`). Отдельно, в обоих режимах — из блока "Онлайн-
  бронирование без предоплаты" убран 4-й шаг "Оплатите" (владелец: оплаты в
  самой брони нет вообще), оставшиеся 3 шага растянуты на всю ширину блока.
- **Проверено:** `npx tsc -b` и `npm run build:app` чистые на релизной
  ветке; визуально до публикации — Playwright с подменой ответов Supabase
  через `page.route` (реальный прод-проект был недоступен из песочницы
  сессии через outbound-прокси); ПОСЛЕ публикации — живой прод:
  `curl https://redevelopment.pro/minsk/one` → 200, в HTML присутствуют оба
  пункта тумблера ("Покупка"/"Аренда") и цены режима "Покупка" по
  умолчанию.
- **Миграции SQL:** нет.
- **Что стоит проверить ещё:** живое переключение на "Аренда" глазами в
  браузере (числа проверялись только в мок-прогоне) и раскладку шапки с
  тумблером на узком экране — в этой сессии не проверялись.
- Ветку `claude/ecstatic-lovelace-s84oc8` (и служебную
  `claude/release-2026-09-13-2`) можно удалять.

### claude/fervent-thompson-xm2a6x — имя отправителя писем поставщикам
По команде владельца «мерджи». PR
[#77](https://github.com/anatolytrashman-del/Redevelopment/pull/77) из
`claude/release-2026-09-13` (собрана из `claude/fervent-thompson-xm2a6x`) в
`oodobu`, мердж-коммит `18519bf`, Vercel `dpl_2p1UyX4Pc44smq3T5AJqwYR6nAo4`
— READY за ~50 сек, законно БЫСТРЫЙ режим (286 путей, 284 скопировано с
прода, «из-за непригодной копии: 0»).
- **Что:** имя отправителя писем поставщикам (`RESEND_FROM_NAME`) изменено с
  «Redevelopment Закупки» на «Анатолий Трэшмен» — во всех местах отправки:
  `api/purchase-send-email.js` (Vercel), Edge Functions
  `process-bulk-send-jobs` и `process-outgoing-emails`, и ручной запасной
  скрипт `scripts/process-bulk-send-jobs.mjs`.
- **Проверено:** `npm run build:app` чистый в релизном worktree; лог сборки
  на Vercel без ошибок; живой прод `curl -L https://redevelopment.pro/` →
  200. Отправку письма поставщику вручную из админки на живом проде глазами
  не проверяли — просили владельца сделать это самостоятельно (см. test
  plan в PR).
- **Миграции SQL:** нет. Обе Edge Function были задеплоены напрямую через
  Supabase Management API ещё в исходной сессии (до этого PR) —
  `process-bulk-send-jobs` → версия 6, `process-outgoing-emails` → версия 3.
- Ветку `claude/fervent-thompson-xm2a6x` (и служебную `claude/release-2026-09-13`)
  можно удалять.

### claude/dazzling-knuth-w7ovo8 — новый title/description и og-превью `/minsk/minsk-mir`
PR [#73](https://github.com/anatolytrashman-del/Redevelopment/pull/73) из
`claude/dazzling-knuth-w7ovo8` в `oodobu`, мердж-коммит `761efe8`, Vercel
`dpl_3UniGDVV14Yy4eV3cMPRGRNZ7QUG` — READY за 6 мин (по команде владельца
«Делай все, мерджи»). Первый прод-билд после публикации законно ПОЛНЫЙ —
отпечаток публичного кода изменился из-за правки `scripts/generate-og-cards.mjs`
(этот файл входит в `public-build-id`, см. CLAUDE.md): в логе
«291 страниц со своей обложкой ... отрендерено 291, скопировано с прода 0».
- **Что:** новые title/description для `/minsk/minsk-mir` (задача владельца
  af839571-minsk-mir-meta-task — под запросы про цены/помещения/аналитику) +
  перегенерированная og-картинка превью с новым текстом (заголовок короче,
  чем `<title>`, плюс подпись «Обновляется ежемесячно · redevelopment.pro»,
  точечный оверрайд только для этого пути в `generate-og-cards.mjs`).
  Видимый `<h1>` страницы не менялся. Другие страницы (включая topic-
  подстраницы `/minsk/minsk-mir/:topic`) не затронуты.
- **Проверено:** `npm run build:app` чистый; локальный прогон
  `PRERENDER_ONLY=minsk/minsk-mir` + `generate-og-cards.mjs` до публикации;
  ПОСЛЕ публикации — живой прод: `curl https://redevelopment.pro/minsk/minsk-mir`
  отдаёт новые `<title>`/`<meta name="description">`/`canonical`/`og:*` (в т.ч.
  `og:image` → `/og/minsk-minsk-mir.png`), сама картинка 200 `image/png`,
  визуально — новый заголовок и подпись поверх фирменного красного макета.
- **Миграции SQL:** нет.
- **Что сказать владельцу:** после публикации нужно сбросить кэш превью в
  Telegram через `@WebpageBot` (отправить ему ссылку на страницу) — Telegram
  кэширует старую карточку надолго.
- Ветку `claude/dazzling-knuth-w7ovo8` можно удалять.

### claude/dazzling-knuth-w7ovo8 (заход 2) — заголовок og-картинки `/minsk/minsk-mir` сжат до 3 строк
Владелец, сразу после релиза выше: «на картинке превью 4 строчки текста —
воспринимается тяжело, умести в 3 строчки то же самое». PR
[#74](https://github.com/anatolytrashman-del/Redevelopment/pull/74) из той же
ветки в `oodobu`, мердж-коммит `5eb8b90`.
- **Что:** в `CARD_TEXT_OVERRIDES['minsk/minsk-mir']` (`scripts/generate-og-cards.mjs`)
  заголовок картинки заменён с «Коммерческая недвижимость Минск Мира —
  аналитика и цены» на «Коммерческая недвижимость Минск Мира: цены и
  аналитика» — тот же смысл, тире на двоеточие и обратный порядок «цены и
  аналитика» укладывают текст в 3 строки вместо 4 при автоподборе размера
  шрифта (`FIT_SCRIPT`). `<title>`/`description`/canonical не трогались.
- **Проверено:** локальным рендером карточки (мок-скрипт с той же
  `CARD_CSS`/`FIT_SCRIPT`, что в `generate-og-cards.mjs`) перебрано несколько
  формулировок, выбрана уложившаяся в 3 строки без обрезки смысла; после
  этого прогнан полный `PRERENDER_ONLY=minsk/minsk-mir` +
  `generate-og-cards.mjs` — картинка визуально 3 строки. `npm run build:app`
  чистый.
- **Миграции SQL:** нет.
- **Особенность прод-билда:** пока шёл этот прод-билд, в `oodobu` параллельно
  прилетели ещё два пуша от других сессий (прямой мердж «гибкие колонки» и
  PR #75 «индикатор N онлайн») — на Hobby-плане команды разрешён 1
  одновременный билд, поэтому Vercel не стал собирать промежуточные коммиты
  отдельно, а собрал сразу самый свежий HEAD (`2fa9b33`, деплой
  `dpl_B35j4p6wWYe6fD9ph6GK5h7vh8Aa`, READY) — этот коммит содержит все три
  публикации разом, мой в том числе. Если в API-опросе деплоев Vercel
  внезапно не видно ожидаемого коммита — сначала проверить запрос с явным
  `teamId=team_kyP6ogW1N5p9HN17G6Sg7iHV` (без него `api.vercel.com` отдаёт
  неполный/не тот список, из-за чего билд короткое время выглядел
  «пропавшим»), а не считать деплой зависшим.
- Проверено на живом проде уже ПОСЛЕ этого билда: `/og/minsk-minsk-mir.png`
  — ровно 3 строки заголовка.

### claude/dazzling-knuth-w7ovo8 (заход 3) — убран дублирующийся домен из подписи og-картинки
Владелец: «два раза упоминается домен, убери его из нижней строки». PR
[#76](https://github.com/anatolytrashman-del/Redevelopment/pull/76) из той же
ветки в `oodobu`, мердж-коммит `772ca0d`, Vercel `dpl_69DcTgJNFCnuwCydQLCG3jCY69Ny`
— READY (fast-forward, без параллельных пушей на этот раз).
- **Что:** подпись под заголовком на og-картинке `/minsk/minsk-mir`
  (`CARD_TEXT_OVERRIDES['minsk/minsk-mir'].kicker` в
  `scripts/generate-og-cards.mjs`) была «Обновляется ежемесячно ·
  redevelopment.pro» — домен уже есть в шапке карточки рядом с «R», внизу
  дублировался. Убран до «Обновляется ежемесячно».
- **Проверено:** `npm run build:app` чистый; `PRERENDER_ONLY=minsk/minsk-mir`
  + `generate-og-cards.mjs` локально, визуально — домен один раз, заголовок
  по-прежнему 3 строки; после публикации то же самое подтверждено на живом
  проде.
- **Миграции SQL:** нет.
- Ветку `claude/dazzling-knuth-w7ovo8` можно удалять (после всех трёх заходов).

### claude/optimistic-ramanujan-69f6jz — индикатор "N онлайн на сайте" в админке
По команде владельца «мерджи». PR
[#75](https://github.com/anatolytrashman-del/Redevelopment/pull/75) из
`claude/optimistic-ramanujan-69f6jz` в `oodobu`, мердж-коммит `2fa9b33`. Тот
же прод-деплой `dpl_B35j4p6wWYe6fD9ph6GK5h7vh8Aa` (READY, 6 мин), что собрал
заодно og-картинку `/minsk/minsk-mir` (заход 2, см. выше) и прямой мердж
«гибкие колонки» — Hobby-план, три пуша слились в одну сборку HEAD `2fa9b33`.
Полный прод-билд был законен и без этого совпадения: `App.tsx` входит в
публичный граф (`public-build-id`), правка в нём (новый хук трекера) сама по
себе меняет отпечаток.
- **Что:** живой счётчик "сколько человек сейчас на сайте" в админке — под
  логотипом, справа от имени пользователя в Sidebar. Видно только Трэшмену
  (гейт `isSuperAdminAllowed`, тот же, что у пункта меню "Метрики").
  Реализовано через Supabase Realtime Presence (`src/lib/onlinePresence.ts`):
  все маркетинговые страницы (всё, что не под `/admin`, тот же критерий, что
  уже использует хук pageview-хитов Метрики/VK-пикселя) джойнятся в общий
  канал `online-visitors` под случайным ключом на вкладку; Sidebar слушает
  `presence sync` того же канала и считает участников, сам не трекается
  (открытая CRM не прибавляет единицу к счётчику). Ни своей таблицы, ни
  поллинга — обновление по сокету, почти мгновенное.
- **Проверено:** `npm run build:app` чистый и до, и после мерджа oodobu;
  Vercel-деплой READY без ошибок в логе (проверены события сборки через
  `api.vercel.com`, ни одной строки с error). Живой WebSocket-коннект к
  Supabase Realtime с прода из ЭТОЙ сессии не проверялся — сетевой прокси
  сессии явно не поддерживает WebSocket upgrade (см. `/root/.ccr/README.md`,
  раздел "Not supported through the proxy"), это ограничение среды
  выполнения агента, не самого сайта. `curl https://redevelopment.pro/minsk`
  отдаёт 200. Нужна одна ручная проверка глазами в настоящем браузере:
  открыть `/minsk` (или любую публичную страницу) в одной вкладке и
  `/admin` рядом — счётчик в сайдбаре должен показать число и увеличиться на
  единицу, пока открыта первая вкладка.
- **Миграции SQL:** нет — presence не трогает БД вовсе, только in-memory
  realtime-сервер Supabase.
- **Что сказать владельцу:** попросить один раз проверить счётчик глазами
  (см. пункт выше) — в этой сессии живой сокет не тестировался технически
  (ограничение прокси, не подозрение на баг).
- Ветку `claude/optimistic-ramanujan-69f6jz` можно удалять.

### Релиз 2026-09-12 (десятый — «база/гипермаркет» тоже по сайту)
PR [#68](https://github.com/anatolytrashman-del/Redevelopment/pull/68) из
`claude/affectionate-allen-fyu7s7` в `oodobu`, мердж-коммит `cb1889d`,
Vercel `dpl_a6keg3Kw5aNayDv6iiCHJw6yeyUk` — READY за ~1 минуту. Владелец
попросил объяснить и навести порядок в логике баз/гипермаркетов; разбор
нашёл 20 явных гипермаркетов (включая Бауцентр и TM.by — названы примерами
в описании хаба), заведённых в узкие категории и нигде не видных как базы,
и треть из 53 заведённых как «Универсальные» без подтверждения сайтом.
Владелец выбрал (`AskUserQuestion`) «сделать сайт источником и здесь» —
`isUniversalOffer()` в `SupplierCatalog.tsx` считает базой и по товарной
группе «Строительный гипермаркет» со снимка сайта, не только по строке
закупки. Проверено на живом проде: JS-чанк `Suppliers-DRbuegOJ.js` (новый
хеш) отдаётся 200. Дедуп дублей компаний (Альбия×3, Центр Красок×2) и
пограничные случаи (8–13 групп) — не делал, отдельная задача. SQL не
требовалась.

### Релиз 2026-09-12 (девятый — флаг страны в шапке, поиск, старый список свёрнут)
PR [#66](https://github.com/anatolytrashman-del/Redevelopment/pull/66) из
`claude/affectionate-allen-fyu7s7` в `oodobu`, мердж-коммит `20cf9ec`,
Vercel `dpl_HURs7wSHZcmPdgraVnYobmpgv4Pd` — READY за ~1 минуту. Владелец
тремя правками подряд уточнил, как должна работать страна в новом каталоге,
плюс попросил свернуть старый плоский список категорий закупки под ним.
Итог: переключатель флагов Россия/Беларусь в шапке каталога (не списки друг
под другом), фильтрует хабы/категории/списки разом; строка поиска
поставщика справа от заголовка, флаги — справа от поиска; старый список
`RequestCard` по группам `materials`/`services` в `Suppliers.tsx` свёрнут в
`<details>` по умолчанию (сама функциональность — добавить/изменить/удалить
категорию, веб-поиск, дубли универсальных — осталась, каталог её не
дублирует). Проверено на живом проде: JS-чанк `Suppliers-CPzSM7YV.js`
(тот же хеш, что в локальной сборке) содержит «Поиск поставщика», «страна
не указана», «управление категориями закупки». SQL не требовалась.

### Релиз 2026-09-12 (восьмой, по команде «применяй для всех поставщиков и пушь на прод и мерджи»)
PR [#64](https://github.com/anatolytrashman-del/Redevelopment/pull/64) из
`claude/affectionate-allen-fyu7s7` в `oodobu`, мердж-коммит `24eaeb1`,
Vercel `dpl_3S7z8feRhcAmZ2CMLers87HqMEDS` — READY за ~1 минуту. Владелец
посмотрел седьмой релиз (плитка «Камень и агломерат»: 0 «своих», 14 «по
сайту тоже возят») и попросил снять деление на «подтверждённых вручную» и
«найденных по сайту» — теперь любой из двух признаков (строка закупки или
товарная группа со снимка сайта) даёт полноценное присвоение категории.
Затронуло все 26 категорий разом (общая функция подсчёта в
`SupplierCatalog.tsx`). Проверено на живом проде: JS-чанк `Suppliers-*.js`
содержит новую строку «Поставщики (» и «заведён как», старой «по сайту тоже
возят» в бандле больше нет. SQL не требовалась.

### Релиз 2026-09-12 (седьмой, по команде «пушь на прод и мерджи»)
PR [#63](https://github.com/anatolytrashman-del/Redevelopment/pull/63) из
`claude/affectionate-allen-fyu7s7` в `oodobu`, мердж-коммит `e144d1f`,
Vercel `dpl_Fo4iMMEKbg3jX8dPVjqoTkBnywfb` — READY за ~1 минуту (быстрый
режим пререндера). Одна ветка в пачке. После публикации осталось выполнить
`supabase/migrations/20260912-supplier-catalog-rename.sql` (переименование
10 строк категорий, код от него не зависит). Ветку `affectionate-allen`
можно удалять.

### claude/affectionate-allen-fyu7s7
- **Что:** (1) у каждого поставщика видно, что именно он поставляет —
  товарные группы («Керамогранит», «Подвесные потолки», «Кабель и провод»...)
  чипами в списке поставщиков и блоком «Что поставляет» в карточке; (2) новый
  КАТАЛОГ ПОСТАВЩИКОВ над списком категорий — 9 хабов → 26 категорий большими
  плитками как у ВсеИнструменты, на плитке категории три числа «своих N · по
  сайту +M · баз +K», внутри — список компаний в трёх корзинах (структура и
  метод — `docs/supplier-catalog.md`). Собирается само: Edge Function
  читает разделы каталога с сайта поставщика (без ИИ, бесплатно; если сайт не
  открывается — с архивной копии в Wayback Machine), сессия Claude Code
  раскладывает их по справочнику `src/data/supplyCategories.ts` (в рамках
  подписки, ProxyAPI не нужен).
- **Данные уже собраны:** 255 доменов из 259 с группами (269 из 278
  карточек), в среднем 5,9 группы. Публикация кода просто делает их видимыми
  в админке.
- **Проверено:** `npm run build:app` чистый; разбор снимка сайта прогнан
  юнит-тестом на фиктивном сайте (меню, корень каталога, sitemap-индекс,
  отсев служебных разделов и дублей); вид чипов — мок-страница со
  скриншотом.
- **Миграции SQL:** ПРИМЕНЕНЫ —
  `supabase/migrations/20260912-supplier-site-snapshots.sql` (таблица
  `supplier_site_snapshots`, функция `supplier_site_host`, триггер на
  `supplier_research_offers`, первичное заполнение 259 доменами, колонки
  `claimed_at` и `attempts`). Аддитивная: только новые объекты, ничего не переименовано и
  не ужесточено, старый прод от неё не ломается. Edge Function
  `process-supplier-jobs` уже задеплоена (версия 9) и наполняет таблицу —
  это тоже безопасно до публикации: фронт в проде о таблице пока не знает.
- **Риск при задержке:** нет. Данные копятся в базе, при публикации кода они
  просто станут видны.
- **Переименование строк категорий** («Ceresit» → «Сухие смеси, грунтовки и
  гидроизоляция», «Грильято 100х100» → «Потолки» и т.д., 10 строк) — SQL в
  `supabase/migrations/20260912-supplier-catalog-rename.sql`, НЕ выполнен
  (классификатор среды не пропустил UPDATE по живым данным). Код от него не
  зависит: плитки находят карточки и по старым названиям. Выполнить после
  публикации — только ради новых имён в шапках карточек категорий.
- **Конфликты при сборке:** `src/pages/Suppliers.tsx` (новый блок в карточке
  и чипы в списке, каталог вместо старого поиска по группам, прокидка
  `snapshotByHost`) и `supabase/functions/process-supplier-jobs/index.ts`
  (новый блок очереди снимков + вызов в `Deno.serve`). Остальное — новые
  файлы. `oodobu` на 2026-09-12 17:xx уже влит в ветку без конфликтов.


### Релиз 2026-09-12 (шестой, по команде «публикуй»)

Собран в `claude/release-2026-09-12-6` от `oodobu` (dee1a16), ОДНА ветка одним
PR: на момент команды в «Ждут публикации» кроме неё оставалась только осознанно
отложенная и уже устаревшая запись `claude/wonderful-einstein-vdg945` (по ней
публиковать нечего). Ветки `claude/release-2026-09-12-3` и
`claude/modular-platform-client-sales-r6fgw4` проверены отдельно — реальных
записей в очереди у них нет.

Конфликтов при сборке не было: `oodobu` на тот момент уже содержал соседний
релиз про пререндер (dee1a16), а правка трогает три файла в переписке с
поставщиками.

Владелец попросил ставить мердж сразу после уже шедшего прод-деплоя
(dpl_Cg4TaRHwEmhKDcn3w5czbbBv9J64) — дождались его READY и мерджили следом
(PR #61, мердж-коммит `0d1683f`).

CI на PR остался в `queued` и дождаться его было нельзя: троттлинг GitHub
Actions держал в очереди ВСЕ прогоны аккаунта, включая прогон на самом
`oodobu` (см. журнал, 2026-09-11). Шаги `ci.yml` прогнаны локально на
релизной ветке — oxlint, `tsc -b`, `npm test` (106), сборка, `audit:rls`.
Формальной галочки CI у релиза нет, это осознанный компромисс.

Прод-деплой подтверждён: `dpl_7YjMqBLfrYMAEoP4KFAV8KfNs1c5` READY за 1.0 мин,
в логе `[prerender] ИТОГ быстрого режима: путей 286, скопировано с прода 284,
отрендерено браузером 2 … из-за непригодной копии: 0` — быстрый путь отработал
как задумано.

### claude/kind-hamilton-bh7qd7
- **Что:** тема письма поставщику по умолчанию — «Закупка материалов». В
  массовой рассылке она подставлялась из названия категории запроса
  («Керамогранит Alma серая»), хотя прикреплённая ведомость почти всегда шире
  одной позиции — владелец получил жалобу на собственном письме. Формулировка
  теперь общая константа `DEFAULT_MATERIALS_SUBJECT`, ею же заменено прежнее
  «Поставка материалов» у единичного письма; название заявки, если оно задано,
  по-прежнему в приоритете (отдельная ветка переписки).
- **Проверено:** `npm run build:app` чистый. Правка — начальное значение поля
  «Тема», поле редактируемое, отдельного UI-прогона не требует.
- **Миграции SQL:** нет.
- **Риск при задержке:** низкий, но это ровно тот текст, который видят
  получатели рассылок: до публикации каждая новая массовая рассылка уедет со
  старой темой по названию категории.
- **Конфликты при сборке:** маловероятны — три файла
  (`src/lib/emailTemplates.ts`, `src/components/suppliers/BulkSendModal.tsx`,
  `src/components/suppliers/SupplierCorrespondenceTab.tsx`), в каждом по
  одной-двум строкам.

### Релиз 2026-09-12 (пятый, по команде «публикуй»)

Собран в `claude/release-2026-09-12-5` от `oodobu`, ОДНА ветка одним PR — на
момент команды в «Ждут публикации» больше ничего готового не было.
`npm run build:app`, `npx vitest run` (106 тестов), oxlint и `npm run audit:rls`
чистые.

Конфликт всё-таки был, но не при сборке релиза, а ПОСЛЕ: пока PR висел, соседняя
сессия опубликовала `claude/clever-curie-uv51xk` (запись ниже), `oodobu` уехал
вперёд и PR стал `dirty` по `docs/release-queue.md` и `docs/session-journal.md`.
Свежий `oodobu` влит в релизную ветку, обе записи сохранены — ровно тот случай,
под который в CLAUDE.md написано правило «сохранять обе».

Отпечаток `public-build-id` не изменился (`3e0e51ff...`, 119 файлов в публичном
графе) — правка чисто админская, значит прод-билд должен пойти БЫСТРЫМ путём
(~1 минута), полный пререндер здесь был бы багом (см. CLAUDE.md, «Сколько идёт
билд»).

### claude/bold-noether-qrzr3l
- **Что:** ведомость материалов перестала быть застывшим снимком — позиции,
  привязанные к смете, зеркалятся из неё на каждом рендере, поэтому правка
  сметы сразу попадает в .xlsx нового письма (владелец, 2026-09-12:
  «внёс изменения по керамограниту, а прикрепляется ведомость без
  изменений»). Ручные правки прямо в ведомости помечаются и синхронизацию
  переживают, у такой позиции в модалке есть кнопка «Вернуть из сметы».
- **Проверено:** `npm run build:app` чистый, `npx vitest run` — 106 тестов
  зелёные (8 новых в `src/lib/ledgerSync.test.ts`), мок-тест модалки в
  Playwright (подпись/кнопка, возврат значений из сметы).
- **Миграции SQL:** нет, не нужны (синхронизация чисто клиентская, схема
  не менялась; `ledgerOverrides` — новое необязательное поле внутри уже
  существующей jsonb-колонки `material_ledgers.items`).
- **Риск при задержке:** до публикации к письмам по-прежнему уезжает
  устаревшая ведомость — это и есть баг, из-за которого поставщики видят
  старые объёмы/примечания. Ждать без нужды не стоит.
- **Что сказать владельцу при публикации:** у ведомости «Краска интерьерная»
  ручные значения (1868/10510 л, примечание «(в 2 слоя)») заменятся сметными
  (2229/10233, «Нужна покраска в 2 слоя, нужно учесть в объеме») — метки
  ручной правки задним числом не восстановить. Если сметная формулировка
  поставщику не годится, поправить её в ведомости после публикации: теперь
  такая правка сохранится.
- **Конфликты при сборке:** только `docs/session-journal.md` и этот файл
  (обе записи сохранять). Код трогает `src/lib/ledgerSync.ts` (новый),
  `src/data/purchases.ts`, `src/pages/Suppliers.tsx` (два memo вокруг
  ведомостей), `src/components/suppliers/MaterialLedgerModal.tsx`.
- **Опубликовано:** PR #57 смерджен в `oodobu`, прод-деплой READY за 58с
  (быстрый режим пререндера: путей 286, скопировано с прода 284, браузером
  отрендерено 2 по списку ALWAYS_FULL_RENDER_PATHS, «из-за непригодной
  копии: 0»). Ветки `claude/bold-noether-qrzr3l` и
  `claude/release-2026-09-12-5` можно удалять.

Что НЕ вошло и почему:

- `claude/clever-curie-uv51xk` — уехала своим PR #58 параллельно, пока этот PR
  ждал; в релиз не включалась, её запись ниже;
- `claude/wonderful-einstein-vdg945` — как и в релизе №4, её содержимое уже в
  проде отдельным путём, запись устарела;
- `claude/modular-platform-client-sales-r6fgw4` — как и раньше, не в очереди.

### 2026-09-12 (по прямой команде владельца, отдельным билдом) — `claude/clever-curie-uv51xk`
- **Что:** в массовой рассылке появился повтор по получателям прошлой рассылки
  (владелец: "отправил неправильную ведомость по керамограниту... хочу написать
  всем, кому ошибно написал ранее"). Селект "Повторить прошлую рассылку" в
  BulkSendModal: выбираешь рассылку этой категории (дата, имя ведомости, автор,
  сколько писем), список получателей становится ровно её адресатами — новые
  поставщики категории туда не попадают, фильтр по стране на него не влияет;
  тема, текст и юрлицо подставляются из повторяемой рассылки. Плюс кнопка
  "Снять с очереди" для писем той рассылки, которые ещё не ушли (иначе старая
  ведомость придёт уже ПОСЛЕ исправленной) — снятые сразу отмечаются для
  повтора.
- **Проверено:** `npx tsc -b` и `npm run build:app` чистые; мок-тест
  BulkSendModal через временную `__ComponentTest.tsx` + Playwright —
  скриншоты трёх состояний (обычный режим, повтор с двумя письмами в очереди,
  после снятия с очереди), временные файлы удалены.
- **Миграции SQL:** нет вовсе. Читаем существующие `bulk_send_jobs`/
  `bulk_send_job_items`, новых колонок и таблиц не добавляли. Единственная
  новая запись в базу — статус `cancelled` у строки задания (колонка
  `status` — обычный text без CHECK); воркеры берут только `pending`, то есть
  старый прод-код такую строку просто не трогает.
- **Риск при задержке:** функциональный — до публикации повторить рассылку и
  снять с очереди ошибочные письма из интерфейса нельзя (в сценарии "ушла не
  та ведомость" это делается руками/через SQL). Ломающего ничего нет.
- **Конфликты при сборке:** `src/components/suppliers/BulkSendModal.tsx` и
  `src/lib/bulkSendJobsApi.ts` — если другая ветка правила ту же модалку.
  Журнал и эта очередь — как обычно, сохранять обе записи.
- **Опубликовано:** PR #58 смерджен в `oodobu`, прод-деплой READY за 44с
  (быстрый режим пререндера: скопировано с прода 284 из 286, «из-за непригодной
  копии: 0»). Ветку `claude/clever-curie-uv51xk` можно удалять.


### 2026-09-12 (вне очереди, по прямой команде владельца) — `claude/elegant-ptolemy-t4oy8r`
- **Дополнено (полный режим):** общий многопроцессный браузер и свой
  статический сервер в пререндере (600с → 376с на проде), OG-обложки в
  несколько вкладок, частичный режим для сохранений объектов
  (`deploy_debounce.scope`, `api/_rebuildScope.js`, `lib/publicRebuild.ts`,
  триггер пересборки у бизнес-центров). Миграция
  `supabase/migrations/20260912-deploy-debounce-scope.sql` применена
  (nullable-колонка, совместима со старым кодом). Тесты 108/108, `build:app`
  чистый. Подробно — журнал за 2026-09-12.
- **Что:** быстрые прод-билды для правок админки: быстрый путь пререндера
  переписан без сопоставления имён чанков (`scripts/prerender-snapshot.mjs`,
  причина 10-минутных билдов — дефисы в хэшах чанков ломали регулярку
  перенацеливания), OG-обложки в быстром режиме копируются с прода вместо
  рендера (~45с на каждой сборке). Подробно — журнал за 2026-09-12.
- **Проверено:** `npm run build:app` чистый, `npx vitest run` 96/96 (8 новых),
  oxlint чистый; реальный снапшот прода с дефисным хэшем переведён на
  локальную сборку и открыт Playwright — все ассеты 200, React смонтировался;
  `generate-og-cards.mjs` локально скопировал 5/5 обложек с прода.
- **Миграции SQL:** нет.
- **Публикация:** смерджена в `oodobu` напрямую (владелец: «пушь всё в прод,
  другие ветки сейчас не публикуются, это приоритетная правка»). Первый
  билд после неё — полный (отпечаток публичного кода изменился из-за
  добавления og-скрипта и шрифтов), дальше — быстрые.

### Релиз 2026-09-12 (четвёртый, по команде «можешь успеть опубликовать»)

Собран в `claude/release-2026-09-12-4` от `oodobu`, две ветки одним PR = один
прод-билд Vercel. `npm run build:app`, `npx vitest run` (88 тестов) чистые.
Конфликт был только в журнале (обе записи сохранены).

Что НЕ вошло и почему:

- `claude/release-2026-09-12-3` — снапшот, устаревший после релиза №3:
  предыдущая сессия признала его совпадающим с `oodobu`, но с тех пор в прод
  уехали PR #54 (откат подмешивания универсальных поставщиков в рассылку) и
  SEO-правки title. Мердж вернул бы оба отката назад — ветка отброшена,
  можно удалять;
- `claude/wonderful-einstein-vdg945` — её содержимое (очередь одиночных писем)
  уже в проде отдельным путём: таблица `outgoing_email_jobs`, Edge Function
  `process-outgoing-emails` и её крон в базе есть. Запись в «Ждут публикации»
  устарела, разобрать отдельно;
- `claude/modular-platform-client-sales-r6fgw4` — как и раньше, не в очереди.

### claude/zen-johnson-i7vjcg
- **Что:** новый файл `api/_belarus.js` — валидатор УНП и клиент реестра ГРП
  МНС. Ни к чему не подключён (так и сказано в коммите), на поведение прода
  не влияет; взят в релиз по просьбе владельца «чтобы и все старые правки
  внеслись».
- **Проверено:** сборка и тесты релизной ветки.
- **Миграции SQL:** нет.

### claude/sharp-meitner-ecztjj
- **Что:** массовая рассылка больше не задваивает письма. Ручной запасной
  воркер (`scripts/process-bulk-send-jobs.mjs`, GitHub Actions
  `workflow_dispatch`) не захватывал строки задания атомарно — брал список
  `pending` снапшотом в начале запуска и слал по нему, — поэтому при
  параллельной работе с Edge Function (pg_cron раз в минуту) каждый поставщик
  получал по два одинаковых письма. Добавлен атомарный захват (как в Edge
  Function), проверка «письмо по этой строке задания уже ушло» в обоих
  воркерах и колонка `supplier_offer_emails.bulk_job_item_id` под уникальным
  индексом — последний рубеж на уровне базы.
- **Проверено:** `npm run build:app` чистая, `node --check` на скрипте;
  причина подтверждена по живой базе (44 письма на 24 получателя в рассылке
  «Краска интерьерная (обновленная поставка)», второе письмо каждой пары —
  от Edge Function, первое от ручного запуска Actions 07:06:40 UTC).
- **Миграции SQL:** применена `supabase/migrations/20260912-bulk-send-no-duplicates.sql`
  (nullable-колонка + частичный уникальный индекс) — совместима со старым
  кодом, который колонку не заполняет. Дополнительно в живой базе удалены 38
  писем-дублей от 2026-09-12 (по одному письму на строку задания оставлено,
  бэкап удалённых строк — в сессии, не в репозитории).
- **При публикации:** после мерджа задеплоить Edge Function
  `process-bulk-send-jobs` (`POST /v1/projects/<ref>/functions/deploy?slug=...`) —
  иначе живой воркер останется без проверки и колонки.
- **Риск при задержке:** есть. Пока не опубликовано, старый ручной воркер в
  `oodobu` по-прежнему без захвата: если запустить воркфлоу «Process bulk send
  jobs» вручную во время активной рассылки, дубли повторятся. До публикации
  воркфлоу руками не запускать (очередь и так разбирается кроном раз в минуту).
- **Конфликты при сборке:** маловероятны — трогает только
  `scripts/process-bulk-send-jobs.mjs`, `supabase/functions/process-bulk-send-jobs/index.ts`,
  новый файл миграции, плюс абзац в `CLAUDE.md` (раздел про фоновые очереди)
  и запись в журнале.

### Релиз 2026-09-12 (третий, по команде «публикуй»)

Одна ветка в очереди, поэтому отдельная релизная ветка не собиралась:
`claude/elegant-davinci-rpxbbp` = `oodobu` + один коммит, конфликтов нет,
опубликована своим PR напрямую в `oodobu` (один PR = один прод-билд Vercel).
`npm run build:app` на ней чистый. SQL-миграций в релизе нет.

Что было и что НЕ вошло на момент сборки:

- в «Ждут публикации» кроме неё оставалась только
  `claude/wonderful-einstein-vdg945` с пометкой «ОТЛОЖЕНА ОСОЗНАННО» — её
  запись оставлена как есть, не публиковалась;
- `claude/release-2026-09-12-3` — уже неактуальная ветка: по коду совпадает с
  `oodobu` до буквы (её содержимое доехало в прод через PR #52/#54),
  расходятся только журнал и этот файл. Мерджить нечего, можно удалять;
- `claude/zen-johnson-i7vjcg` (11:24 того же дня) — один новый файл
  `api/_belarus.js`: валидатор УНП и клиент ГРП МНС, сам коммит говорит
  «пока не подключён». Записи в очереди у ветки нет, значит другая сессия её
  готовой не объявляла — в релиз не бралась.

### claude/elegant-davinci-rpxbbp
- **Что:** массовая рассылка по профильной категории больше не подмешивает
  универсальных поставщиков. Владелец: «когда я отправляю рассылку узким
  поставщикам (например, краска), мне в общем списке в рассылке не нужны
  универсальные поставщики вообще... универсальную ведомость я буду рассылать
  отдельно». Откат поведения из PR #52 (оно уже в проде): убраны галочка
  «Добавить Универсальные поставщики», селект мастер-ведомости для них, метка
  «· универсальный» и второе задание очереди — в очередь встаёт одно задание
  с ведомостью категории. Универсальным теперь пишется отдельной рассылкой:
  категория «Универсальные поставщики» в селекте модалки + мастер-ведомость
  выбирается в пикере ведомости (он их и так показывает).
- **Проверено:** `npm run build:app` чистый; мок-тест BulkSendModal со
  скриншотами — «Краска» показывает только профильных, «Универсальные
  поставщики» только универсальных.
- **Миграции SQL:** нет, правка целиком во фронте (`BulkSendModal.tsx`,
  один проп в `Suppliers.tsx`).
- **Риск при задержке:** низкий, но неприятный для владельца: до публикации в
  проде остаётся нежелательное поведение — универсальные видны и по умолчанию
  отмечены в рассылке по любой узкой категории, так что случайное «Поставить в
  очередь» отправит им ведомость краски. Обходится вручную: снять галочку
  «Добавить Универсальные поставщики» перед отправкой.
- **Конфликты при сборке:** только журнал (`docs/session-journal.md`) и этот
  файл — сохранять обе записи. Правки в `BulkSendModal.tsx` пересекутся с
  любой другой веткой, которая трогает массовую рассылку; смысл правки —
  удаление универсальной группы, при конфликте выигрывает удаление.


### Релиз 2026-09-12 (второй, по команде «собирай всю доступную очередь»)

Собран в `claude/release-2026-09-12-2` от `oodobu`, пять веток одним PR = один
прод-билд Vercel. `npx tsc -b`, `npx vitest run` (88 тестов) и `npm run build:app`
чистые на собранной ветке; дополнительно прогнан сам пререндер в быстром режиме —
284 пути скопированы с прода с перенацеливанием ассетов, все 285 итоговых
снапшотов проверены: ни одной ссылки на несуществующий чанк, во всех есть входной
чанк текущей сборки.

- `claude/wizardly-pascal-r8lt3l` — мастер-ведомость: сводная ведомость на смету,
  собирается из остальных сама и обновляется при любом их изменении.
- `claude/sweet-shannon-9kwco6` — универсальный поставщик живёт одной карточкой
  (предупреждение о дубле, объединение карточек с переносом переписки/КП/файлов,
  баннер для разовой чистки), универсальные поставщики в массовой рассылке,
  страна в сравнении карточек.
- `claude/optimistic-newton-y97o9x` — счета из писем записываются в базу сами, без
  ручного «Подтвердить»; плюс три починки распознавания (.xlsx-счета, выбор
  правильного вложения вместо картинки из подписи, вложения без имени). Бэкофилл
  по реальной базе уже прогнан ДО публикации (5 счетов).
- `claude/vigilant-thompson-l67u4o` + `claude/bold-rubin-ptv4ew` — **две сессии
  независимо чинили одно и то же**: прод-билд по 10 минут на каждом пуше (быстрый
  режим пререндера отвергал копию с прода из-за несовпадения хэша входного чанка,
  а хэши пересчитываются каскадом от любой правки, даже чисто админской). При
  сборке релиза взяты непересекающиеся части:
  - от `vigilant-thompson` — перенацеливание ассетов снапшота по стему имени чанка,
    повторы (3 попытки) на select к Supabase в `prerender.mjs`/`generate-sitemap.mjs`
    (разовый 504 free-tier дважды за сутки ронял прод-деплой) и правило в `CLAUDE.md`
    про ожидание деплоя;
  - от `bold-rubin` — отпечаток публичного кода (`scripts/public-build-id.mjs`, новый
    шаг в `build:app` → `dist/public-build-id.txt`): полный рендер включается сам,
    когда правился код маркетинговых страниц, а правки админки идут быстрым путём.
  Дубль-реализация из `bold-rubin` (`scripts/prerender-assets.mjs` и её тест) при
  сборке удалена — механизм остался один.
- `claude/wizardly-maxwell-hvcere` — только запись в очереди про прямой пуш текста
  массовой рассылки (сам коммит `9f6ddd1` уже был в `oodobu`).

**Что учесть после публикации:** первый прод-билд снова будет полным — на проде
ещё нет `/public-build-id.txt`, отпечаток сверять не с чем. Быстрые билды
начнутся со следующего пуша, не трогающего публичные страницы.

**Догон по `claude/sweet-shannon-9kwco6` (влит отдельно, сразу после релиза):**
релиз собрали по состоянию ветки на коммит `f111a9a`, а в ней к тому моменту
были ещё три: мастер-ведомость универсальным поставщикам в массовой рассылке
(рассылка встаёт в очередь двумя заданиями — профильным ведомость категории,
универсальным мастер-ведомость), схлопнутый в один `useMemo`
`materialLedgersWithMasters` вместо двух копий и записи в журнале/очереди.
Влиты в `oodobu` отдельным PR по команде владельца («билд прошёл, заливай
остальное»); дубликатов после мерджа нет — проверено по объявлениям
`materialLedgersWithMasters`/`masterLedgerForEstimate`/`handleLedgersChange`
и по импорту `lib/masterLedger`.

### claude/wonderful-einstein-vdg945 — опубликована раньше, вне очереди

Запись в «Ждут публикации» устарела: работа уехала в прод отдельным PR #49
(«очередь для одиночных писем»). Проверено 2026-09-12 запросом к базе — таблица
`outgoing_email_jobs` существует, крон `process-outgoing-emails` заведён, Edge
Function и миграция лежат в `oodobu`. Больше ничего делать не нужно.

### Прямой пуш 2026-09-12 (вне очереди, по просьбе владельца)

- `claude/wizardly-maxwell-hvcere` → коммит `9f6ddd1` в `oodobu`. Новый текст
  дефолтного письма массовой рассылки поставщикам (`defaultBulkBody()` в
  `src/components/suppliers/BulkSendModal.tsx`): «Планируем закупку материала
  согласно ведомости, прикрепленной к письму» + «Карточку организации и адрес
  доставки прикрепил к письму». Только текст, логика рассылки и подстановка
  `{компания}`/`{контакт}` не трогались. `npm run build:app` чистый,
  SQL-миграций нет. Пушился напрямую по команде «пушь сразу в прод»; владелец
  сразу после этого попросил впредь ставить в очередь — запись здесь, чтобы
  коммит не потерялся при сборке следующего релиза.

### Релиз 2026-09-12 (первый через очередь)

Собран в `claude/release-2026-09-12` от `oodobu`, семь веток одним PR = один
прод-билд Vercel. `npx tsc -b` и `npm run build:app` чистые на собранной ветке.

- `claude/peaceful-feynman-98kvhd` — транзакции: вкладки периодов (этот месяц /
  прошлый / год / произвольный), дата новой транзакции по умолчанию = сегодня,
  сортировка по дате после добавления и правки, «Альфа» убрана из источников,
  подкатегория «ИИ» у Claude/ProxyAPI. Плюс сама очередь релиза.
  SQL-миграции применены заранее (правка данных, не схемы).
- `claude/great-bardeen-0xvlmt` — метрики обновляются раз в минуту без
  перезагрузки страницы.
- `claude/epic-archimedes-k7wmyk` — задачи: сотрудник видит только свои,
  владелец весь список. Схему не трогает, фильтрация на клиенте
  (`isTaskVisible`) — миграция не нужна, проверено при сборке релиза.
- `claude/epic-allen-lwowmr` — уникальное превью ссылки под каждую страницу
  (+ шаги сборки `generate-og-cards` и `generate-admin-shells`).
- `claude/happy-planck-5tnxyl` — убрана кнопка «Найти в сети» с карточки
  категории поставщиков.
- `claude/dreamy-faraday-jc3bc3` — веб-поиск поставщиков: логирование раундов,
  пропуск второго раунда для узких рынков (`MIN_ROUND1_FOR_SECOND_ROUND`).
- `claude/sharp-hawking-f1n70s` — бейдж уведомления в `ToggleGroup` не
  обрезается по правому краю.

**Конфликты, разобранные при сборке** (пригодится на будущее):
- `ToggleGroup.tsx` — один и тот же баг чинили два диалога по-разному. Взяты
  обе половины: `-mr-2` из ветки + `z-10` у бейджа из `oodobu`.
- `package.json` — обе стороны дописали свои шаги в `build`/`build:app`.
  Объединены все три: `notify-indexnow` (из oodobu), `generate-og-cards` и
  `generate-admin-shells` (из ветки). Проверено — сборка прогоняет все.
- `scripts/process-supplier-web-search-jobs.mjs` — ветка писала по старому
  имени `country`, в `oodobu` колонка уже переименована в `region`. Взята
  сигнатура из `oodobu`, логирование и порог второго раунда — из ветки.
- `CLAUDE.md` у `dreamy-faraday` — ветка старая, дописывала журнал ещё в
  `CLAUDE.md`, откуда он с тех пор переехал. Взята сторона `oodobu`, а
  уникальная запись ветки (180 строк про логи ProxyAPI) перенесена в
  `docs/session-journal.md`, а не потеряна.
