# Товарные группы поставщиков — как это работает

Задача (владелец, 2026-09-12): «каждый поставщик поставляет только свой спектр
товара. У кого-то это десятки категорий, у кого-то сотни, у кого-то всего
несколько. Можем ли мы реализовать задачу по парсингу категорий поставки
существующим поставщикам в базе? Желательно, не через проксиапи, он дорого
обходится, а через самую экономную модель в рамках моего тарифа Max x20».

Поэтому конвейер разделён на два шага, и ProxyAPI не участвует ни в одном.

## Шаг 1 — снимок сайта, без ИИ (работает сам)

Edge Function `process-supplier-jobs` (крон раз в минуту) разбирает очередь
`supplier_site_snapshots`: скачивает главную, корень каталога и `sitemap.xml`,
вытаскивает названия и адреса разделов каталога. Модели здесь нет вовсе —
обычный разбор HTML, стоит ноль.

Домен в очередь ставит триггер в базе при сохранении карточки поставщика с
сайтом (`supabase/migrations/20260912-supplier-site-snapshots.sql`), поэтому
новые поставщики подхватываются сами, ничего запускать не надо.

Строка — на **домен**, не на карточку: одна компания живёт в нескольких
категориях закупок (278 карточек / 259 доменов), а сайт у неё один.

## Шаг 2 (с 2026-09-14) — пошаговая переклассификация пачками с ручной проверкой

Массовые прогоны через haiku-субагентов (описаны ниже, оставлены как
история) дали в базе выдуманные группы: у avangardrf.ru при 116 верно
скачанных разделах — пять групп, которых нет ни в одном разделе. Владелец
(2026-09-14): «категории в массе придуманы… я готов двигаться пачками по
10 поставщиков, включая ручную проверку и дообучение модели». Поэтому
теперь так:

```bash
node scripts/supply-categories/review.mjs next --limit=10   # выгрузить пачку
#   → out/review-batch.json: верифицированные поставщики первыми, в порядке
#     каталога; полное дерево разделов с путями; текущие группы («было»).
#     Компания с несколькими доменами идёт целиком.
#   классифицирует ОСНОВНАЯ модель сессии (не haiku!), прочитав LESSONS.md,
#   и пишет out/review-result.json — формат в шапке review.mjs: у каждой
#   группы обязательный массив evidence (разделы дословно из батча)
node scripts/supply-categories/review.mjs diff              # «было → стало»
#   → печатает и пишет out/review-diff.md; группы без найденной улики
#     помечены «✗ НЕ ЗАПИСАНО»
node scripts/supply-categories/review.mjs apply [--dry]     # записать
#   → categories, categories_note, classified_at, categories_verified = true:
#     пачка появляется во вкладке «Верификация» (Закупки) — там владелец
#     проверяет каждую компанию рядом с её сайтом
```

Дальше — ручная проверка владельцем. Его поправки («у X нет Y», «верни Z»)
применяются SQL-ом к строке и записываются в `LESSONS.md` как правило с
примером; следующая пачка идёт уже по обновлённым правилам. Это и есть
«дообучение» — веса не трогаем, растим инструкцию. Найденные пробелы
справочника (`missing` в результате) копятся там же, в таблице кандидатов.

Флаг `categories_verified` (см. `data/supplierSiteSnapshots.ts`) значит
«пересчитан этим методом, с уликами, и выведен на ручную проверку»; вся
остальная база (старые haiku-прогоны) остаётся вне очереди верификации,
пока не пройдёт через `review.mjs`. Осталось: ~250 верифицированных
доменов, затем ~930 неверифицированных.

## Шаг 2 (до 2026-09-14, массовый прогон через субагентов — ненадёжен, см. выше)


Выполняется человеком-оператором через Claude Code, когда накопились новые
снимки (или после правки справочника). ProxyAPI не нужен, платит подписка.

```bash
node scripts/supply-categories/export.mjs          # только новые
node scripts/supply-categories/export.mjs --all    # всё заново
```

Появится `scripts/supply-categories/out/batch-NN.json` — по 12 сайтов в файле
(название компании, заголовок и описание главной, список разделов каталога).

Дальше — по одному субагенту (`Agent`, модель haiku) на пачку, с промптом:

> Прочитай `scripts/supply-categories/out/batch-NN.json`. Для каждого сайта
> определи, какие товарные группы из справочника `src/data/supplyCategories.ts`
> компания реально поставляет — по разделам её каталога, а не по догадкам.
> Бери только те группы, под которые в разделах есть явные позиции. Если по
> разделам не понять — оставь пустой массив. Запиши результат в
> `scripts/supply-categories/out/result-NN.json` массивом
> `[{"host": "...", "categories": ["..."], "note": "одна строка: чем компания
> занимается"}]`. Названия групп — дословно из справочника. Если какой-то
> группы явно не хватает, упомяни её в note, но в categories не пиши.
>
> Жёсткие правила, их нарушение делает работу бесполезной:
> 1. «Строительный гипермаркет (всё для ремонта)» НИКОГДА не ставится в
>    одиночку. Это пометка «ассортимент очень широкий», а не замена разбору:
>    рядом всегда идут конкретные группы. У специализированной компании
>    (плитка, потолки, фасады, сантехника) этой группы нет вовсе.
> 2. Разбирай КАЖДЫЙ сайт отдельно и до конца. Типичный результат — 2–12
>    групп; один и тот же набор у нескольких подряд сайтов означает, что
>    разделы не прочитаны.
> 3. `note` — своими словами, чем компания занимается. Копировать в него
>    заголовок страницы (`pageTitle`) нельзя.
> 4. Группа ставится, когда под неё на сайте есть свой раздел каталога или
>    несколько явных позиций. Единичное упоминание внутри чужого раздела —
>    не повод: у магазина сантехники «зеркало для ванной» в разделе ванных
>    не делает его поставщиком зеркал.
> 5. Критерий — «покупаем ли мы это у него», а не «стройматериалы ли это».
>    Магазин электроники, заведённый в категорию «Умные замки», поставляет
>    замки и видеонаблюдение; отбрасывать его как «не поставщика
>    стройматериалов» неверно — так три карточки остались без групп и их
>    пришлось править руками (2026-09-12).
>
> Пачки не пересекаются: один файл `batch-NN.json` отдаётся ровно одному
> субагенту. Два агента на одну пачку перезаписывают результат друг друга —
> так уже терялась работа (2026-09-12).

Потом:

```bash
node scripts/supply-categories/import.mjs --dry   # посмотреть, что запишется
node scripts/supply-categories/import.mjs
```

Импорт сверяет группы со справочником: незнакомые в базу не идут, а
печатаются списком — это кандидаты в `src/data/supplyCategories.ts`. После
правки справочника имеет смысл переклассифицировать всё (`export --all`).

## Сайты, которые не открылись

Часть доменов не отдаёт страницы никак (защита от ботов, мёртвый домен) —
у таких снимка нет и разбирать нечего. Им группа проставлена по КАТЕГОРИИ
ЗАКУПКИ, в которой поставщик заведён (это факт из базы, а не догадка), с
заметкой «Сайт не открылся — группа взята из категории закупки». Такую
строку видно и в карточке: одна группа вместо обычных пяти-шести.

## Где это видно

Страница «Поставщики»: группы чипами в списке под каждым поставщиком
(первые четыре) и полным набором с заметкой в карточке «Подробнее», блок
«Что поставляет».

## Зачем

Главное — не сама разметка, а то, что при заведении новой категории закупки
видно, кто из уже известных поставщиков её закрывает, без нового веб-поиска
(одно задание поиска через ProxyAPI стоило ~195 ₽, см. журнал за 2026-09-11).
Плюс массовая рассылка по категории может идти только тем универсальным
поставщикам, у кого есть нужная группа, а не всем подряд.
