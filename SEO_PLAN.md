# SEO-план redevelopment.pro — рабочий файл для Claude

> **Как пользоваться этим файлом (инструкция для будущих сессий Claude).**
> Это дорожная карта SEO-продвижения, составленная 2026-08-22 по итогам
> глубокого исследования (6 параллельных направлений + критика полноты,
> источники в конце). Владелец загружает этот файл и говорит «продолжаем
> по SEO-плану» — бери первую незакрытую задачу из самого приоритетного
> этапа, уточняй только то, что помечено «спросить владельца», делай,
> отмечай чекбокс, дописывай дату и результат в Журнал внизу. НЕ переделывай
> уже отмеченное. НЕ предлагай ничего из раздела «Что НЕ делаем». Если
> владелец приносит данные (коды счётчиков, телефон, реквизиты) — сразу
> смотри раздел «Ждём от владельца», там написано, что с ними делать.

---

## Цель и контекст

- **Цель этапа 1:** видимость лендинга `/one` (деловой центр Red One,
  Минск, Полтавская 10) по запросам «офисы в Минск Мире», «помещения в
  Минск-Мире» и смежным коммерческим запросам; заявки на бронь из органики.
- **Рынок:** Беларусь. Google ~65–75% поиска, Яндекс ~20–30% (источники
  расходятся в точных цифрах, но не в порядке). Приоритет технических
  компромиссов — Google, но Яндекс обязателен: у него свои механики
  (Вебмастер, регион, коммерческие факторы, Яндекс Карты).
- **Жёсткое ограничение владельца:** НИКАКИХ покупных ссылок и серых схем.
  Исследование подтвердило: это совпадает с направлением алгоритмов
  (спам-апдейты Google 2025–2026 бьют по ссылочному спаму глобально,
  Яндекс «Минусинск» пессимизирует за покупные ссылки, накрутка
  поведенческих факторов в Яндексе — высокорисковая практика).
- **Решение владельца (2026-08-22):** URL `/one` НЕ меняем на ЧПУ-слаг.
  Не поднимать этот вопрос снова.

## Что уже сделано (не переделывать)

- `index.html`: title/description/og/canonical под «офисы в Минск Мире»,
  JSON-LD RealEstateListing (id=`object-json-ld`).
- `public/sitemap.xml` (оба лендинга), `robots.txt` со строкой Sitemap.
- Динамические meta по объекту: `src/lib/pageMeta.ts` (SEO_OVERRIDES для
  `one`, fallback-генерация для остальных), вызывается из
  `ObjectLandingPage.tsx`.
- H1 под целевые запросы + alt главного фото (`HeroImageSlider`, проп alt).
- Код-сплиттинг админки (`App.tsx`, React.lazy) — главный чанк 382 КБ
  (gzip 114), было 1171. `fetchPriority="high"` на hero-фото (LCP),
  `loading="lazy"` на плане этажа.

## Ключевые выводы исследования (сжато)

1. **Ниша — наше преимущество.** Core-апдейты Google 2025–2026 стабильно
   награждают узких специалистов с оригинальными данными и наказывают
   генералистов. Стратегия — глубина по «офисы у Минск Мира» (реальные
   планировки, цены, фото реновации, история здания), не общие тексты
   про недвижимость.
2. **Главный техдолг — клиентский рендеринг.** AI-краулеры (GPTBot,
   PerplexityBot, ClaudeBot) НЕ исполняют JS — для них контент лендингов
   не существует (Google и Gemini рендерят — для них видим). Яндекс
   рендерит JS нестабильно (инструмент в Вебмастере — бета с 2022;
   официальная рекомендация Яндекса — SSR/пререндер). Решение — пререндер
   публичных страниц на сборке (задача Э2-1). Динамический рендеринг по
   user-agent (Prerender.io и т.п.) — deprecated у Google, НЕ использовать.
3. **Локальный блок решает.** Whitespark 2026: сигналы Google Business
   Profile ~32% веса локальной выдачи, отзывы ~20%, ссылки лишь ~15%.
   GBP + Яндекс Бизнес + NAP-консистентность + ровный приток отзывов —
   самый дешёвый рычаг. Оговорка для РБ: регистрация GBP из Беларуси
   затруднена (санкционный тренд, видеоверификация фактически обязательна),
   запасной канал — Яндекс Бизнес, он для РБ работает гарантированно.
4. **Коммерческие факторы Яндекса:** цены видимым текстом, телефон и адрес
   в шапке/футере, «О компании», реквизиты юрлица, условия сделки, отзывы.
   (Точные «веса» из SEO-блогов непроверяемы — но сам список подтверждён
   многими источниками и полезен и для Google E-E-A-T.)
5. **FAQ:** Google отключил FAQ rich results (май 2026), но FAQ-контент
   стал важнее — его цитируют AI-системы, разметку читают Яндекс и Bing.
   Делать видимый FAQ-блок + FAQPage JSON-LD, НЕ ради сниппетов Google.
6. **Ссылки — только естественные и локальные:** каталоги недвижимости РБ
   (realt.by — у них есть отдельный каталог бизнес-центров, re.kufar.by,
   domovita.by, hata.by), местные СМИ (realt.onliner.by регулярно и
   бесплатно пишет про реновации зданий в Минске — наш случай ровно их
   формат), probusiness.io (принимает истории компаний), Pressfeed
   (ответы эксперта на запросы журналистов). Упоминание бренда без ссылки
   тоже ценно — не выпрашивать анкоры.
7. **Уникальный актив:** cron-синки статистики спроса (scripts/sync-*-stats)
   — готовый датасет для публичной аналитики «рынок офисов Минска», такого
   нет у конкурентов; это магнит для ссылок и AI-цитирования.
8. **Бренд:** «Red One» коллизирует с фильмом 2024 года. Везде писать
   связкой «деловой центр Red One» / «Red One Минск», в Organization
   добавить sameAs на карточки карт/каталогов.

### Сомнительные цифры — НЕ выдавать владельцу за факты
- «Коммерческие факторы = 60–70% веса в Яндексе» — оценка блогов.
- «Обход по счётчикам: индексация за 1–3 дня» — сроки из блогов, механизм
  включить стоит, сроков не обещать.
- Статистика AI Overviews — американская; по русскоязычной выдаче из РБ
  покрытие не проверено. Прежде чем инвестировать в «оптимизацию под
  цитирование», проверить, показываются ли AIO по нашим запросам вообще.
- Антифрод Яндекса «Тайфун» — название не из официальных источников
  (вывод «не накручивать ПФ» верен независимо).
- «Google рендерит нас надёжно» — исследование Vercel/MERJ было на их
  выборке; наш контент приходит XHR-ом из Supabase с холодным стартом —
  снапшот Google может случиться ДО прихода данных. Проверить через URL
  Inspection после заведения GSC.

---

## Дорожная карта

### Этап 1 — код, можно делать сейчас (не ждёт владельца)

- [x] **Э1-1. noindex токен-страниц.** `/plan/:token`, `/tz/:token`,
  `/summary/:token` — клиентские ссылки, попадают в мессенджеры/почту,
  могут индексироваться с данными клиентов. При монтировании этих страниц
  ставить `<meta name="robots" content="noindex">` (добавить хелпер в
  `pageMeta.ts`, снимать при размонтировании). ВАЖНО: именно noindex, НЕ
  Disallow в robots.txt (закрытая в robots страница не получит noindex и
  может попасть в индекс по внешним ссылкам).
- [x] **Э1-2. Фикс soft-404.** Маршрут `/:slug` (App.tsx) перехватывает
  любой односегментный URL раньше `*` → NotFound недостижим, опечатки
  индексируются с кодом 200. В `ObjectLandingPage` при ненайденном слаге:
  meta robots noindex + явный 404-контент (стейт notFound уже есть —
  проверить, что он ставит noindex).
- [x] **Э1-3. Серверный редирект корня.** `/` сейчас — клиентский
  `<Navigate>` на `/one`, боты без JS видят пустую страницу. Добавить в
  `vercel.json` постоянный redirect `/` → `/one`. (Альтернатива «корень =
  главная Red One» отклонена не была — если владелец захочет, обсудить, но
  redirect — безопасный дефолт, спросить мимоходом, не блокироваться.)
- [x] **Э1-4. Зеркало GitHub Pages.** Владелец подтвердил: зеркало не
  нужно. `.github/workflows/deploy-pages.yml` удалён, README и условный
  `base` в `vite.config.ts` (было `/Redevelopment/` для Pages) убраны как
  мёртвый код.
- [x] **Э1-5. Цены видимым текстом в HTML** на `/one`: секция с H2 вида
  «Кабинеты от N м² — от X BYN/мес», «Рабочее место — X BYN/мес» (данные
  есть в системе). Это коммерческий фактор Яндекса №1 и материал для
  AI-цитирования (ценовые запросы чаще прочих вызывают AI-ответы).
- [x] **Э1-6. Блок «Как проходит сделка»** на `/one`: бронь → соглашение
  о намерениях (онлайн-подписание с email-кодом — УТП) → оплата/рассрочка.
  Снимает возражения + коммерческий фактор.
- [x] **Э1-7. FAQ-блок.** 6 вопросов (цена, отделка, парковка, бронь,
  рассрочка/лизинг/кредит, юрадрес) — тексты согласованы с владельцем
  в диалоге. `components/objects/FaqCard.tsx` (видимый блок, `<details>`,
  контент в DOM независимо от открыт/закрыт) + `setFaqJsonLd` в
  `pageMeta.ts` (FAQPage JSON-LD, плейсхолдер `#faq-json-ld` в
  `index.html`). Контент общий для всех объектов (как и остальные
  коммерческие блоки страницы — цены, варианты покупки), не привязан
  к `/one` конкретно.
- [ ] **Э1-8. Э2-подготовка: NAP-каркас.** Футер лендинга под полный
  адрес + телефон + юрлицо/УНП — вёрстку сделать сразу, данные вставить,
  когда владелец пришлёт (см. «Ждём от владельца»).

### Этап 2 — крупная техработа (после Э1, можно параллельно с Э3)

- [x] **Э2-1. Пререндер публичных лендингов** (`/one`, `/redstorage` и
  любой новый объект с `landing_slug` — список берётся из Supabase на
  этапе сборки, не захардкожен). Реализовано и проверено на реальном
  деплое (см. журнал):
  - Подход — свой build-time скрипт на headless-браузере
    (`scripts/prerender.mjs`, playwright-core), НЕ переезд на
    framework-режим react-router v7 (затронул бы роутинг всей админки
    ради 2–3 публичных страниц — несоразмерно).
  - Актуализация данных — Vercel Deploy Hook при сохранении объекта
    (владелец выбрал этот вариант из трёх; `objectsApi.ts` дёргает
    `api/trigger-rebuild.js` после `insertObject`/`updateObject`).
  - Deploy Hook создан владельцем (Project Settings → Git → Deploy Hooks,
    ветка `claude/redevelopment-platform-prototype-oodobu`), URL сохранён
    в переменных окружения Vercel как `VERCEL_DEPLOY_HOOK_URL`. Цепочка
    целиком проверена прямым POST на `/api/trigger-rebuild` —
    `{"triggered":true}`, реальная пересборка запускается.
  - **Проверено на реальном деплое 2026-08-22 — всё сработало с первого
    раза:** curl без JS на `/one` и `/redstorage` отдаёт 200 с полностью
    отрисованным HTML (реальные H1, цены, план этажа, JSON-LD с верными
    данными конкретного объекта — не общей заглушкой Red One), значит
    `@sparticuz/chromium` успешно запускается в build-контейнере Vercel и
    достаёт данные из Supabase (то, что не получилось проверить локально
    из-за сетевого ограничения песочницы — на реальном Vercel-билде его
    нет). `/admin` и `/plan/:token` не задеты. Осталось сделать в
    Яндекс.Вебмастере: переключить рендеринг на «не выполнять JS».
- [ ] **Э2-2. LocalBusiness JSON-LD** на `/one` рядом с RealEstateListing:
  адрес, гео-координаты, телефон, часы, `sameAs` (карточки Карт/каталогов
  по мере появления). Данные в разметке обязаны дословно совпадать с
  видимым текстом страницы. Organization (компания целиком, не конкретное
  здание) сюда НЕ класть — см. Э2-4, для неё будет отдельная страница.
- [ ] **Э2-3. IndexNow + Bing.** После заведения GSC: Bing Webmaster Tools
  импортом из GSC (индекс Bing — основа веб-поиска ChatGPT). IndexNow-пинг
  при деплое (ключ — статический файл в public/, fetch в build-скрипте) —
  ускоряет Яндекс и Bing.
- [x] **Э2-4. Мультигео-техфундамент: объекты (Минск/РБ) vs будущая
  платформа (Россия).** Решение владельца (2026-08-22): компания
  Redevelopment планирует продавать доступ к своей IT-платформе
  (сейчас внутренний инструмент — просчёт смет, `Estimates.tsx` в
  админке) под аудиторию из России, отдельно от продающих страниц
  конкретных объектов (`/one` и т.п.), которые остаются гео-таргетом
  на свой город/страну. Публичной страницы платформы пока НЕТ — сделан
  только техфундамент, не контент (осознанное решение владельца — тонкий
  контент без готового продукта за ним вредит SEO, см. журнал):
  - `vercel.json`: редирект `/` → `/one` был постоянным (308/`permanent:
    true`) — это де-факто говорило поисковикам «здесь навсегда `/one`»,
    что создало бы лишнее трение при вводе позже отдельной страницы
    корня. Сделан временным (307/`permanent: false`) — то же поведение
    для посетителей сейчас, но не закрепляет `/` за `/one` намертво.
  - **Когда появится реальная страница платформы (не раньше):**
    - Убрать редирект `/` → `/one` из `vercel.json`, завести отдельный
      компонент страницы с собственными title/description/og/JSON-LD
      (не переиспользовать `pageMeta.ts`'s `SEO_OVERRIDES` формат объекта
      — это другая сущность).
    - Organization JSON-LD (компания целиком) — на этой странице, не на
      `/one` (см. правку Э2-2 выше).
    - Яндекс.Вебмастер: «Регионы для разделов сайта» — выставить `/one`
      (и другие `/:slug` объектов) отдельно на Минск/РБ, оставить общий
      регион сайта под будущий контент платформы (Россия или конкретный
      регион, когда решится). НЕ выставлять весь сайт на Россию уже
      сейчас — реального контента под этот регион ещё нет, а Яндекс
      сверяет регион с содержимым страниц (несовпадение — минус, не
      нейтрально).
    - Google не даёт настройки региона по разделам сайта (только через
      контент/GBP/ссылки) — там это решится само собой контентом и
      адресами на каждой странице, отдельных технических действий не
      требует.

### Этап 3 — контент (после/параллельно Э1; тексты — совместно с владельцем)

- [~] **Э3-0. Аудит выдачи и семантика (сделать ПЕРВЫМ в этапе).**
  Частично сделано инструментом веб-поиска (см. журнал) — важная оговорка:
  этот поиск идёт из США, не из РБ, и это Google, не Яндекс. Реальную
  гео-специфичную выдачу (Google.by из Минска, Яндекс, частотности
  Wordstat) им не заменить — доступного инструмента для Яндекса/Wordstat
  из этой сессии нет вообще. То, что удалось выяснить таким способом
  (см. журнал) — это черновой ориентир, не финальный baseline. Настоящий
  baseline (ручные скрины site:redevelopment.pro в Google.by и Яндексе,
  ручные позиции) — либо руками владельца, либо после подключения GSC/
  Яндекс.Вебмастера (тогда позиции и показы видны без ручного поиска).
- [x] **Э3-1. Гид «Район Минск Мир для бизнеса».** `/rayon-minsk-mir`
  (`DistrictGuidePage.tsx`) — транспорт, инфраструктура, деловая часть
  района, ссылка на `/one` в конце. Цены соседей НЕ вставлены текстом
  (решение владельца — цифра живая, потом сделать через данные
  cron-синков, см. Э3-2, не переписывать текстом вручную). Ссылки С `/one`
  СЮДА нет — осознанно, владелец не хочет отвлекать с продающей страницы;
  единственные пути обнаружения — sitemap.xml и прямой поиск. Дата
  «Обновлено: август 2026» на странице + `dateModified` в Article
  JSON-LD — обновлять вручную при следующем квартальном пересмотре
  (см. журнал про Wordstat-обоснование выбора темы и фраз).
- [ ] **Э3-2. Публичная аналитика спроса** (`/analitika` или подобное) из
  данных cron-синков Kufar/Realt/Avito: динамика объявлений и ставок по
  офисам Минска, квартальное обновление с видимой датой. Двойное
  назначение: магнит для СМИ/ссылок + AI-цитирование. Перед постройкой
  согласовать с владельцем, какие цифры публичны.
- [ ] **Э3-3. Кластер поддержки** (по итогам Э3-0, 2–4 страницы):
  «Купить или арендовать офис малому бизнесу», «Кабинет vs рабочее место»,
  «Офис с отделкой vs без». Каждая ссылается на `/one`. Не плодить страницы
  под словоформы — одна страница = один интент.
- [ ] **Э3-4. E-E-A-T-блоки на сайт:** «О компании» (кто делает
  редевелопмент, юрлицо, опыт), фото «до/после» реновации, реальные
  резиденты (с их согласия). Материалы — от владельца.

### Этап 4 — внешние площадки (руками владельца, по инструкциям из этого файла)

- [ ] **Э4-1. Яндекс Бизнес** — карточка «деловой центр Red One»: категории
  (бизнес-центр / аренда офисных помещений / коворкинг), фото, прайс-лист
  позициями (мощный фактор карточки), ссылка на /one. Модерация ~3 дня.
  Приоритетнее GBP, т.к. для РБ работает без ограничений.
- [ ] **Э4-2. Google Business Profile** — через business.google.com/add.
  Правила: профиль = действующий бизнес-центр/офис продаж с вывеской и
  часами приёма (вакантные помещения сами по себе права на профиль НЕ
  имеют). Готовиться к видеоверификации: вывеска, вход, интерьер,
  подтверждение управления одним дублем. Основную категорию НЕ дублировать
  с арендаторами в том же здании (риск фильтрации). Если регистрация из РБ
  упрётся в ограничения — не обходить через чужие реквизиты (риск бана),
  сфокусироваться на Яндекс Бизнесе и каталогах.
- [ ] **Э4-3. Каталоги недвижимости:** карточка БЦ в каталоге realt.by
  (не только объявления!), активные объявления на re.kufar.by, domovita.by,
  hata.by со ссылкой на /one («полная планировка и онлайн-бронирование» —
  УТП, которого нет у других объявлений).
- [ ] **Э4-4. Справочники NAP:** tam.by, b2b.by, catalog.onliner.by,
  kontakt.by, orgpage.by, 1info.by — эталонный NAP-блок везде дословно.
  Завести таблицу «где размещены» (например, прямо в этом файле ниже).
- [ ] **Э4-5. PR-питч «реновация»** в realt.onliner.by и realt.by: фото
  до/после, история выкупа, цифры, деталь-крючок (онлайн-подписание
  соглашения). Их редакции регулярно и бесплатно пишут ровно про такое
  (пример — БЦ D16). Повторные поводы: открытие, заполнение, аналитика Э3-2.
- [ ] **Э4-6. probusiness.io** — история от первого лица «выкупаю здания и
  переделываю под малый бизнес» с цифрами (их формат). Дубль-питч в
  Office Life. Новости компании — в бесплатный раздел /press/.
- [ ] **Э4-7. Pressfeed** — владелец как эксперт по коммерческой
  недвижимости, отвечать на запросы журналистов (для Яндекса упоминания
  в русскоязычных СМИ особенно полезны).
- [ ] **Э4-8. Резиденты и подрядчики:** просить указывать «офис в деловом
  центре Red One» с ссылкой на их страницах «Контакты»/портфолио.
  Отзывы: после подписания/заселения отправлять клиенту ссылки на отзыв
  в Google и Яндекс Картах. Всех подряд, без фильтра и вознаграждений
  (review gating запрещён обеими платформами). Отвечать на каждый отзыв
  за 1–2 дня. Ровный приток важнее залпа: 2/мес лучше, чем 20 разом.

### Ждём от владельца (блокеры, см. задачи выше)

| Что нужно | Разблокирует |
|---|---|
| ~~Аккаунт Яндекса → счётчик Метрики + верификация Вебмастера~~ — счётчик 111858495 и мета-тег подтверждения на сайте (2026-08-22). Осталось владельцу в самом Вебмастере: подтвердить (кнопка «Проверить»), добавить sitemap.xml, выставить регион Минск | Цели в Метрике (клик по телефону, модалка брони, бронь, подписание) — отдельная задача, код для них ещё не добавлен; Яндекс Бизнес (Э4-1) |
| Аккаунт Google → верификация Search Console | GSC (URL Inspection — проверить, что Google реально видит; заявки sitemap), затем Bing импортом, GBP |
| Телефон компании (+375 в одном формате), юрлицо, УНП, полный адрес с индексом | Э1-8 NAP-футер, Э2-2 LocalBusiness, регион в Вебмастере, все карточки/каталоги |
| Тексты/факты: FAQ-ответы, «О компании», фото до/после, согласие резидентов | Э1-7, Э3-4 |
| Решения: приоритет /redstorage; вариант актуализации пререндера; что из статистики спроса публично | Э2-1, Э3-2, SEO_OVERRIDES для redstorage |

### /redstorage — отдельное решение
Сейчас у него нет SEO_OVERRIDES (fallback-мета корректны), в Яндексе до
пререндера он выглядит как Red One. Спросить владельца: важен ли он
коммерчески? Если да — мини-ядро («склады Минск», «storage помещение
аренда»), запись в SEO_OVERRIDES, включить в пререндер. Если нет —
зафиксировать здесь и не таскать по задачам.

## Что НЕ делаем (принципиально, не предлагать владельцу)

- Покупка ссылок, «прогоны по каталогам», биржи, PBN — запрет владельца,
  подтверждён исследованием (спам-апдейты, «Минусинск»).
- Накрутка поведенческих факторов — высокорисковая практика в Яндексе.
- Динамический рендеринг по user-agent (Prerender.io, Rendertron) —
  deprecated у Google, клоакинг-риск.
- llms.txt — источники противоречат, крупные системы не подтверждают
  использование; не включать в план (вреда нет, но и смысла нет).
- Массовая генерация текстов под ключевики, страницы под словоформы,
  keyword stuffing — наказывается core/spam-апдейтами.
- «GEO/AEO-оптимизация» как отдельная услуга — по официальному гайду
  Google таких требований не существует, это обычное SEO.
- Регистрация GBP на чужие/нерезидентские реквизиты при проблемах — риск
  бана; вместо этого Яндекс Бизнес + каталоги.

## KPI и измерение (после заведения счётчиков)

- Baseline (Э3-0): скрины site:-выдачи, ручные позиции по 5–7 запросам.
- Ежемесячно: показы/клики из GSC и Вебмастера по целевым запросам;
  брони/заявки из органики (цель в Метрике); приток отзывов.
- Ожидания по срокам (сказать владельцу честно): первые сдвиги по
  низкочастотникам — 1–2 месяца после индексации изменений, по основным
  запросам — 3–6 месяцев; ИКС Яндекса может не отображаться первые 2–4
  месяца — это норма, не тревога.

## Журнал выполнения

- **2026-08-24 — Фикс: блок "Первичный рынок" на проде показывал только
  "Бизнес-апартаменты".** Владелец прислал скриншот — в таблице только
  одна строка вместо пяти. Причина — `fetchPrimaryMarketOffers` делал
  голый `select('*')` без пагинации, а PostgREST по умолчанию режет
  ответ на 1000 строк; апартаментов ровно 1466 и они вставлены в базу
  первыми (по `id`) — остальные категории (кладовые/торговые/офисы)
  просто не долетали до клиента, хотя в самой базе все 3389 строк были
  на месте (перепроверил SQL-запросом перед тем, как чинить код — не
  предполагать причину, а убедиться). Добавлена постраничная загрузка
  через `.range()` (страницы по 1000, пока очередная не окажется
  короче лимита) — проверено мок-тестом на наборе больше 1000 строк с
  намеренно смещённым распределением по категориям (сначала 1200
  "лишних" строк одной категории, потом остальные), имитирующим
  реальную причину бага.

- **2026-08-24 — Первичный рынок недвижимости Минск Мира: парсинг bir.by,
  новая таблица `primary_market_offers`, заполнен блок гайда.** Владелец
  предложил спарсить всю базу bir.by (не только машиноместа) и построить
  аналитику первичного рынка — прислал 9 URL категорий (1-5 комнатные,
  сданные дома, апартаменты, коммерческие, кладовые), правила: комнатность
  свести в одну категорию, сданные дома фиксировать отдельно для сравнения
  с вторичкой, коммерческие делить по этажу (1 этаж — торговые, 2+ —
  офисы), для террасных помещений — верификация в админке, ссылку на
  объявление сохранять.

  Перед тем как писать код, разведал структуру всех категорийных страниц
  bir.by (curl + разбор инлайновых `<script>`, тот же метод, что и для
  машиномест) и нашёл 3 находки, которые меняют план:
  1. Комнатные страницы (1-5 room) и страница апартаментов — не разные
     источники, а один и тот же пул объявлений, разрезанный разными
     фильтрами (`comnat` — комнатность, `vid` — Квартира/Апартаменты) —
     если парсить и то, и другое, часть объявлений задвоится.
  2. "Сданные дома" — не отдельная категория объявлений, а статус
     (`stage`: Сдано/Строится) у тех же апартаментов/квартир.
  3. У коммерческих bir.by САМ уже отдаёт террасу отдельным полем от
     чистой площади ("Помещение м²" отдельно от "Терраса м²") — в отличие
     от Kufar/Realt, где это была текстовая путаница, требующая ручного
     разбора. Проверено на реальной записи: официальная "Цена за м²"
     bir.by считается по общей площади (с террасой) — то же искажение,
     что у Kufar/Realt, но здесь его можно поправить автоматически.

  Уточнил у владельца напрямую два момента, от которых зависела схема:
  нужна ли всё равно ручная верификация в админке для террас, раз bir.by
  уже структурирует это чисто (ответ: нет, считать автоматически) — и
  какие категории реально нужны, раз комнатные страницы избыточны (ответ:
  "квартиры вообще не нужны, только апарты и остальная коммерция" —
  обычные vid=Квартира сознательно не собираются вовсе).

  Новая таблица `primary_market_offers` (Supabase, создана и наполнена
  через Management API — миграция и первая заливка данных сделаны
  напрямую, `SUPABASE_SERVICE_ROLE_KEY` недоступен в песочнице). Новый
  скрипт `scripts/sync-bir-primary-market.mjs` — три категории через
  найденные AJAX-эндпоинты bir.by (`ajax/get-search-objects-new/` с
  `type=live`/`vid=Апартаменты` для апартаментов двумя проходами по
  `stage[]`, `type=pantry` для кладовых, `ajax/get-search-objects-com/`
  для коммерческих — делится на "Торговые помещения"/"Офисы" по полю
  "Этаж" при записи). Итог: 3389 строк (1466 апартаментов, 1759
  кладовых, 101 торговых, 63 офиса). Локальный запуск скрипта требует
  `NODE_USE_ENV_PROXY=1` (Node `fetch` не подхватывает `HTTPS_PROXY`
  автоматически, в отличие от `curl` — потрачено время на диагностику
  первого 403/ECONNRESET, прежде чем вспомнил про этот флаг из раздела
  "Workflow" CLAUDE.md).

  На гиде района заполнен ранее заготовленный плейсхолдер "Первичный
  рынок коммерческой недвижимости" — новая связка
  `data/primaryMarketOffers.ts` + `lib/primaryMarketOffersApi.ts` (тот же
  паттерн Foo/FooRow + fetchFoo, что у `market_offers`), таблица сводки
  по 5 строкам (апартаменты сдано/строится отдельно, торговые, офисы,
  кладовые) — количество, диапазон площади, медианная цена за чистый м²
  (`primaryNetAreaM2`/`primaryNetPricePerM2Eur`, тот же принцип
  `netSize`/`netPricePerSqm`, что у market_offers). Проверено мок-тестом
  (Playwright, замоканный ответ `primary_market_offers` с реальными
  числами из офисной записи с террасой) — площадь и цена в таблице
  корректно пересчитаны по чистому м² (25,8 м² вместо 48,6 общих).

- **2026-08-24 — Гид Минск Мира: список транспорта в блоке "Транспорт"
  дополнен.** Владелец озвучил новые номера автобусов/троллейбусов —
  явно предупредил "не убираем старое... могут быть повторы", поэтому
  новые номера объединены с уже имевшимся списком через `Set` (не
  конкатенация — иначе задвоились бы совпадающие вроде троллейбуса 82,
  который владелец и сам продиктовал дважды в одном сообщении), итог
  отсортирован по возрастанию номера (суффиксы вроде "47с" — по ведущему
  числу). Плюс две новые категории тем же заходом: маршрутки (8 новых
  номеров, раньше в блоке вообще не было такой строки — см. более раннюю
  запись в этом журнале про отложенное добавление маршруток без
  вымышленных номеров) и электричка ("До станции Минск-Южный") — обе
  добавлены новыми строками в блок "Транспорт" тем же паттерном
  pill-списка, что уже используется для метро/автобусов/троллейбусов,
  ничего нового не изобреталось.

- **2026-08-24 — Гид Минск Мира: доработка блока "Паркинги" по
  спарсенным данным bir.by + новый блок "Стихийная парковка".**
  Владелец по скриншоту блока "Паркинги", 5 правок:
  1. Цены убраны из карточек ("будут в другом блоке") — остались только
     количество мест/паркингов/домов и площадь.
  2. Количество предложений на первичке (bir.by) — 2 769 мест, в вводном
     абзаце блока. На вторичке (Kufar/Realt, "только из верифицированных")
     не добавлено — категории "машиноместа" нет в `MARKET_PROPERTY_TYPES`
     (только Офисы/Торговые помещения/Кладовые), владелец подтвердил
     без уточняющего вопроса: "если нет категории, спарсим и загрузим
     позже" — не стал городить временную заглушку под то, чего пока нет.
  3. Количество паркингов/домов посчитано по уникальным кодам "Паркинг
     X.Y" в уже собранном срезе (см. запись про парсинг машиномест ниже) —
     у крытых "13.1 1с"/"13.1 2с" оказались двумя подъездами ОДНОГО
     здания (общий адрес в сырых данных) → считаются одним паркингом,
     итого 3 крытых паркинга; у подземных таких сдвоенных кодов не
     нашлось → 11 домов.
  4. Список паркингов с адресами — под `<details>/<summary>` (тот же
     паттерн, что уже используется в `FaqAccordion.tsx`, ничего нового не
     изобреталось), сгруппирован по категориям. Адрес известен только для
     4 из 11 подземных кодов и всех 3 крытых — для остальных 7 bir.by не
     публикует адрес НИГДЕ (проверено — даже на отдельной карточке
     объекта его нет, только "Наименование/Подъезд/Номер/Этаж/Площадь"),
     честно помечено "адрес не публикуется" вместо того, чтобы гадать
     или домысливать по номеру дома.
  5. Новый отдельный блок "Бесплатная стихийная парковка"
     (`id="street-parking"`) — владелец: "стоит уточнить, что они в
     резком дефиците... особенно вечером". Тот же паттерн
     warning-колбэка (жёлтый фон + `TriangleAlert`), что уже используется
     в блоке "Плотность бизнеса по нишам".
  Заодно синхронизированы описания тех же двух пунктов в "Виды
  недвижимости" (без цен, "3 паркинга"/"11 домов" вместо диапазонов цены) —
  чтобы не расходиться с блоком "Паркинги" (тот же принцип "эта же инфа в
  двух местах", который владелец задавал изначально для этого раздела).

  Вдогонку, 4 правки по свежему скриншоту:
  1. "1 658"/"1 111" читались как общее число мест в комплексе, а не как
     то, что реально в продаже сейчас — рядом с числом добавлена явная
     подпись "мест в продаже" (раньше это уточнение было только в общем
     вводном абзаце блока, легко упускалось).
  2. "Адрес не публикуется" → "Адрес не присвоен" — владелец поправил
     формулировку (точнее описывает причину: не то что bir.by скрывает
     адрес, а что у части паркингов его просто нет).
  3. "Бесплатная стихийная парковка" была отдельным блоком-секцией —
     владелец: "должна быть не отдельным блоком, а отдельной карточкой
     внутри блока паркингов". Перенесена третьей карточкой в тот же grid
     (стал `sm:grid-cols-3`), убрана из `SECTION_NAV` как отдельная секция.
  4. Жёлтая плашка-предупреждение (`bg-warning-bg`) владельцу не
     понравилась — убрана, текст обычного мутного цвета, предупреждающая
     функция осталась только на маленькой жёлтой иконке `TriangleAlert`
     перед текстом.

- **2026-08-24 — Гид Минск Мира: "Рынок недвижимости" → "Вторичный
  рынок" + заготовка "Первичный рынок".** Владелец: переименовать блок
  (он же сводка по объявлениям Kufar/Realt — по сути вторичка) в
  "Вторичный рынок коммерческой недвижимости" и заложить над ним такой
  же блок "Первичный рынок коммерческой недвижимости" — "сделаем
  позже". Новая секция `id="primary-market"` — тот же паттерн
  плейсхолдера, что уже использован для "Паркинги" до заполнения
  ("Информацию добавим отдельно"). `SECTION_NAV` дополнен пунктом на
  нужном месте (перед "Вторичный рынок").

- **2026-08-24 — Парсинг машиномест bir.by + агрегированная статистика в
  двух блоках гайда.** Владелец: "спарсить инфу о машиноместах в Минск
  Мире... а то открывается по 15 штук за раз вручную, уже надоело" —
  ссылка на фильтр `bir.by/search-by-parameters/parking/`. Домен изначально
  был заблокирован прокси песочницы (как и другие внешние сайты в этой
  сессии) — владелец добавил его в Allowed domains по ходу задачи.
  `WebFetch`-тул при этом продолжил отдавать `EGRESS_BLOCKED` даже после
  добавления домена (у него, похоже, свой отдельный список разрешённых
  доменов, не совпадающий с общим прокси) — обошлось прямым `curl`,
  который уже проходил. В HTML страницы таблица машиномест изначально
  пустая (рендерится через AJAX) — нашёл сам вызов в инлайновом `<script>`
  на странице: `POST bir.by/ajax/get-search-objects-car/`, с параметрами
  фильтра (комплекс/площадь/цена) и `limit`. Подобрав `limit=6000` (вместо
  дефолтных 15), получил все объявления одним запросом — 2769 позиций для
  фильтра "Минск-Мир". Кнопка на самой странице показывает другую цифру
  ("Найдено 4475") — не совпадает с тем, что реально отдаёт та же AJAX-
  логика при отдельном запросе счётчика (`get-number-of-objects-for-pc-
  parking/`, тоже вернул 2769) — расхождение не объяснил, но взял то,
  что реально возвращает таблица/счётчик по одинаковым параметрам, не
  цифру с кнopки. Спарсенные данные (per-объявление: паркинг/дом, адрес,
  номер места, площадь, цена BYN/EUR) сохранены в CSV и отправлены
  владельцу файлом — сырые данные не хранятся в репозитории.

  Владелец в ответ: "там есть два вида машиномест. Дешёвые... 6-9 тысяч
  евро — крытые паркинги. Более дорогие — подземные, внутри существующих
  домов" — цифры сам не считал заранее, узнал их постфактум по факту
  спарсенных данных: цены и правда чётко группируются на два кластера без
  единого объявления между 9900 € и 13000 € — граница видна в самих
  данных, слова владельца подтвердились количественно. Агрегаты (диапазон
  цены/среднее/площадь/доля рынка) вставлены в два места: (1) блок
  "Паркинги" (`id="parking"`, до этого был заготовкой-плейсхолдером,
  см. запись про "Транспорт и парковка" чуть ниже) — две карточки
  сравнения крытых/подземных; (2) "Виды коммерческой недвижимости" —
  заполнены только 2 из 8 пунктов ("Машиноместа на паркингах",
  "Подземные машиноместа"), остальные 6 остались плейсхолдерами до
  вводных от владельца, как и было изначально задумано для этого блока.

- **2026-08-24 — Гид Минск Мира: правки блока "Транспорт и парковка".**
  Владелец по скриншоту, 3 пункта: (1) аэропорта рядом на самом деле нет —
  строка про Национальный аэропорт/Южную магистраль убрана как
  недостоверная; (2) маршрутки — "не хватает маршруток, они туда активно
  ходят", но реальных номеров маршрутов нет под рукой ни у меня, ни в уже
  собранной фактуре — не стал выдумывать транспортные данные на странице,
  где до этого каждая цифра тщательно сверялась (метро/автобусы/
  троллейбусы — с реальными номерами маршрутов), дожидаюсь от владельца
  списка; (3) блок про паркинг убран из карточки транспорта полностью,
  вместо него — отдельная секция-заготовка `id="parking"` ("Информацию по
  паркингам добавим отдельно"), владелец пришлёт данные следующим шагом.
  Заголовок карточки транспорта сокращён с "Транспорт и парковка" до
  "Транспорт" (парковка больше не её содержимое) — `SECTION_NAV` дополнен
  новым пунктом "Паркинги".

- **2026-08-24 — Гид Минск Мира: новый блок "Ключевые цифры" (8 плиток)
  сразу после hero.** Владелец видел карточки цифр (было 4: возраст,
  метро, Avia Mall, маршруты) и попросил расширить до 8, дать заголовок
  "Ключевые цифры", разместить их сразу после главного блока, перед
  "Застройщиком района". Новые 4 — частично на основе уже посчитанного
  в этой же сессии (80 000 жителей района, плотность 427 чел/га — тот
  же источник, цитата из статьи Onliner), частично из уже собранной
  фактуры о застройщике (320 га, ~30 000 квартир — заявленный масштаб
  Dana Holdings). Обёрнуто в отдельную секцию `id="key-stats"`, иконка
  Sparkles, добавлен пункт в `SECTION_NAV`.

  Отдельно стоит зафиксировать: пока шла эта правка, в параллельном
  диалоге (владелец ведёт вторую линию правок по фичам в этом же файле,
  см. "Workflow при внесении изменений" в CLAUDE.md) плитки цифр как раз
  убрали с первого экрана — перенесли под карточку застройщика,
  "владелец попросил освободить первый экран под чисто главный блок".
  При `git pull` это дало реальный конфликт (не в CLAUDE.md, как обычно,
  а прямо в JSX) — уточнил у владельца напрямую в этом диалоге, какая
  инструкция актуальна: подтвердил текущую (сразу после hero). Их
  релокацию отменил — убрал безымянный дублирующий рендер `statTiles`
  после карточки "Управляющая компания", оставил один блок с заголовком
  на первом экране. Держать в голове: если ещё раз возникнет похожий
  конфликт по расположению блока на этой странице между двумя линиями
  правок — переспрашивать владельца, чья инструкция свежее, не решать
  самостоятельно молча в пользу той или другой стороны.

- **2026-08-24 — Гид Минск Мира: правка "Генераторы ежедневного
  трафика" — фактическая ошибка про Avia Mall и финцентр.** Владелец
  указал на блок: "Сам авиамолл вообще не заселен и не является точкой
  притяжения пока... Также как и международный финансовый центр, он
  пока не введен в эксплуатацию, но станет большой точкой притяжения в
  будущем" — прежний текст ошибочно подавал оба объекта как уже
  работающие генераторы трафика. Так как прямой доступ к Gemini из
  песочницы недоступен (см. запись 2026-08-22 ниже), владельцу отдан
  промт для ручного прогона через Gemini (с явным требованием разделить
  "уже сейчас" и "скоро/в перспективе", не выдумывать даты, сохранить
  тон/объём остальных блоков страницы) — владелец вернул готовый текст
  от Gemini, реализовано как есть. Формат при переносе в код — не два
  абзаца, а 4 пункта (жители района, туристы и арендаторы, гости города,
  запуск ТЦ/финцентра), каждый с жирным подзаголовком и своей иконкой
  (уточнение владельца: "иконки в самом блоке", без ссылок — первая
  версия с внешней ссылкой на avia-mall.by в тексте пункта была явно
  отклонена: "Вставляй только текст без ссылок"). Новый `trafficHighlights`
  рядом с `audienceHighlights` — тот же паттерн (label+text в `<ul><li>`),
  плюс иконка на пункт, которого раньше в этом паттерне не было.
  Вдогонку — иконки перед пунктами всё же убраны (не понравилось,
  вернулись к обычному `audienceHighlights`-виду без иконки на пункт),
  заодно подзаголовки уточнены: "Жители района" → "80 000 жителей
  района", "Запуск ТЦ и финцентра" → "Заселение ТЦ и запуск Финансового
  центра". Ещё вдогонку — концовка пункта про ТЦ/финцентр смягчена с
  "позволяет занять ликвидную точку... роста ставок" (звучало как
  инвестиционный совет) на "станет дополнительной точкой притяжения и
  даст району ещё один буст".

  Вдогонку — шкала плотности населения. Владелец видел статью Onliner
  про аномальную плотность в Минск Мире, прислал цитату оттуда (сама
  статья на realt.onliner.by недоступна из песочницы — домен
  заблокирован прокси, цифры не перепроверить самостоятельно, взяты как
  есть из присланного текста владельца): норматив генплана Минска для
  "высокоплотной застройки" — плотность населения до 290 чел/га, в
  Минск Мире — 427 чел/га. Добавлена шкала прямо в блок "Генераторы
  трафика" — две горизонтальные полосы (норматив приглушённым серым,
  Минск Мир акцентным primary), подпись с готовым множителем ("почти в
  1,5 раза выше"). Переиспользован уже существующий паттерн
  прогресс-баров (тот же, что у "Плотность бизнеса по нишам" —
  продуктовые/табак), не изобретался новый компонент.

  Ещё вдогонку — шкала понравилась, владелец попросил вынести её из
  карточки "Генераторы трафика" в отдельный блок, "как раз перед блоком
  плотности бизнеса". Своя секция `id="population-density"`, иконка
  Building2, заголовок "Плотность населения". `SECTION_NAV` дополнен
  новым пунктом на нужном месте в порядке — заодно обнаружился и
  исправлен более старый баг: `property-types` и `business-density` в
  самом списке шли не в том порядке, что реальные секции на странице
  (несовпадение появилось ещё в прошлом раунде при первой сборке меню,
  не заметили тогда).

  Ещё вдогонку — владелец прислал ещё подборку цифр по плотности (AI-обзор
  с несколькими источниками, не прямая цитата одной статьи), попросил
  самому выбрать нужные и обновить график ("плотность частной застройки
  не нужна"). В подборке был повторный вариант норматива "высокоплотной
  застройки" — 60–76 чел/га при тех же 6000–7600 м²/га, что и в уже
  проверенной цитате (231–290 чел/га) — числа не бьются арифметически
  (при типичных ~26–30 м² жилья на человека 6000–7600 м²/га даёт
  214–271 чел/га, что соответствует именно 231–290, не 60–76): этот
  вариант отброшен как вероятная ошибка конкретно того ответа, прежний
  проверенный норматив (290) оставлен как есть. Взяты только цифры без
  внутренних противоречий и точнее ложащиеся в тезис "самая плотная
  застройка Минска": среднегородская плотность (57 чел/га — сходится с
  независимым расчётом из Википедии о населении Минска ещё из более
  раннего WebSearch этой сессии) и самые плотные существующие районы
  города — Фрунзенский и Московский (~100 чел/га, со ссылкой на Главное
  статистическое управление Минска). Шкала стала map по массиву из 4
  точек вместо двух захардкоженных полос (Минск в среднем → Фрунзенский/
  Московский → норматив → Минск Мир), подпись — множитель сразу к обоим
  ключевым базам сравнения ("в 7,5 раза плотнее среднего по городу... в
  1,5 раза выше норматива").

- **2026-08-23 — Гид Минск Мира: меню-оглавление на ликвид-глас подложке
  + мобильная версия.** Продолжение предыдущего пункта (боковое меню с
  якорями), два уточнения владельца подряд.
  1. "Было бы классно и меню выделить в блок с подложкой ликвид глас, а к
     пунктам добавить иконки" — навигация обёрнута в ту же карточку
     `glassCardClass`/`glassCardShadow`, что используется по всему
     сайту (не переизобреталось), у каждого из 12 пунктов — иконка,
     взятая из заголовка того же раздела (Застройщик — `HardHat`,
     Целевая аудитория — `Users`, и т.д. по списку `SECTION_NAV`) —
     единообразие, не произвольный набор иконок.
  2. "На телефоне и планшете вообще не видно, а было бы круто. На
     платформе меню классно прячется, давай и на лендинге повторим, на
     превью пусть будет слово 'Содержание гайда'" — на ширинах `<lg`
     меню было полностью недоступно (боковая колонка `hidden lg:block`).
     Воспроизведена ровно та же off-canvas схема, что в админке
     (`Sidebar.tsx`/`AppLayout.tsx`, изучены построчно перед копированием
     паттерна, не написано с нуля): плавающая кнопка-триггер
     "Содержание гайда" (`fixed bottom-4 right-4`, glass-pill), клик —
     затемнение фона + выезжающая слева панель с теми же 12 пунктами
     (иконка+текст), закрытие по клику на пункт (с прокруткой к разделу)
     или на крестик. Проверено Playwright на трёх ширинах (390/900/1440) —
     на 390 и 900 виден только триггер и рабочее меню, на 1440 виден
     только десктопный вариант с ликвид-глас подложкой, оверлапов нет.
  Попутно фикс из предыдущего раунда закреплён и остаётся в силе:
  `position: sticky` для этой навигации не работает из-за
  `overflow-x: hidden` на `body`/`#root` (см. запись ниже про баг) —
  используется `position: fixed` с координатами, измеренными через
  `getBoundingClientRect()`.

- **2026-08-23 — Гид Минск Мира: боковое меню-оглавление с якорями.**
  Владелец: "боковое левое меню с заголовками основных пунктов
  страницы... каждая ссылка - якорь, при клике переносит на нужную
  часть страницы". Именно то, для чего просилось свободное место
  слева от узкой колонки контента, на которое жаловались в предыдущем
  раунде ("сайт слишком узкий") — не растягивать карточки, а
  использовать это место под навигацию.
  `SECTION_NAV` — 12 пунктов по всем H2-секциям (Застройщик, Целевая
  аудитория, Генераторы трафика, Виды недвижимости, Плотность
  бизнеса, Рынок недвижимости, Аналитика по сферам бизнеса, Решения
  под бизнес, Транспорт и парковка, Карта района, Частые вопросы, Red
  One), `id` на обёртке каждой секции, `FaqAccordion.tsx` получил
  опциональный проп `id` (общий компонент, используется и на `/one`,
  остальным вызовам без `id` ничего не меняет). Обычные `<a
  href="#id">`, без JS-роутинга.
  Технический нюанс по пути: изначально сделал на `position: sticky`
  — совсем не сработало, ссылки просто уезжали со скроллом. Причина —
  `body`/`#root` в `index.css` намеренно носят `overflow-x: hidden`
  (фикс мобильного off-canvas сайдбара, комментарий там же), а по
  спецификации CSS это принудительно переводит `overflow-y` в `auto`
  ДАЖЕ если он нигде явно не задан — и это ломает `position: sticky`
  для любых потомков на всём сайте, не только в этом блоке. Проверил
  вручную (Playwright + `page.addStyleTag`) — явный `overflow-y:
  visible` поверх тоже не спасает, браузер всё равно считает
  эффективный overflow как auto. Трогать `overflow-x: hidden` на
  body/#root не вариант — реальный, документированный фикс под другую
  проблему (см. его же комментарий), ломать его ради этого меню
  нельзя. Решение — `position: fixed` (на него overflow предков не
  влияет, ломают только `transform`/`filter` у предков, которых в
  цепочке нет) с `left`/`width`, измеренными от реального узла aside
  через `ref` + `resize`-listener — при любой ширине экрана колонка
  меню остаётся точно на своём месте, не подобранными на глаз
  координатами. `top-24` (96px) — с запасом ниже хедера сайта, чтобы
  не перекрывался даже в самом верху страницы.
  Меню показывается только от `lg` и выше (`hidden lg:block` на
  aside) — на планшете/мобильном для него просто нет места сбоку.
  Проверено Playwright: `getBoundingClientRect()` до и после клика по
  якорю — на sticky координаты навигации уезжали вместе с контентом
  (баг воспроизведён и подтверждён точечно измерением, не только "на
  глаз"), на fixed остаются неизменными при любом `scrollY`.
  Скриншотами — верх страницы (меню не наезжает на хедер), после
  клика по "Транспорт и парковка" (меню на месте, страница
  проскроллена), мобильная ширина 800px (меню корректно скрыто).

- **2026-08-23 — Гид Минск Мира: блок "Виды коммерческой недвижимости"
  + откат ширины.** Владелец: "Мне нужен новый смысловой блок - Виды
  коммерческой недвижимости в Минск Мир" с явным списком 8 типов
  (Торговые помещения, Бизнес-апартаменты, Офисные помещения,
  Машиноместа на паркингах, Подземные машиноместа, Кладовые,
  Помещения в ТЦ Avia Mall, Офисы в МФЦ) — "по каждому из них я дам
  разные вводные, пока заложи блок". `districtPropertyTypes` — иконка
  + заголовок + `description` (пока `null` у всех восьми, рендерится
  плейсхолдером "Текст добавим отдельно"). Сознательно НЕ придумал
  описания заранее — владелец явно обещал прислать вводные по каждому
  типу отдельно, а в этом проекте уже не раз ловили Gemini на
  переоценке фактов сверх присланных данных (см. запись про блок
  "Застройщик района"), сочинять текст без вводных было бы той же
  ошибкой. Блок отдельно от `MARKET_PROPERTY_TYPES`
  (`data/marketOffers.ts` — Офисы/Торговые помещения/Кладовые, три
  категории сырых объявлений с Kufar/Realt для сводной таблицы цен) —
  здесь список шире, с форматами, которых в парсинге вовсе нет
  (апартаменты, машиноместа, Avia Mall, МФЦ). Секция вставлена перед
  "Рынок коммерческой недвижимости" — после общего портрета района,
  перед конкретными цифрами по ценам.
  Тем же сообщением, ещё до этой правки, владелец попросил откатить
  расширение страницы из предыдущего раунда (см. запись ниже) —
  "верни более узкую верстку как раньше, мне не нравится новая
  ширина". `max-w-6xl` → обратно `max-w-3xl` (хедер и контент), и
  убраны все `max-w-2xl`/`[&>div]:max-w-2xl` добавки, которые чинили
  читаемость текста именно при широком контейнере ("Целевая
  аудитория", "Генераторы трафика", "Аналитика по сферам бизнеса",
  финальный CTA, `FaqAccordion.tsx`) — при узкой раскладке они снова
  не нужны, оставлять было бы лишней сложностью без пользы.
  Проверено Playwright-скриншотом — новый блок (3 колонки на
  десктопе) хорошо смотрится и в узкой раскладке, остальная страница
  вернулась к виду "как раньше" (сверено с записью ниже, где
  зафиксирован набор мест, где добавлялся `max-w-2xl`).

- **2026-08-23 — Гид Минск Мира: страница шире, узкие карточки — во
  всю ширину.** Владелец прислал тот же скриншот хиро-блока (уже после
  сокращения текста, см. запись ниже) — "вроде текста мало, а всё
  равно мало места. Мне кажется, сайт слишком узкий. Давай сделаем все
  блоки шире, посмотрим как будет смотреться". Контейнер всей страницы
  (и хедер, и весь контент) был `max-w-3xl` (768px) — расширил оба до
  `max-w-6xl` (1152px).
  Проверил Playwright-скриншотом всю страницу целиком на 1440px — сетки
  (плотность бизнеса по нишам, портрет арендаторов, банковская матрица,
  бары прогресса магазинов/табака) на новой ширине смотрятся заметно
  лучше, но у ЧИСТЫХ текстовых абзацев (без сетки рядом) строка стала
  неприлично длинной для комфортного чтения — классический побочный
  эффект широкой колонки прозы, который сам по себе не заметил бы,
  если бы не посмотрел на реальный скриншот всей страницы, а не только
  хиро-блока, который просил владелец.
  Решение — не то же самое, что "откатить ширину": страница остаётся
  широкой, но текстовые блоки внутри неё сужены изнутри, а не сама
  карточка. "Целевая аудитория", "Генераторы трафика", финальный CTA
  Red One — добавлен `max-w-2xl` прямо на текстовый элемент. Секция
  "Аналитика по сферам бизнеса" (10 подряд подблоков — Медицина/
  Общепит/Спорт/Банки/СТО/Салоны/Магазины/ПВЗ/Цветы/Табак) — вместо
  правки каждого блока по отдельности одна общая утилита на обёртку
  `[&>div]:max-w-2xl` (Tailwind arbitrary variant на прямых
  детей-`<div>`) — разделительные линии между подблоками (divide-y)
  остались на всю ширину карточки, сужается только контент внутри
  каждого. `FaqAccordion.tsx` (общий компонент, использующийся и на
  `/one`) — тоже `max-w-2xl` на ответ, узкий ответ уместен в обоих
  местах одинаково. Карточка/фон/бордер везде остаются на всю ширину
  (визуально в ряд с соседними wider-карточками) — сужается только
  сам текст, привычный паттерн "широкая карточка, узкая колонка
  чтения", а не хаотично разные ширины карточек в одном вертикальном
  списке.
  Проверено Playwright-скриншотами по частям (вся страница 6912px
  высотой разбита на 5 кусков python'ом + отдельно открытый вопрос
  FAQ, чтобы увидеть развёрнутый ответ).

- **2026-08-23 — Гид Минск Мира: короче H1 и подзаголовок хиро-блока.**
  Владелец прислал скриншот хиро-секции — "хочется и заголовок, и текст
  короче, чисто визуально много. Подумайте с Gemini" — с оговоркой не
  трогать ключевые слова в тегах. Попробовал реальный вызов Gemini через
  ProxyAPI (тот же способ, что и раньше в этой сессии для текста блока
  "Застройщик района") — домен снова недоступен из песочницы (таймаут
  при прямом curl, подтверждено через `$HTTPS_PROXY/__agentproxy/status`
  — другие сторонние домены типа google.com отбиваются гейтвеем быстро
  с 403, а proxyapi.ru просто не отвечает, тот же симптом, что уже
  документирован в этом файле для transcribe-meeting). Переписал текст
  сам, без Gemini, ориентируясь на уже устоявшийся для страницы тон
  (короткие фразы без воды — тот же стиль, что задавался в ТЗ для
  Gemini в исходном полном редизайне).
  `PAGE_H1`: "Гайд и аналитика по офисам и коммерческим помещениям в
  районе Минск Мир" (11 слов, 4 строки на экране) → "Коммерческая
  недвижимость Минск Мира: гайд и аналитика" (7 слов, 3 строки).
  `INTRO_TEXT`: было два предложения с повтором "коммерческая
  недвижимость в районе Минск Мир" (уже сказано в новом H1 — стало
  избыточно) → одно предложение по существу. `TITLE`/`DESCRIPTION`
  (`<title>`/og/canonical) НЕ трогал — владелец явно попросил оставить
  ключевые слова в тегах без изменений, они уже отдельно подобраны под
  целевые запросы (см. комментарий в файле про разницу TITLE vs
  PAGE_H1). Проверено Playwright-скриншотом хиро-блока.

- **2026-08-23 — /admin/market-offers: фильтры-тумблеры перенесены к
  таблице одиночных.** Владелец прислал скриншот страницы верификации:
  тумблеры (Обработка/Источник/Сделка/Отделка) стояли в карточке
  прямо над блоком дублей и выглядели так, будто должны на него
  влиять — "выбираю, а ничего не меняется. Надо либо чтобы работали,
  либо убирать". Они и не должны были — рядом уже была текстовая
  подсказка про это, — но чисто визуально были "не на своём месте":
  оставлять группу дублей ПОЛНОЙ принципиально важно (иначе легко
  спрятать половину пары при сравнении), значит логику менять было
  нельзя, а вот расположение — да. Карточка с четырьмя тумблерами
  физически переехала вниз, к заголовку "Одиночные объявления" и
  самой таблице, которую она фильтрует. Сверху над блоком дублей
  остался только поиск по адресу/типу — он не ломает пары (дубли уже
  сгруппированы по адресу, поиск сужает видимые группы целиком, не
  разбивает их), поэтому продолжает действовать и на дубли, и на
  одиночные. Чистая перестановка JSX, без изменений в
  filtered/duplicateGroupsList — проверено Playwright-скриншотом.

- **2026-08-23 — Рынок недвижимости: только верифицированные в
  сводке + пересмотр категорий.** Спросил владельца про следующие
  идеи упрощения верификации (клавиатурные шорткаты для конвейера) —
  ответ "нет, не нравится, остальные идеи придут от Светланы", и
  переключение на правки лендинга Минск Мира. Два запроса разом:
  1) "в таблице со списком цен попадали только верицифированные
  объявления" — `buildMarketPivot`/`countSmallFinishedOffices`
  (`DistrictGuidePage.tsx`) получили `offer.reviewed` в условие
  фильтра (было — сырые непроверенные данные Kufar/Realt могли
  искажать публичную сводку и подсчёт дефицита маленьких офисов).
  2) Пересмотр категорий (перечень владельца, все 6 пунктов):
  "Магазины, торговые помещения" → "Торговые помещения" (1 этаж),
  "Офисы" без изменений (2 этаж и выше — владелец пояснил смысл,
  авто-деления по этажу не делал, это ручное суждение при
  верификации), "Сфера услуг"/"Общепит" убраны как категории (не
  нужны для сводки), но объявления в базе НЕ трогаем — "помещения
  нужны, Светлана отсортирует по этажу" — при верификации она сама
  решит, Офисы это или Торговые. "Склады" → "Кладовые" (просто
  переименование, не новая категория — цена/reviewed сохранены).
  "Промышленные помещения"/"Прочая коммерческая" — типы убраны
  целиком, владелец прямо сказал сбросить: "объявления по умолчанию
  сделай просто непроверенными без категории" / "сделай без
  категории, но все карточки оставь" — 50 строк (12+38) получили
  `property_type='Без категории'`, `reviewed=false`, ни одна не
  удалена.
  `MARKET_PROPERTY_TYPES` (`data/marketOffers.ts`) сокращён с 7 до 3
  вариантов — этот же список одновременно и опции формы
  редактирования на `/admin/market-offers`, и порядок строк сводной
  таблицы (`MARKET_PROPERTY_TYPE_ORDER = MARKET_PROPERTY_TYPES` в
  `DistrictGuidePage.tsx`) — поэтому убранные категории сами
  перестали попадать в сводку, отдельный фильтр не понадобился.
  Новая константа `UNCATEGORIZED_PROPERTY_TYPE = 'Без категории'` —
  значение НЕ входит в список вариантов (не выбирается из формы),
  видно только как повод пересмотреть объявление при верификации.
  Кастомный `Select` (не нативный) спокойно показывает текущее
  значение вне списка опций (устаревшую категорию или "Без
  категории") как обычный текст — она просто не подсвечена, пока
  Светлана не выберет что-то из трёх новых вариантов.
  Одноразовая SQL-миграция для уже существующих 807 строк выполнена
  сразу через Management API (переименования + сброс
  Промышленные/Прочая). Отдельно поправил ОБА sync-скрипта для
  будущих месячных синков — без этого следующий синк начал бы снова
  писать старые названия категорий с нуля:
  - Kufar отдаёт `property_type` одной строкой на весь commercial-
    раздел разом, не по отдельным URL на категорию (в отличие от
    Realt) — добавил `normalizePropertyType()` постфильтром сразу
    после получения (переименования + Промышленные/Прочая → "Без
    категории"), фильтрация URL тут физически невозможна.
  - У Realt категория — часть URL (`CATEGORIES` с `slug`), поэтому
    для 'production'/'storages' пошёл более чистым путём — убрал их
    из списка целиком, они больше не запрашиваются вовсе (экономит и
    запросы). 'shops'/'warehouses' получили новые русские названия
    прямо в том же списке. 'services'/'restorant-cafe' не тронуты.
  Проверено dry-run на реальных сайтах (`--json`, оба скрипта) —
  подтверждено на живых данных: ни одного старого названия категории
  в выводе, "Без категории" появляется у бывших Промышленные/Прочая
  с Kufar. DistrictGuidePage — Playwright-скриншотом с мок-данными (5
  объявлений: reviewed+валидная категория, unreviewed+валидная,
  reviewed+убранная категория, reviewed+"Без категории", и выброс по
  цене) — в сводку попали ровно 2 нужные строки, остальные три
  корректно исключены (видно в счётчике "Небольших офисов... 1 в
  аренду" — считает только reviewed-строку, не выброс).

- **2026-08-23 — Скрытый лог активности сотрудников + супердоступ
  только владельцу.** Владелец: "отдельная страница, не видная
  никому... начнём со Светланы, со сбора количества верифицированных
  карточек за день". Проверил перед реализацией текущие
  access_profiles — оказалось, что Светлана и Степан уже имеют
  `pages: 'all'` (полный доступ), значит обычный гейтинг по ключу
  страницы (RequirePage/PageKey) их не остановил бы, узнай они URL
  напрямую. Сообщил это владельцу в процессе; в ответ пришло уточнение:
  "Делаем супер-доступ только мне - Трэшмену. Светлане отключаем
  доступ к логам действий и к транзакциям".
  Решение — новое измерение доступа `is_super_admin` (колонка в
  access_profiles), ортогональное `pages`: `'all'` даёт все ОБЫЧНЫЕ
  разделы, но не подразумевает попадание в "видно только владельцу".
  `true` выставлен только профилю "Трэшмен" (прямой SQL, не через UI
  Settings — insertAccessProfile/updateAccessProfile теперь физически
  не принимают это поле в теле запроса, потому что страница
  /admin/settings доступна и Степану, и Светлане — отдавать им форму,
  где можно включить себе супер-доступ, было бы бессмысленно).
  Новый компонент `RequireSuperAdmin` (components/layout) — гейт
  строже `RequirePage`: не пропускает даже `pages:'all'`, только
  `isSuperAdmin===true`. Новая страница `/admin/activity-log`
  (`ActivityLog.tsx`) обёрнута именно в него, а не в обычный
  `RequirePage` — и намеренно НЕ заведена в `data/pages.ts`/сайдбаре
  вовсе (даже без PageKey): открыть можно только зная точный URL, и то
  только с `isSuperAdmin`. Страница группирует сырые записи новой
  таблицы `activity_log` (id, profile_id, profile_name, action,
  created_at; RLS открыт на anon select/insert — тот же уровень
  защиты, что у всех таблиц в проекте) по календарному дню (часовой
  пояс браузера) и сотруднику — тот же принцип "считать агрегат на
  лету из сырых строк", что и везде в аналитике проекта, не
  предпосчитанный агрегат.
  `logActivity()` (`lib/activityLogApi.ts`) вызывается из
  `MarketOffersReview.tsx` сразу после успешного сохранения карточки
  верификации (`action='market_offer_verified'`) — пишет молча, без
  `withRetry` и не блокируя основной поток: сбой логирования не
  должен портить реальную работу (сохранение карточки), а слепой
  повтор рисковал бы задвоить счётчик, если первая попытка на деле
  долетела. `action` в базе — свободный `text`, не enum: список
  известных типов только для человеческих подписей
  (`ACTIVITY_ACTION_LABELS`), чтобы новые виды действий не требовали
  миграции — задел на то, что лог будет расширяться за пределы одной
  метрики.
  Отдельно — Светлане `pages` заменён с `'all'` на явный список всех
  обычных разделов КРОМЕ `transactions`: теперь "Транзакции" у неё в
  сайдбаре серые с замочком, прямой переход по ссылке блокируется
  `RequirePage`. Степана не трогал (не было в просьбе) — остался
  `pages:'all'`, `is_super_admin=false` (лог активности ему тоже не
  виден). Оговорка владельцу: раз Светлана всё ещё видит "Настройки",
  она технически может зайти в форму профилей и вернуть себе
  "Транзакции" вручную — та же модель "пароли не настоящая
  авторизация", что и везде в проекте (см. PasswordGate.tsx), не
  стал городить отдельную защиту для формы профилей без явной
  просьбы.
  Проверено Playwright-скриншотами под обоими профилями (мок
  access_profiles/activity_log): Трэшмен видит страницу и данные
  (сегодня/по дням/по сотруднику), Светлана получает "недоступна для
  вашего доступа" при прямом переходе на `/admin/activity-log` и видит
  заблокированный пункт "Транзакции" в сайдбаре.

- **2026-08-23 — /admin/market-offers: пошаговая верификация "Начать
  → Далее → Далее".** Спросил владельца, как ещё упростить работу
  Светланы — предложил конвейер "Сохранить → следующее" вместо
  ручного клика по каждой строке; владелец согласился и уточнил
  формат: кнопка "Начать верификацию", дальше сама ведёт по одному
  объявлению/группе, пока не кончится. Порядок тот же, что и на самой
  странице (сначала нерешённые группы дублей, потом непроверенные
  одиночные) — режим просто фокусирует на одном элементе за раз вместо
  прокрутки списка.
  Ключевое архитектурное решение: НЕ снапшот очереди с индексом, а
  производные значения (`verifyGroupTarget`/`verifySingleTarget`),
  каждый раз вычисляемые заново из текущих `duplicateGroups`/`offers`.
  Причина — очередь-снапшот устарела бы сразу же: например, отклонение
  группы дублей ("Это разные помещения") возвращает её объявления в
  пул одиночных — им всё ещё нужна индивидуальная проверка цены/
  отделки, просто вопрос дублирования снят. Со снапшотом эти
  освободившиеся объявления не попали бы в текущий проход (queue был
  построен раньше, до появления). С реактивным подходом это просто
  работает само — подтверждено тестом (см. ниже): после дисмисса
  группы "Теслы, 7" её собственные два объявления действительно
  показались следующими в очереди на индивидуальную проверку.
  "Пропустить" — не то же самое, что "Это разные помещения": это
  временная отсрочка на время ТЕКУЩЕЙ сессии верификации
  (`skippedGroupKeys`/`skippedSingleIds`, сбрасываются при новом
  запуске), ничего не пишет в базу. Закрытие карточки крестиком/фоном
  во время верификации тоже считается пропуском (иначе эффект
  автооткрытия тут же открыл бы её опять — цель ведь не изменилась) —
  единая функция `closeOrSkipEdit` на Modal.onClose и кнопку
  "Отмена"/"Пропустить".
  Экран завершения — "Всё проверено!" + подсказка сколько пропущено
  (пропущенное не теряется, просто остаётся в обычном списке ниже,
  можно доразобрать вручную).
  Проверено Playwright-сценарием с мок-данными Supabase (перехват
  GET/PATCH/DELETE/POST на market_offers и market_offer_dedup_
  dismissals): полный проход группа → автооткрытие карточки → "Сохранить"
  (реально ставит reviewed=true) → "Пропустить" (реально НЕ ставит) →
  экран "Всё проверено".

- **2026-08-23 — /admin/market-offers: верификация только через
  карточку, дубли первыми.** Владелец прислал скриншот таблицы и
  сказал, что вид не нравится — колонки "Отделка"/"Обработка" были
  переключателями прямо в строке (по 2-3 кнопки каждая), из-за чего
  строка не помещалась по ширине и обрезалась. Попросил упростить
  работу помощницы (Светлана): по умолчанию нужна кликабельная ссылка
  на объявление (открывается в новой вкладке — уже была) + кнопка
  редактирования карточки, верификация происходит там, не построчными
  кликами по таблице. Убрал `FinishStatusPicker`/`ReviewedPicker` из
  строки, заменил статичными бейджами (`FinishBadge`/`ReviewedBadge`,
  только отображают статус); кнопка "Редактировать" получила подпись
  вместо голой иконки-карандаша. Источник (Kufar/Realt) убрал из
  отдельной колонки, вставил мелким текстом под адресом — эта колонка
  была одной из причин переполнения по ширине, а после переноса
  переключателей в модалку оказалась лишней тратой места. Любое
  сохранение через модалку по-прежнему автоматически ставит
  reviewed=true (логика `updateMarketOffer` не менялась) — открыл
  карточку и сохранил, даже без правок, — это и есть подтверждение
  проверки, отдельный тумблер "Проверено" в строке для этого больше
  не нужен.
  Заодно длинные значения (тип "Магазины, торговые помещения", строки
  террасы/чистой цены из прошлой правки) раньше форсили
  `whitespace-nowrap` на всю ячейку и раздували таблицу вширь даже
  после переноса источника — разрешил перенос по словам в этих
  ячейках, таблица теперь помещается без горизонтального скролла на
  обычном экране (проверено Playwright-скриншотом на 1440px с
  реалистичными данными, включая длинный тип и террасу).
  Второе пожелание — "начинать логичнее с дублей, потом одиночные" —
  убрал тумблер "Дубли: Все/Только дубли", секция дублей теперь
  всегда идёт первой на странице (если дубли есть), с заголовком и
  пояснением; одиночные объявления (те, что не попали ни в одну
  группу дублей) — отдельная секция "Одиночные объявления" ниже.
  Обычные фильтры (отделка/сделка/источник/обработка/поиск) действуют
  только на секцию одиночных — дубли по-прежнему показываются без
  фильтров целиком, чтобы не спрятать половину пары, как было и
  раньше в режиме "Только дубли".
  По пути пробовал зафиксировать колонку с кнопкой действий через
  `position: sticky` (чтобы она не уезжала при горизонтальном
  скролле) — оказалось, что sticky на `<td>` внутри
  `overflow-x-auto`-контейнера накладывается поверх соседней колонки,
  а не занимает своё место в потоке (визуальный баг, видно на
  скриншоте). Откатил, вместо этого просто сократил реальную ширину
  таблицы (перенос текста + перенос источника под адрес) — тот же
  результат (кнопка редактирования всегда видна), без хрупкости
  sticky-колонок в таблицах.

- **2026-08-23 — Рынок недвижимости: помещения с террасой считаем по
  чистой площади.** Владелец прислал конкретный пример из базы — Realt
  object/4148005 (аренда торгового помещения, ул. Братская 15): в
  объявлении указано 64.5 м² общей площади, но по плану 25.4 м² из
  них — терраса, чистых 39.1 м². Терраса стоит заметно дешевле
  закрытого помещения, а ни Kufar, ни Realt не выделяют её отдельно в
  структурированных данных — посчитанная по общей площади цена за м²
  занижена и тянет вниз медиану в сводке. Решение то же, что и у
  finish_status/floor — ручная правка на /admin/market-offers, без
  попытки автоопределения (на плане это иногда подписано словом
  "терраса", иногда видно только по форме контура — не парсится
  надёжно). `market_offers` получил `has_terrace boolean` +
  `terrace_area numeric`. `netSize`/`netPricePerSqm`
  (`data/marketOffers.ts`) вычисляют площадь без террасы и
  пересчитывают ту же общую стоимость на неё (`64.5×20 / 39.1 ≈ 32.99
  $/м²` вместо исходных 20 $/м²). В форме редактирования — галочка
  "Помещение с террасой", раскрывающая поле площади террасы и живой
  расчёт чистой площади; в таблице у такой строки под общей
  площадью/ценой отдельной строкой показаны терраса/чистая площадь и
  цена на чистую. Сводная таблица на /rayon-minsk-mir
  (`buildMarketPivot`) и подсчёт дефицита маленьких офисов с отделкой
  (`countSmallFinishedOffices`) теперь бакетируют/усредняют по
  net-функциям, а не по сырым `size`/`pricePerSqm` — правка владельца
  сразу видна в публичной таблице, без пересинка. Оба sync-скрипта
  (Kufar/Realt) добавили `has_terrace, terrace_area` в
  reviewed-защиту строки — тот же класс бага, что чинили с `floor`
  (без этого месячный синк тихо стирал бы ручную пометку террасы на
  уже проверенной строке). Живых объявлений с проставленной террасой
  на момент правки ещё нет — проверено сквозным Playwright-тестом с
  мок-данными на реальном примере владельца (те же 64.5/25.4/39.1 м²
  и 20 → 32.99 $/м²), не живыми данными.

- **2026-08-23 — Рынок недвижимости: этаж как доп. критерий поиска
  дублей.** Владелец на живом примере из карточки группы (ул. Алфёрова
  14, 78.7 м², 16 "похожих" объявлений) указал: это минимум 3 разных
  помещения, не одно — попросил сравнивать ещё и по этажу для более
  жёсткой фильтрации. У обоих источников этаж реально есть в исходных
  данных (Kufar — `ad_parameters` код `floor`, Realt — поле `storey`
  прямо на объекте), просто раньше не тянул. Добавлена колонка
  `floor` (Kufar покрытие ~60% объявлений, Realt ~99%), включена в
  `dedupKey`: если этаж известен у обеих сторон группы и различается —
  это не дубль, группа разводится; неизвестен — не повод разделять,
  просто менее надёжный сигнал (не строгий фильтр, чтобы не терять
  реальные пары там, где у одной стороны просто нет данных об этаже).
  Пересчитал "уверенные" дубли (адрес+площадь+цена между источниками,
  см. запись про удаление дублей выше) с учётом этажа — было 12
  групп/32 записи, стало 11/26: ул. Савицкого 24 (7 записей) отсеялась
  благодаря разным этажам — конкретное подтверждение, что фильтр
  реально работает.
  Отдельно проверил САМ пример владельца (Алфёрова 14, 78.7 м²) — там
  у всех 16 записей этаж 1 (все на одном этаже), поэтому этаж эту
  конкретную группу не разбивает. Сказал владельцу прямо: тут либо
  правда много одинаковых по площади кабинетов на одном этаже одного
  бизнес-центра (та же модель, что у самого Red One), либо один лот,
  перевыложенный агентством под разными категориями (Офисы/Сфера
  услуг/Магазины/Общепит) с разными ценами — без ручного открытия
  нескольких ссылок не разобрать, для этого и есть кнопка "Это разные
  помещения"/удаление в карточке группы.
  По пути поймал и исправил забавную побочность: бэкафилл этажа для
  существующих 833 строк был через `INSERT ... ON CONFLICT (source,
  ad_id) DO UPDATE` — но для уже удалённых ранее 32 дублей `ad_id` в
  таблице не было, конфликта не случилось, и они просто вставились
  заново (801 → 833). Пересчитал дубли на свежих данных (теперь уже
  с этажом) и удалил снова — на этот раз 833 → 807.

- **2026-08-23 — Рынок недвижимости: сгруппированный вид дублей для
  ручной сверки ассистентом.** Владелец: раз дублей много, ассистент
  должна видеть, что дублирует что — открывать, смотреть вручную и либо
  удалять дубль, либо подтверждать, что это два разных помещения (не
  всё можно доверить автоматике — см. запись ниже про 21 "дубль" на
  Алфёрова 7, которые оказались одинаковыми кабинетами одного бизнес-
  центра, не дублями). Режим "Только дубли" на `/admin/market-offers`
  теперь не плоская таблица, а карточки-группы: заголовок с адресом,
  площадью и числом похожих объявлений + мини-таблица самих объявлений
  внутри (источник/тип/сделка/цена/ссылка/отделка) — Kufar- и
  Realt-варианты одного и того же помещения видно рядом, сравнивать
  одним взглядом, не листая всю таблицу в поиске пары. У каждой группы
  кнопка "Это разные помещения" — сохраняет подтверждение в новой
  таблице `market_offer_dedup_dismissals` (ключ — тот же `dedupKey`,
  что группирует карточки), группа больше не считается дублем ни в
  счётчике в шапке, ни в списке (проверено — кнопка есть, счётчик
  дублей уменьшается ровно на размер отклонённой группы). Остальные
  фильтры (отделка/сделка/источник) в режиме дублей намеренно не
  действуют — иначе легко спрятать половину пары и потерять сравнение.
  Рендер строки объявления вынесен в переиспользуемый `OfferRow` —
  общий и для обычной таблицы, и для карточек групп.

- **2026-08-23 — Рынок недвижимости: докрутил категории Realt +
  удалил однозначные дубли из базы.** Владелец скинул ответ Claude из
  другой сессии/репозитория (там свой Python-скрипт `market_realt.py`,
  не имеет отношения к этому проекту — не трогал) с полным списком
  категорий Realt по sitemap.xml. Из него забрал две недостающие:
  `storages` (несмотря на название — это НЕ склады, а общая категория
  "Помещения" без уточнения назначения, ровно аналог "Прочая
  коммерческая" у Kufar, которого не хватало) и `restorant-cafe`
  (своя категория "Общепит", у Kufar отдельно не выделяется — тематически
  стыкуется с блоком "Общепит" на самой странице гида). Итого у Realt
  теперь 7 категорий, 484 предложения (было 448).
  Владелец отдельно попросил: для однозначных дублей — удалять сразу,
  сложные случаи — на потом. Первая попытка (совпадение адреса+площади
  между источниками) чуть не привела к ошибке: нашлось 295 "дублей" в
  85 группах, но при ручной проверке несколько групп оказались по
  10-21 записи с ОДИНАКОВОЙ площадью и РАЗНОЙ ценой (пример — ул.
  Алфёрова 7: 21 запись, 44.2 м², цены от $17 до $56) — это не дубли,
  а однотипные кабинеты в одном бизнес-центре (у Red One та же модель
  — много одинаковых по площади юнитов). Совпадение только площади
  оказалось слишком слабым сигналом. Ужесточил критерий — совпадение
  И площади, И цены за м² (допуск 3% — Kufar/Realt округляют по-своему)
  между РАЗНЫМИ источниками. Это дало 12 групп / 32 записи — гораздо
  правдоподобнее (например, один и тот же лот на Kufar и продублирован
  Realt-агентством сразу под тремя категориями "Магазины"/"Сфера услуг"/
  "Помещения" с одинаковой ценой — известная практика перевыкладки для
  охвата). Удалено из `market_offers` напрямую через Management API
  (833 → 801 строка); при выборе, какую запись из группы оставить —
  приоритет уже проверенным (`reviewed=true`), затем Kufar (его
  `property_type` берётся из структурных полей самого объявления, у
  Realt категория — это URL, под которым мы его нашли, что при кросс-
  постинге под разными категориями менее однозначно). Полный список
  удалённого — в выводе сессии, не дублирую сюда. Более широкий набор
  "возможных дублей" (только по адресу+площади, без цены — тот же
  критерий, что уже показывает бейдж "дубль" на /admin/market-offers)
  оставлен нетронутым — это и есть "сложные случаи", которые владелец
  попросил отложить на отдельный разговор про верификацию.

- **2026-08-23 — Рынок недвижимости: Realt как второй источник + поиск
  дублей.** Владелец: начать Realt тем же принципом, что Kufar, плюс
  явно попросил замечать дубли (один и тот же объект часто выставлен
  сразу на обеих площадках) и уметь их удалять.
  `scripts/sync-realt-market-offers.mjs` — та же архитектура, что у
  Kufar (сырые объявления в `market_offers`, `reviewed=true` защищает
  строку при повторном синке), но детали другие: у Realt свой точный
  фильтр по микрорайону в самом URL (`minsk-mir-mk-r-n`) — проверено на
  реальных адресах, чужих улиц не примешивает (в отличие от Kufar, где
  пришлось фильтровать самим по списку улиц) — свой список тут не
  понадобился. Категории (офисы/магазины/сфера услуг/склады/
  производство) берём из URL, а не из числовых кодов Realt (там нет
  текстовых меток) — прямого аналога "Прочая коммерческая" у Realt не
  нашлось, опущено. **Важная находка, которая могла тихо испортить все
  цифры**: сырая цена объявления Realt — в валюте, которую указал
  конкретный продавец (BYN/USD/EUR — поле `priceCurrency`, ISO 4217),
  НЕ всегда в USD, как я предполагал по одному первому проверенному
  примеру раньше в сессии. Проверил на реальных данных — часть
  объявлений в выборке была в EUR. Решение: цену за м² берём не из
  сырого `pricePerM2`, а из `priceRatesPerM2['840']` — Realt сам
  конвертирует и отдаёт цену в каждой валюте под её ISO-кодом, `840` —
  гарантированно USD. Отделка — поле `repairState` (шкала 0-8: 0
  евроремонт .. 8 без отделки), 0-3 → "с отделкой", 4-8 → "без
  отделки" — то же осознанное огрубление, что у Kufar, поправимо
  вручную. Первый прогон: 448 предложений Realt (для сравнения — 349 у
  Kufar), сложены в ту же таблицу — второй источник в блоке на сайте
  заработал автоматически (там уже подавно ничего не хардкожено про
  Kufar, просто больше строк с другим `source`).
  Дубли: `dedupKey` (`data/marketOffers.ts`) — грубый ключ (тип сделки
  + "фамилия" улицы перед "ул" + номер дома + округлённая площадь).
  Точное сравнение строк адреса бесполезно — Kufar пишет "Михаила
  Савицкого ул, 24Н, Минск", Realt — "Минск Савицкого ул. 24" (другой
  порядок слов, Realt отбрасывает имя/титул, другая пунктуация).
  Специально НЕ удаляет автоматически — только группирует и подсвечивает
  бейджем "дубль" в `/admin/market-offers`, добавлен фильтр "Только
  дубли" (сортирует так, что пара/тройка объявлений идёт подряд —
  сравнивать глазами удобно), решение и нажатие корзины остаются за
  владельцем. На реальных данных нашлось 558 объявлений в 163
  вероятных группах дублей — среди них видны как честные кросс-
  платформенные совпадения (одинаковая площадь до сотых на Kufar и
  Realt, но РАЗНАЯ цена — стоит проверить, какая правильная), так и,
  возможно, повторные выкладки одним продавцом на одной площадке.
  По пути поймал баг в самом `dedupKey`: JavaScript `\b` (граница
  слова в regex) не работает с кириллицей — `\w`, от которого `\b`
  считается, matches только `[A-Za-z0-9_]`, поэтому после кириллической
  буквы `\b` тихо не совпадал НИКОГДА, и функция всегда возвращала
  `null` (обнаружено при живой проверке в браузере — счётчик "дублей"
  в шапке был 0 при том, что ручной прогон той же логики на Python
  находил 163 группы). Заменил `\b` на негативный lookahead
  `(?![а-я])` — юникод-безопасная альтернатива.

- **2026-08-23 — Рынок недвижимости: раздельный статус обработки +
  полноценное редактирование строки.** Владелец после первой версии
  страницы верификации: (1) нужны отдельные статусы "Не обработано"/
  "Проверено" — не путать со статусом отделки (можно пометить строку
  разобранной, даже не меняя в ней отделку — например, свериться по
  ссылке и убедиться, что "не указано" и есть правда); (2) нужен понятный
  способ редактировать не только отделку, а вообще любое поле — цену,
  тип помещения, площадь.
  `finish_status_verified` переименован в `reviewed` (ALTER TABLE RENAME
  COLUMN) — стал независимым, явно переключаемым статусом со своим
  тумблером-фильтром в интерфейсе (по умолчанию открыт "Не обработано" —
  это и есть очередь разбора). Кнопка-карандаш на строке открывает
  модалку редактирования (тип помещения — `Select`, сделка — `ToggleGroup`,
  площадь/цена — `Input`, отделка — тот же пикер, что в таблице, адрес —
  `Input`) — тот же набор компонентов, что и везде в админке, ничего
  нового не изобретал. Сохранение помечает `reviewed=true`.
  Важное следствие для скрипта синка: раньше `reviewed`(тогда ещё
  `finish_status_verified`) защищал от перезаписи только поле отделки,
  остальные поля (цена/тип/площадь) при каждом месячном синке всё равно
  подтягивались заново с Kufar. Теперь, когда через модалку можно
  поправить и их, это стало бы багом — правка терялась бы при следующем
  синке. Исправлено: `reviewed=true` теперь защищает всю строку целиком,
  синк для таких строк только подтверждает, что объявление ещё живо
  (обновляет `ad_link`/`updated_at`), остальные поля не трогает.
  Заодно `MARKET_PROPERTY_TYPES` (список типов помещений) вынесен в
  `data/marketOffers.ts` — раньше дублировался в `DistrictGuidePage.tsx`,
  теперь общий источник для таблицы на сайте и формы редактирования.

- **2026-08-23 — Рынок недвижимости: переход на сырые объявления +
  ручная верификация владельцем.** После разводки цены по отделке
  выяснилось (см. запись ниже) — у подавляющего большинства объявлений
  Kufar статус отделки Kufar сам не проставляет ("не указано" — 252 из
  349). Владелец предложил проставить вручную и заодно сверить базу —
  попросил удобный интерфейс, не готов лезть в SQL/Supabase-консоль для
  этого.
  Архитектурный сдвиг: вместо готового месячного агрегата в таблице
  `market_offers_stats` (удалена) теперь храним сырые объявления в новой
  `market_offers` — таблица на гиде района считает медиану прямо из этих
  строк на лету (`buildMarketPivot` в `DistrictGuidePage.tsx`), поэтому
  правки владельца сразу видны на публичной странице, без пересинка.
  Новая страница `/admin/market-offers` (`MarketOffersReview.tsx`, пункт
  "Рынок недвижимости" в группе "Маркетинг" сайдбара): список объявлений
  с адресом (ссылка на само объявление на Kufar — можно проверить
  вручную одним кликом), фильтры по сделке/отделке (по умолчанию — "Не
  указано", это и есть очередь на разбор), поиск по адресу/типу. Клик по
  статусу отделки сохраняет сразу без отдельной кнопки "Сохранить" (тот
  же принцип мгновенного сохранения, что у тегов лида) и помечает
  `finish_status_verified=true`; кнопка удаления — для явно битых строк
  (сверка базы, не только отделка). Обработанная строка сразу пропадает
  из фильтра "Не указано" — видно, что очередь уменьшается.
  Скрипт синка (`sync-kufar-market-offers.mjs`) переписан писать сырые
  объявления вместо агрегата; при повторном месячном прогоне
  цена/площадь/ссылка обновляются как обычно, а вручную подтверждённый
  статус отделки не перезатирается — сверяется по `finish_status_
  verified` перед upsert. 349 текущих объявлений перенесены в новую
  таблицу без потерь (обычный INSERT, не пересбор с нуля).

- **2026-08-23 — Таблица рынка: развели цену с отделкой и без вместо
  общей цифры.** Владелец сразу после предыдущей правки: "общая цена на
  помещения с отделкой и без — это неверно". И тут же поймал конкретное
  следствие на скриншоте — маленький офис (<40 м²) в аренде выходил
  дешевле за м², чем средний (20 → 25 → 50 $/м²/мес), что для рынка
  недвижимости нелогично (обычно наоборот — чем меньше площадь, тем
  дороже метр). Проверил гипотезу на сырых данных: это не баг подсчёта,
  а артефакт смешивания отделки — среди маленьких офисов в выборке
  почти нет "с отделкой" (доминируют "без отделки"/"не указано",
  которые дешевле), а у средних выше доля дорогих "с отделкой". Когда
  смотришь только "с отделкой" в изоляции — цена растёт с площадью
  монотонно (15 → 38 → 39), как и должна. Данные для разводки по
  отделке уже были честно посчитаны в скрипте синка (прошлая правка) —
  не хватало только развести их на уровне интерфейса. Добавлен второй
  `ToggleGroup` "Отделка" (С отделкой / Без отделки / Не указано) рядом
  с тумблером сделки — таблица больше никогда не показывает общую
  сводную цифру по всем статусам сразу, только выбранный статус.
  По умолчанию — "С отделкой" (самый релевантный срез для питча Red One).

- **2026-08-23 — Блок "Рынок коммерческой недвижимости" на гиде района
  + правка методики подсчёта после жалобы владельца на нереальные
  цифры.** Сначала собрал сам блок (`src/data/marketOffers.ts`,
  `src/lib/marketOffersApi.ts`, новая секция в `DistrictGuidePage.tsx`
  сразу после "Плотности бизнеса") — первый блок на этой странице,
  тянущий данные не из статики, а живьём из Supabase (`useEffect` +
  `fetchLatestMarketOfferStats`, читает только последний собранный
  месяц). Тумблер Продажа/Аренда (`ToggleGroup`), таблица тип помещения
  × площадь (кол-во + цена), плашка-инсайт про дефицит маленьких офисов
  с отделкой → ссылка на `/one`. Владелец сразу заметил: "неадекватные
  ставки". Причина — единичные битые объявления (цена "по запросу" у
  Kufar иногда сохраняется как 0/$0.01/$0.67 за м², плюс один явный
  выброс $29 583/м² — 72 м² на ул. Игоря Лученка 4, при рынке района
  $1-13 тыс/м²) при маленьком N в ячейке (1-3 предложения) полностью
  ломали среднее. Владелец явно сказал не хочет вручную проверять
  десятки объявлений, но хочет способ свериться с методикой. Решение —
  `PRICE_BOUNDS` в sync-скрипте (продажа 300-15000 $/м², аренда
  5-150 $/м²/мес — широкие, отсекают только математически невозможное,
  не пытаются угадывать "подозрительно круглые" цены типа повторяющихся
  $990/$1035 в одном доме на Савицкого, 39 — это остаётся на усмотрение
  владельца, если возникнут вопросы) + список отфильтрованных объявлений
  с id и ссылкой печатается в консоли скрипта — лёгкая точечная сверка
  вместо разбора всех объявлений. Заодно поправлена сама механика
  агрегации: раньше таблица на сайте пересчитывала СРЕДНЕЕ по уже
  готовым под-группам "с отделкой"/"без отделки"/"не указано" — так
  нельзя корректно получить медиану постфактum. Теперь скрипт сразу
  считает отдельную сводную группу `finish_status='все'` напрямую из
  сырых (уже отфильтрованных) цен, и таблица показывает медиану, а не
  среднее — устойчивее к любому оставшемуся одиночному отклонению.
  Данные в `market_offers_stats` за август 2026 пересчитаны и
  перезаписаны (не накоплены поверх старых — удалены и вставлены заново
  тем же месяцем). Realt (второй источник) — ещё впереди, тем же
  принципом фильтрации.

- **2026-08-23 — Новая фича (не по этому плану, отдельная просьба
  владельца): сбор рыночной статистики по коммерческой недвижимости
  Минск Мира с Kufar — первый источник, Realt следующим заходом.**
  Владелец хочет сводную таблицу (кол-во предложений, средняя/медианная
  цена за м², в разрезе тип сделки/тип помещения/площадь/отделка) —
  чтобы, среди прочего, подсветить дефицит маленьких помещений с
  отделкой и предложить в ответ Red One. Раз в месяц, не чаще.
  Технически: у Kufar недвижимость — отдельный поддомен `re.kufar.by`
  (не `www.kufar.by` — тот отдаёт 404 для коммерческой недвижимости),
  ещё и свои `cre-api*`/`content.kufar.by` под статику — потребовалось
  несколько заходов добавить домены (та же "поддомен ≠ домен" ловушка,
  что раньше с `api.supabase.com`). Сама страница — SPA (Next.js),
  обычным браузерам отдаёт пустой стейт с флагом `isSearchBot` — но с
  `User-Agent: Googlebot` делает полноценный SSR для тех же
  `props.initialState.listing.ads` (легитимный SEO-путь для краулеров,
  не обход защиты). Гео: у district-фильтра Kufar (`coder_district-28`
  / slug `minsk-oktyabrskij-rajon`) — это весь Октябрьский район Минска,
  Минск Мир лишь часть; свой `address=`-параметр в URL, который дал
  владелец, на SSR-выдачу не влияет (проверено — с ним и без него
  одинаковый `total`). Поэтому фильтруем сами по списку улиц Минск Мира
  (`MINSK_MIR_MARKERS` в скрипте — те же улицы, что уже встречаются в
  блоке застройщика/медицины на этой странице; сверено вдобавок по
  координатам объявлений: улицы Минск Мира почти все ≤1.4 км от центра
  комплекса, остальные — от 2 км). Тип и отделка — НЕ угадываются:
  Kufar сам отдаёт структурированные `ad_parameters` (`property_type`,
  `commercial_repair`, список удобств с тегом "С отделкой") — то же
  самое, что раньше подтвердилось у Realt через `repairState`.
  `scripts/sync-kufar-market-offers.mjs` (`--dry-run`/`--json` для
  отладки без записи), таблица `public.market_offers_stats` (агрегат
  по месяцам, не сырые объявления), workflow
  `sync-market-offers-stats.yml` (1-го числа). Первый реальный прогон
  (август 2026): 134 предложения продажи + 226 аренды по Минск Миру.
  Уже видно то, что искал владелец: маленьких офисов (<40 м²) с
  отделкой — по 1 предложению что на продажу, что в аренду на весь
  район. Локальный запуск скрипта в песочнице потребовал
  `NODE_USE_ENV_PROXY=1` (встроенный `fetch` в Node не читает
  `HTTPS_PROXY` без этого флага) — в GitHub Actions прокси нет, там не
  нужен. Realt (второй источник) и публичный блок на странице — следующим
  шагом, ещё не сделаны.

- **2026-08-23 — Гид: новый блок "Застройщик района" (Dana Holdings),
  текст через Gemini.** Владелец заметил в запросах много "застройщик
  минск мир" — попросил блок под этот интент, сразу перед блоком ЦА.
  Знал только название компании, фактуру (историю, другие проекты) не
  дал — попросил найти самостоятельно. Собрал по открытым источникам
  (t-s.by, naviny.by, Wikipedia): Dana Holdings — международная группа
  со штаб-квартирой в Швейцарии, основатели — сербские предприниматели
  Богoлюб и Драгомир Каричи; в Минске с 2006 года, другие проекты —
  «Маяк Минска», Vogue, «Вивальди», «Рахманинов»; территорию бывшего
  аэропорта Минск-1 юрлицо ИООО «Дана Астра» получило указом Президента
  в 2014-м, заявленный масштаб на старте — 320 га / ~30 тыс. квартир /
  ~$3,5 млрд инвестиций. Текст абзаца пропустил через Gemini на
  редактуру (по прямой просьбе владельца — "для текста обратить за
  помощью к Gemini, если надо"); в черновике Gemini поймал переоценку
  — "среди реализованных проектов" (подразумевает завершённые), хотя
  источники подтверждают только начало строительства — поправил на
  "среди других проектов" перед тем, как применить текст.
  Контакты — как дал владелец: телефон 7675 (кликабельный `tel:`),
  адрес отдела продаж (ул. П. Мстиславца, 9, «Дана Центр», 1 этаж),
  режим работы. Ссылки на официальные сайты minskworld.by и bir.by —
  пилюли с иконкой `ExternalLink`, `target="_blank"`.
  Логотип — прислал ibb.co-ссылкой на JPEG с белым фоном ("только фон
  вырежи"). В песочнице нет ни PIL изначально, ни ImageMagick/rembg —
  поставил `pip install pillow` и написал вырезание фона вручную:
  классический трюк `alpha = 255 − min(R,G,B)` + деумножение цвета
  (чтобы края не выглядели вымытыми), затем автокроп по `getbbox()`.
  Проверил на тёмной и светлой подложке перед тем, как сохранить в
  `public/images/district/dana-holdings-logo.png` — тот же паттерн
  self-hosting, что уже был у логотипа метро и у 6 фото хиро-слайдера
  (внешние ibb.co-ссылки не годятся в проде: не тот домен в CSP/хешах
  сборки не будет, к тому же нет контроля над временем жизни хостинга).

- **2026-08-23 — Блок банков: иконки на бейджах "Отделение"/"Банкомат".**
  Донастройка блока банков сразу после его создания (см. запись ниже
  про разделение отделений/банкоматов) — владелец попросил добавить
  иконки к текстовым бейджам, явно уточнив, что подпись должна
  остаться видимой текстом (не спрятана за одну иконку), чтобы по
  запросу вида "минск мир приорбанк банкомат" поиск видел совпадение
  прямо в тексте страницы. `Landmark` — для "Отделение", `CreditCard`
  — для "Банкомат", оба рядом с текстом внутри того же бейджа.

- **2026-08-23 — Блок банков: разделил отделения и банкоматы, нашёл
  пропущенный БНБ-Банк.** Владелец заметил на скриншоте: непонятно, где
  в списке реальное отделение банка, а где просто банкомат — и сразу
  же: "точно есть БНБ-Банк, добавь". Первый заход (единый поиск
  "банкоматы") не разделял типы — категория у большинства точек
  оказалась пустой в самой выдаче Яндекс.Карт, не только у меня в
  парсинге. Решение — не гадать по формату адреса ("этаж 1" vs "отдел
  №" ненадёжный сигнал), а попросить владельца пересохранить ДВУМЯ
  раздельными запросами: "банкоматы" отдельно, "отделения банков"
  отдельно — тогда категория "Банк" у второго подтверждена достоверно.
  Результат: 22+11 строк → 30 уникальных точек, 13 банков (было 18/11).
  БНБ-Банк нашёлся сразу — просто числится в выдаче под полным
  юрлицом "Белорусский народный банк", не аббревиатурой; у него и
  отделение, и банкомат, оба в Avia Mall. Заодно всплыли ещё 2
  пропущенных банка в первом заходе (Сбер Банк отдельно от БНБ).
  "Три цены" (сетевой магазин, не банк) попал в выдачу по отделениям
  шумом — исключён.
  Формат — вместо ранжированного списка с номерами (прошлый дизайн)
  компактная сетка в 2 колонки: название банка + бейджи "Отделение"/
  "Банкомат" у каждого. Владелец отдельно просил уменьшить высоту
  блока — сетка в 2 колонки вместо одной вертикальной решает это же.
  9 из 13 банков — с полноценным отделением, у всех 13 — банкомат.

- **2026-08-23 — Гид: реструктуризация страницы — тепловая карта наверх,
  10 блоков сфер бизнеса объединены, чёрные графики вместо красных.**
  Три отдельные правки от владельца после просмотра готовой страницы:
  1. **"Плотность бизнеса по нишам" перенесена** сразу после "Генераторы
     ежедневного трафика" (была в самом низу, перед FAQ) — логичнее
     видеть сводную картину раньше, чем детали по каждой категории.
  2. **10 блоков конкретных сфер (Медицина, Общепит, Спорт, Банки, СТО,
     Салоны красоты, Продукты, ПВЗ, Цветы, Табак/вейп) объединены в одну
     карточку** с общим заголовком-эйброу "Аналитика по сферам бизнеса"
     — раньше это были 10 независимых карточек подряд, каждая с
     собственной рамкой/тенью, из-за чего терялось, что это части
     одного целого. Технически: убрал у каждого подраздела
     `glassCardClass`/`glassCardShadow`, разделил через `divide-y`
     внутри одной внешней карточки; заголовки подразделов H2→H3 (эйброу
     стал H2) — заодно более корректная вложенность для SEO, не только
     косметика. Внутреннюю вёрстку каждого подраздела (5 разных
     дизайнов из прошлого раунда — нарратив/ранжированный
     список/контраст/герой-цифра/бары) не трогал, только внешнюю
     оболочку — разнообразие, которое владелец раньше одобрил, не
     потерялось.
  3. **Красный цвет в графиках заменён на чёрный** — прогресс-бары в
     блоках "Продукты" и "Табак/вейп" (`bg-primary/70` → `bg-ink/70`),
     владелец: "какое-то небезопасное чувство". Крупные красные
     цифры-герои (Салоны красоты — 88, ПВЗ — 31) оставлены как есть —
     владелец отдельно уточнил, что они смотрятся хорошо, трогать не
     надо. Полезный урок: "красный цвет" в фидбэке не значило "уберите
     везде" — надо было различать красный-как-акцент (единичная крупная
     цифра, читается как бренд) и красный-как-элемент-графика
     (повторяющийся визуальный паттерн, читается как предупреждение).

- **2026-08-23 — Тепловая карта: финальные цвета, подобраны через
  визуальный пресет-пикер в Artifact.** Владелец не принял ни
  предыдущую схему (сплошной красный), ни статусную триаду проекта —
  попросил пастельные варианты. Вместо перебора хексов в чате собрал
  сравнительную страницу (`Artifact`, публикуется отдельно от кода
  сайта) — 5 пресетов на реальном макете плитки. Владелец выбрал
  структуру D (зелёный/жёлтый/терракотовый, не монохром) как понравившуюся
  ещё на первом показе. По пути было одно недопонимание — принял его
  фразу "три оттенка красного" за просьбу сделать монохромную шкалу
  (вариант E) вместо того, что он на самом деле просил: вариации
  яркости/тусклости для ТЕХ ЖЕ трёх цветов D. Поправил после уточнения
  — показал D-тусклее/D-ярче через HSL-манипуляцию (та же насыщенность
  ± тем же трём базовым тонам), владелец выбрал "ярче". Финальные
  значения — не токены проекта: готовая success/warning/danger триада
  не подходила по яркости, отдельного "яркого" пресета в дизайн-системе
  нет, значения посчитаны (S+25, L−8 от пастельного черновика) и
  зафиксированы в коде.
  **Отдельно про инструмент**: впервые в этой сессии для выбора цвета
  использован Artifact как визуальный пикер, а не хекс-коды текстом в
  чате — сработало быстрее (владелец сразу видел реальные плитки, а не
  представлял цвет по названию/коду) и снизило число раундов
  уточнений. Держать в голове на будущее для любых визуальных
  UI-решений, где вариантов больше двух.
  Также поймал на этом же деплое ложный "ещё не задеплоилось" — polling
  curl-ом искал буквальный hex (`#92e7aa`) в HTML, а браузер сериализует
  инлайн-стили в `rgb()` — проверка была обречена никогда не совпасть.
  Поймал вручную (`grep rgb(146, 231, 170)`), остановил зависший монитор.
  На будущее: при проверке инлайн-цветов на проде искать `rgb(...)`,
  не hex.

- **2026-08-23 — Тепловая карта: замена цветовой схемы + вынос сноски
  (правки владельца после ревью).** Два независимых замечания сразу
  после публикации. (1) Сплошной один оттенок красного (ordinal-рамп
  из прошлой записи) читался как "тревога" на всей сетке, даже там, где
  плотность низкая — справедливо. Заменил на статусную триаду проекта
  (`--color-success/warning/danger` из `index.css` — уже готовые токены
  под ровно этот смысл, ничего изобретать не пришлось): низкая плотность
  зелёная, высокая красная, привычный светофор. Текст на плитках
  пересчитан по контрасту заново (success/warning — тёмный текст
  ~5.4:1, там белый давал только ~3.3:1; danger — белый держит 4.6:1).
  (2) Пояснение "высокая плотность — не только конкуренция, но и
  доказанный спрос" уже было в тексте (шапка блока), но владелец не
  заметил его на фоне яркой сетки — попросил отдельную сноску со своим
  фоном и знаком внимания. Вынес в отдельный блок (`bg-warning-bg` +
  иконка `TriangleAlert`, тот же паттерн, что `Badge` использует для
  warning-статуса в других местах проекта).

- **2026-08-23 — Гид: тепловая карта плотности бизнеса — синтез всех
  собранных цифр в один блок.** Владелец предложил идею (раз уже есть
  число точек по 9 категориям — сделать что-то вроде тепловой карты,
  где выше конкуренция, где ниже), попросил самому предложить методику
  расчёта.
  Методика, согласованная перед вёрсткой: намеренно НЕ называть это
  "конкуренцией" в тексте — количество точек само по себе не хорошо и
  не плохо (может значить и доказанный спрос), формулировка нейтральная
  — "плотность", вывод оставлен читателю. Считаем только категории, где
  точки физически внутри района (СТО не входит — у него другая суть,
  свой блок с контрастом уже есть). Три уровня по абсолютному числу
  точек, не на душу населения — нет достоверной цифры населения района
  (решение ещё из блока аудитории, не стали изобретать заново): высокая
  ≥40, средняя 20–39, низкая <20, границы округлые, не строгие терцили
  (аптеки и табак/вейп оба по 22 — терцили бы их развели, некрасиво).
  Реальные данные разложились логично: 3 ниши высокой плотности (Салоны
  красоты 88, Общепит 73, Продукты 44), 4 средней (Цветы 36, ПВЗ 31,
  Аптеки 22, Табак/вейп 22), 2 низкой (Банки 18, Спорт 10).
  Цветовая шкала — впервые в этой сессии подключил skill `dataviz` для
  выбора цветов вместо "на глаз". Это ordinal-шкала (уровень плотности,
  не идентичность категории) — один оттенок, светлее→темнее. Готовые
  токены проекта (`--color-primary-soft`/`primary`/`primary-hover`) не
  прошли валидатор (`validate_palette.js --ordinal`): светлый край
  слишком светлый против фона (контраст 1.18 при пороге 2:1), шаг
  между primary/hover слишком узкий (ΔL 0.055 при пороге 0.06). Подобрал
  новые hex в том же оттенке красного, гоняя через валидатор, пока не
  прошли все проверки (`#ee8f97` / `#e4152b` / `#8f0d1c`) — это НЕ
  цвета из палитры проекта, а производные того же тона, специально
  подобранные под эти пороги. Текст на плитках — тёмный на светлой
  плитке, белый на средней/тёмной (проверено через `contrast()` из того
  же скрипта — на светлой плитке белый текст даёт только 2.33:1,
  провал). Легенда (3 кружка-подсказки) добавлена отдельной строкой —
  число на каждой плитке и так полностью читаемо без цвета, но по
  гайду skill'а цвет не должен быть единственным носителем смысла.
  Скриншот сначала визуально ПОКАЗАЛ баг (плитка "22 Табак/вейп"
  выглядела светлее плитки "22 Аптеки" с тем же числом) — перепроверил
  через `getComputedStyle` в браузере напрямую, оказалось оптическая
  иллюзия соседних цветов (одновременный контраст), не реальная ошибка
  — оба фона реально `rgb(228, 21, 43)`. Хороший урок: не доверять
  глазу на скриншоте там, где можно проверить фактическое значение.
  Технический побочный эффект: "22 аптеки" в блоке медицины было
  магическим числом в строке текста — вынес в константу `pharmacyTotal`,
  чтобы не задваивать в новом блоке.
  Отдельно: ветка успела уйти на 15 коммитов вперёд (параллельная
  сессия добавляла расшифровку встреч/новый раздел меню) — `git pull`
  перед коммитом, слилось чисто, без пересечений по файлам.

- **2026-08-23 — Гид: мелкая полировка хиро после ревью скриншота.**
  Владелец прислал скриншот готового хиро, две правки: (1) убрать
  координаты района из текстового блока, на их место — "Обновлено:
  август 2026" (раньше эта строка дублировалась сверху блока, теперь
  только одна, снизу); (2) больше отступа между хиро и плитками
  статистики — блоки визуально слипались. `DISTRICT_COORDS` как
  константа больше не используется нигде на странице — удалена.

- **2026-08-23 — Гид: вертикальный слайдер + двухколоночный хиро-блок
  (исправление предыдущего захода).** Владелец сразу указал на реальную
  проблему: фото портретной ориентации, а слайдер выше сверстан
  альбомным — `object-cover` резал кадр уродливо, "не пойдёт". Плюс
  прямая просьба — раз слайдер теперь у́же (вертикальный), не оставлять
  пустое место, а занять его вторым столбцом с текстом: новый H1
  ("Гайд и аналитика по офисам и коммерческим помещениям в районе
  Минск Мир") и описание под ключевые слова.
  Технически: `HeroImageSlider` (общий компонент, использует и `/one`)
  получил опциональный `aspectClassName` (по умолчанию `aspect-video`,
  как было) — гид передаёт `aspect-[4/5]` под реальные пропорции фото
  (512×640 — ровно 4:5). `/one` не тронут (проп не передаётся — старое
  поведение). Хиро переверстан в `grid sm:grid-cols-2`: текст слева,
  вертикальный слайдер справа, на мобильном стекается в одну колонку.
  Разделил H1 и мета-`<title>` на две константы (`PAGE_H1` vs `TITLE`)
  — новый H1 длиннее и точнее по позиционированию ("гайд и аналитика"
  отражает, что страница обросла блоками с реальными цифрами по каждой
  категории), но в `<title>`/og/canonical его тянуть не стали —
  обрежется в выдаче, а старый `TITLE` уже подобран под целевые запросы
  и трогать не было причины. Новый интро-абзац (`INTRO_TEXT`) — с
  ключевыми словами (коммерческая недвижимость, офис, кабинет, торговое
  помещение, аренда) вместо старого упоминания соседства с Red One
  (оно и так есть в CTA-карточке внизу страницы, не потерялось).
  Проверено скриншотами (десктоп 1280px + мобилка 390px) до пуша.

- **2026-08-23 — Гид: слайдер аэрофото вместо пустого HERO_IMAGE_URL, +
  реальный баг найден и исправлен в `HeroImageSlider.tsx`.** Владелец
  прислал 6 реальных аэрофото района (ссылки на ibb.co, автор
  @PROMIR_BY) — сначала спросил про Google Диск как способ загрузки
  большого числа фото разом, объяснил, что публичные Drive-ссылки не
  отдаются как `<img src>` напрямую (ведут на HTML-страницу
  предпросмотра), предложил присылать файлы прямо в чат или готовые
  прямые ссылки. Владелец дал прямые ссылки на ibb.co.
  **Важно — у всех 6 фото водяной знак «@PROMIR_BY».** Прежде чем
  публиковать, остановился и спросил владельца, чьи это фото и есть ли
  права — не стал считать "прислал в чате" достаточным основанием
  (тот же принцип, что и с любыми непроверенными фактами в этом
  проекте, просто применённый к авторским правам, а не к данным).
  Владелец подтвердил: фото публичные, с сохранённым водяным знаком
  публиковать можно.
  Скачал и самостоятельно захостил в `public/images/district/` (не
  зависеть от ibb.co для LCP-критичной картинки — тот же принцип, что
  и с логотипом метро ранее). Переиспользован готовый `HeroImageSlider`
  (компонент уже используется на `/one` для слайдера рендеров) — не
  писал новый.
  **Попутно нашёл реальный баг**: у присланных фото портретная
  ориентация (512×640), а компонент рассчитан на альбомные — контейнер
  с `aspect-video` разрастался под естественные пропорции картинки
  (880px вместо ожидаемых 396px при ширине 704px), полностью игнорируя
  заданное соотношение сторон. Причина — классическая для flex-детей с
  `aspect-ratio`: `min-height: auto` по умолчанию у flex-элемента
  позволяет содержимому (тут — `<img class="h-full">` с сильным
  собственным intrinsic-размером) продавить размер контейнера в обход
  aspect-ratio. Фикс — `min-h-0` на корневой div компонента. Правка
  общая для компонента (затрагивает и `/one`), но по построению строго
  не может ухудшить уже корректный случай (альбомные фото) — только
  чинит некорректный (портретные). Живьём на `/one` перепроверить не
  вышло (headless Chromium в этой песочнице не может выполнить живые
  запросы к Supabase из-за той же инфраструктурной проблемы прокси, что
  и с Яндекс.Картами/2ГИС выше) — прод после деплоя проверен curl-ом
  (200, верный `<title>`), полноценная визуальная проверка — за
  владельцем при следующем открытии `/one`.

- **2026-08-23 — Гид: визуальный редизайн 8 блоков-сводок, 5 разных
  форматов вместо однотипных пилюль.** Владелец заметил, что блоки
  категорий (спорт/банки/СТО/красота/продукты/ПВЗ/цветы/табак) выглядят
  одинаково (иконка + заголовок + абзац + строка пилюль подряд) — попросил
  один оставить эталоном, остальные переосмыслить, не больше 5 разных
  решений (не плодить 8 уникальных дизайнов). Логика распределения — по
  форме данных, не произвольно:
  - Общепит — **без изменений**, эталон (первый паттерн, уже утверждён
    ранее).
  - Нарратив без пилюль, цифры вплетены в текст (Спорт, Цветы) — подходит
    простым датасетам из 1-2 подкатегорий, даёт больше связного текста
    (сам повод для правки — "кажется, что надо больше текста, чтобы и
    SEO работало").
  - Ранжированный список с номерами 1-11 (Банки) — подходит длинному
    плоскому списку одноуровневых сущностей.
  - Контрастная разбивка на 2 колонки "в районе / по соседству" (СТО) —
    единственный блок, где сама суть данных (пусто здесь vs густо рядом)
    — готовый визуальный контраст.
  - Крупная цифра-герой + короткая подпись (Салоны красоты, ПВЗ) —
    подходит, когда один общий счётчик важнее разбивки.
  - Горизонтальные прогресс-бары, ширина пропорциональна значению
    (Продукты, Табак/вейп) — подходит, когда важно сравнение долей
    (максимум считается динамически через `Math.max`, не хардкодится).
  Технически всё внутри уже существующих карточек (`glassCardClass`),
  меняется только внутренняя вёрстка — надёжно, не трогает общую
  структуру страницы/JSON-LD/мету. Проверено скриншотами (десктоп 1280px
  full-page + мобилка 390px) — ни один формат не ломается на узком
  экране (split-грид у СТО схлопывается в одну колонку, бары не
  переполняются из-за длинных подписей — подписи вынесены строкой над
  баром, не сбоку).

- **2026-08-23 — Гид: 8 новых блоков-сводок по категориям арендаторов
  (спорт, банки, СТО, бьюти, продукты, ПВЗ, цветы, табак/вейп) —
  webarchive-метод поставлен на поток.** Продолжение записи ниже про
  первый успешный опыт с аптеками/общепитом — владелец подтвердил, что
  метод рабочий, и стал присылать webarchive пачками (по 1-3 файла за
  раз) под каждую следующую категорию, попутно уточняя формат подачи:
  - **Спорт и фитнес** — 11 строк → 10 уникальных мест.
  - **Банки/банкоматы** — 19 строк → 18 уникальных точек, 11 разных
    банков (варианты написания вроде "Альфабанк, банкомат" объединены
    с "Альфа-Банк" вручную).
  - **СТО и автосервисы** — владелец сразу дал готовую гипотезу для
    текста ("в самом Минск Мире их нет, но по соседству крупная зона"),
    попросил не проверять, а писать как есть. Данные (67 строк → 64
    уникальных точки) её всё равно подтвердили цифрами: 42 из 64 точек
    сконцентрированы на ул. Казинца/Брестской/Бородинской/Брилевском
    тупике — соседний кластер, не сам район. Единственный блок этой
    пачки с другой структурой текста (не "N точек, разбивка по типам",
    а "внутри почти нет, зато рядом много").
  - **Салоны красоты** — 91 строка → 88 уникальных, самый крупный
    сегмент из всех найденных категорий.
  - **Магазины продуктов** — 44 строки, категории в выдаче почти всегда
    пустые (в отличие от кафе/спорта/красоты) — считал по названиям,
    отдельно выделил специализированные мясные/овощные лавки по
    ключевым словам в названии (8 из 44).
  - **ПВЗ** — 46 строк, но владелец попросил учитывать только Ozon и
    Wildberries (в выдачу по общему запросу "пункт выдачи заказов"
    попали посторонние организации с похожей категорией — Emall.by,
    Lamoda, Aza.by и т.п., не относящиеся к запросу) → 31 точка (20
    Wildberries + 11 Ozon).
  - **Цветочные магазины** — 36 строк, все уникальны, просто.
  - **Табак и вейп-шопы** — единственная категория, где владелец сам
    сохранил ДВА отдельных поиска ("табак" и "вейп-шоп") и явно попросил
    смёржить их в одну категорию с удалением дублей, попавших в обе
    выдачи (18 + 15 строк, 7 точек пересеклись → 22 уникальные).
  Для каждой категории — общий счётчик + разбивка по типам/сетям пилюлями
  (тот же паттерн, что уже был у общепита), кроме СТО (текстовый абзац,
  не список) и цветов/ПВЗ (просто число, разбивка не нужна при 1-2
  подкатегориях). Блоки вставлены в порядке присылки владельца, между
  "Медицина и здоровье" и "Готовые решения под ваш тип бизнеса". Каждая
  категория — отдельный коммит с деплоем и проверкой на проде сразу
  после (не копил несколько категорий в один пуш) — держит риск
  минимальным при таком количестве последовательных правок за одну
  сессию.
  Парсер (`parse_cafes.py` в scratchpad, не в репо — разовый инструмент)
  переиспользован без изменений на всех восьми категориях: по
  `search-business-snippet-view__title/__address/__category` классам
  из HTML внутри `.webarchive` (тот же `plistlib`-приём, что и с первой
  категорией, аптеками — см. запись ниже). Дедупликация — по паре
  (название, адрес), нужна почти везде (5-15% дублей от повторного
  скролла при сохранении длинных списков).

- **2026-08-23 — Гид: блок аудитории + конкретика в блоке медицины,
  второй раунд правок владельца после ревью готовой страницы.**
  Владелец увидел опубликованную страницу и счёл её "буквально пара
  предложений" — по факту весь текст уже был на месте (все 7 блоков из
  предыдущего раунда), но плотность ощущалась низкой: пустой блок под
  фото (~400px, ждёт ссылку), короткие абзацы (сам же просил формат
  "лендинг, коротко и конкретно"). Уточнили через `AskUserQuestion` — не
  формат неверный, страница просто ещё не полная, нужно нарастить
  глубину, оставаясь в лендинг-стиле. Владелец дал конкретные пометки по
  3 блокам:
  1. **Целевая аудитория** — раньше одна плитка "25–45 лет", нужен
     полноценный блок: возрастное ядро, "почти нет пенсионеров" (факт от
     владельца, не проверить веб-поиском), акцент на активное
     потребление и семьи с детьми.
  2. **Медицина** — раньше общие слова без цифр, нужна конкретика с
     реальными объектами.
  3. **Транспорт** — метро "читалось непонятно" на фоне бейджей
     автобусов/троллейбусов, нужна иконка метро + названия станций
     бейджами, и иконки для автобусов/троллейбусов "не рандомные, а
     знакомые минчанам, официальные".
  **Транспорт** сделан отдельным быстрым коммитом (не требовал
  Gemini/веб-поиска, чисто вёрстка): владелец сначала прислал ссылку на
  оригинал `Minsk_metro_logo.svg` (Wikimedia Commons), потом ссылку на
  ibb.co с уже готовым бейджем линии (красная "M" + зелёный кружок "3" —
  Зеленолужская линия) — им и воспользовался, точнее и не требует
  досборки. Оба домена (`upload.wikimedia.org`, `i.ibb.co`) сначала были
  не в Allowed domains — впервые в этой сессии картинку от владельца не
  получилось вытащить из чата как файл (тот же лимит, что раньше был с
  фото района — вставка текстом в диалог, а не файлом, не даёт доступа к
  файлу на диске), пришлось просить ссылку. Файл лёг в
  `public/icons/minsk-metro-line3.png`, для автобусов/троллейбусов —
  нейтральные lucide-иконки (`Bus`/`TramFront`), т.к. официальных иконок
  автобусов/троллейбусов Минсктранса под рукой не было, а связываться
  с ещё одним источником ради этого не стали.
  **Аудитория и медицина** прошли полный цикл владельца: расписал бриф
  для Gemini на собранных фактах → владелец утвердил → Gemini вернула
  текст → заверстал. Факты собирал сам (владелец предложил помочь
  вручную, но лучше найти автоматический способ):
  - Веб-поиск дал часть данных: **41-я городская поликлиника**
    (ул. Кижеватова, 5а, открыта в январе 2026 — подтверждено двумя
    независимыми источниками), 2 стоматологии, медцентр "ИдеалМед",
    ветклиника "Главное Хвост" — через `blizko.by` (curl + свой
    regex-парсер под их вёрстку, домен пришлось добавлять в Allowed
    domains) и обычный `WebSearch`.
  - Численность населения района/точную демографию в открытых
    источниках не нашёл (ни у застройщика, ни в Википедии) — решили с
    владельцем не выдумывать цифру, ограничиться качественными
    формулировками.
  - **Попытка автоматизировать поиск по Яндекс.Картам/2ГИС провалилась
    на инфраструктурном уровне, не на уровне блокировки домена** —
    стоит подробно, пригодится на будущее: headless-браузер (Playwright)
    из этой песочницы не проходит TLS-рукопожатие через прокси сессии
    вообще ни для одного внешнего домена (проверил на нескольких, включая
    уже разрешённые) — `ERR_CONNECTION_RESET` до входа в само TLS,
    точечный SPKI-пиннинг сертификата прокси не помог, `apt-get` для
    установки `certutil`/NSS-тулинга недоступен из песочницы. Это
    ограничение самой среды выполнения, не что-то, что чинится добавлением
    домена в allowlist. 2ГИС отдельно закрыт анти-бот JS-челленджем
    (servicepipe.tech) прямо на входе — даже с рабочим браузером не
    пройти без решения капчи. У Яндекс.Карт (curl, без браузера) страница
    загружается, но результаты поиска подгружаются JS через внутренний
    API — путь эндпоинта не нашёл (JS-бандл сильно заминифицирован,
    погонял штук 6 вероятных путей вручную — везде 400 без тела).
    Официальный API Яндекса ("Поиск по организациям", geosearch) — не
    имеет вменяемого бесплатного тарифа для разовой задачи: минимум
    1000 запросов/сутки за **20 800 ₽/мес** (владелец проверил в своём
    кабинете, там же есть отдельные бесплатные JavaScript API и API
    Геокодера — но они не решают "найди все точки категории X в районе",
    геокодер — это адрес↔координаты, не поиск по категориям).
  - **Рабочее решение нашёл владелец**: открыл Яндекс.Карты у себя в
    браузере (без ограничений песочницы), ввёл категорию "аптеки",
    прокрутил список до конца и сохранил страницу через Safari
    (`.webarchive` — бинарный plist с живым DOM на момент сохранения) →
    прислал файл. Разобрал через `python3 plistlib` (извлекает
    `WebResourceData` — HTML) + свой regex-парсер под классы
    `search-business-snippet-view__title`/`__address` → **22 аптеки**
    с реальными адресами, все по улицам района. Заодно нашлась разгадка
    для "In塗то" — нечитаемого из-за битой кодировки названия в
    исходном портрете арендаторов (предыдущий раунд, см. запись про
    полную переписку текста): это **InLek**, самая частая сеть в районе
    (5 из 22 точек) — добавлено обратно в `tenantProfiles`. Метод
    "webarchive от владельца" рабочий и переиспользуемый для остальных
    категорий арендаторов (следующий шаг — разбирать их по одной,
    владелец попросил сначала завершить текущий цикл правок).
  Итог по инструментам на будущее: **для одноразового ресёрча
  Яндекс.Карт/2ГИС из этой сессии — единственный надёжный путь: владелец
  сам сохраняет `.webarchive`/HTML у себя и присылает файлом**, не
  скриншотом/вставкой текста в чат (see выше про недоступность вставленных
  картинок как файлов). Автоматизация со стороны Claude в этой
  инфраструктуре не работает ни через headless-браузер, ни бесплатно
  через официальные API.

- 2026-08-22 — Этап 1, шесть задач подряд (все не требовали данных
  владельца):
  - **Э1-1 (noindex токен-страниц).** `lib/pageMeta.ts` получил
    `setNoIndex()`/`clearNoIndex()` (переключают `<meta name="robots">` —
    тег заведён в `index.html` заранее, иначе переключать нечего).
    `PublicBuildingPlan.tsx`, `BriefPublicPage.tsx`,
    `MeetingSummaryPublicPage.tsx` ставят noindex на маунте, снимают на
    анмаунте.
  - **Э1-2 (soft-404).** `ObjectLandingPage.tsx`: пока `notFound === true`
    (несуществующий слаг), тоже noindex — снимается, когда объект найден.
  - **Э1-3 (редирект корня).** `vercel.json` — блок `redirects`, `/` → `/one`
    (308, `permanent: true`), выполняется раньше `rewrites`. Клиентский
    `<Navigate>` в `App.tsx` не трогал — он всё ещё нужен для зеркала
    GitHub Pages (Vercel-редирект туда не достаёт) и для локальной разработки.
  - **Э1-5 (цены видимым текстом).** Новая карточка "Цены" на `/one` между
    блоком удобств комплекса и вариантами покупки — два H2-уровня факта:
    диапазон кабинетов (11–40 м², числа теперь константы `MIN_ROOM_AREA`/
    `MAX_ROOM_AREA`, использованы и в hero-тексте, и здесь — раньше 11/40
    были захардкожены только в hero-строке) с ценой через `zonePrice()`, и
    рабочее место через `WORKSTATION_PRICE` — те же функции/константы,
    что считают цену на самом плане (`data/buildingPlans.ts`), не
    задублированные вручную числа.
  - **Э1-6 (как проходит сделка видимым текстом).** У существующего блока
    `BookingTermsCard` ("Онлайн-бронирование без предоплаты", 3 шага) было
    почти всё нужное — не стал городить отдельную секцию, а достроил его
    до полного цикла: шаг 3 явно называет **email-код** как способ
    подписания (раньше — обтекаемое "Подтверждение по email"), добавлен
    4-й шаг "Оплатите" (рассрочка/лизинг/кредит) со ссылкой-скроллом наверх
    к карточке "3 варианта покупки" (её id `purchase-options` — якорь,
    тем же приёмом, что и `PLAN_AND_UNITS_ANCHOR_ID`: строка-константа
    продублирована в обоих файлах, не кросс-импортирована). Сетка шагов
    `sm:grid-cols-3` → `sm:grid-cols-2 lg:grid-cols-4`.
  - Мок-тест перед пушем (реальный Supabase недоступен из песочницы —
    `ERR_CONNECTION_RESET` на `*.supabase.co`, не баг, ограничение
    окружения): временный `__ComponentTest.tsx` + роут в `App.tsx`,
    скриншоты десктоп/мобилка Playwright — новые блоки легли в сетку без
    переполнений на обеих ширинах, оригиналы восстановлены после.
  - Не взяты в этот заход (ждут владельца, см. таблицу ниже): Э1-4 (судьба
    зеркала GH Pages), Э1-7 (тексты FAQ нужно согласовать), Э1-8 (нет
    данных для NAP-футера).
- 2026-08-22 — файл создан по итогам исследования (workflow: 6 направлений
  + критика). До этого сделан техфундамент (см. «Что уже сделано»).
- 2026-08-22 — H1 на `/one` укорочен и очищен от дублирования: сначала по
  просьбе владельца убрана добавленная в предыдущем заходе формулировка
  «в Минск Мире» (визуально вводила в заблуждение — здание физически не в
  Минск Мире, а рядом; title/description для поиска её сохранили
  сознательно), заменена на «рядом с Минск Миром»; затем владелец обратил
  внимание, что это дублирует уже существующий бейдж «Рядом с Минск
  Миром» поверх фото — H1 сокращён до сути предложения без локации:
  «Приватные кабинеты и фиксированные рабочие места от $12 000». Alt
  главного фото менять не просили, оставлен как был.
- **Э1-4 закрыт.** Владелец подтвердил: зеркало GitHub Pages не нужно.
  `.github/workflows/deploy-pages.yml` удалён, README и мёртвый условный
  `base` в `vite.config.ts` (`/Redevelopment/` для Pages) убраны.
  Остаётся клиентский `<Navigate>` `/` → `/one` в `App.tsx` — он нужен не
  только для зеркала (см. запись про Э1-3 выше), но и для локальной
  разработки/предпросмотра, не привязан к Pages как таковому.
- Этап 2 начат: **Э2-1 (пререндер лендингов)** реализован, ждёт проверки
  на реальном деплое (детали и то, что осталось сделать/проверить — в
  чекбоксе задачи выше, не дублирую здесь):
  - Владелец подтвердил приоритет и выбрал схему актуализации данных
    (Vercel Deploy Hook при сохранении объекта, не cron и не «только
    стабильный контент»).
  - Оценил framework-режим react-router v7 против build-time
    headless-скрипта — выбрал второе (несоразмерно мигрировать роутинг
    всей админки ради 2–3 публичных страниц).
  - Новые файлы: `scripts/prerender.mjs` (сам пререндер, вызывается из
    `package.json` build-скрипта после `vite build`), `api/trigger-rebuild.js`
    (проксирует Deploy Hook, best-effort, не роняет сохранение объекта
    при сбое). `objectsApi.ts`: `insertObject`/`updateObject` дёргают его
    после успешного сохранения (только если у объекта заполнен
    `landingSlug` — иначе рёбилдить нечего).
  - Зависимости: `playwright-core` + `@sparticuz/chromium` (второй —
    именно под serverless/build-контейнеры вроде Vercel, обычный
    `playwright` в таком окружении рискует не найти системные библиотеки
    под Chromium). В build-контейнере Vercel (`process.env.VERCEL`)
    скрипт запускает `@sparticuz/chromium`, локально — Chromium из
    `PLAYWRIGHT_BROWSERS_PATH` по прямому пути.
  - **Важное ограничение, вскрытое при отладке:** headless-браузер в этой
    песочнице не может достучаться до Supabase (`net::ERR_CONNECTION_RESET`
    после ~15с) — та же проблема, что и в записи про мок-тест 2026-08-16
    выше по этому файлу («реальный Supabase недоступен из песочницы»),
    только теперь подтверждено и для браузерного, не только fetch-трафика.
    Из песочницы это не лечится (проверено: не в проксировании — обычный
    `curl`/`fetch` к тому же Supabase-эндпоинту отрабатывает мгновенно и с
    реальными данными, значит дело именно в браузерном TLS-фингерпринте
    или похожем механизме на стороне Supabase/сети песочницы, не в самом
    скрипте). Из-за этого локально проверено было только то, что сборка
    целиком не падает, если у слага не появился `h1` за 15с — а вот
    запуск `@sparticuz/chromium` в самом build-контейнере Vercel и
    реальное сохранение корректного HTML оставались непроверенными до
    деплоя. **Проверено на первом же реальном деплое — оба неизвестных
    сняты, всё сработало с первой попытки** (см. подробности в чекбоксе
    задачи выше).
- **Э1-7 (FAQ) закрыт.** Владелец ответил на вопросы, которые знает
  только он (парковка — бесплатная, без фиксированных мест; отделка —
  полная финишная чистовая, без мебели; юрадрес — можно регистрировать),
  остальное собрано из уже установленных фактов страницы (цена, шаги
  брони, условия рассрочки/лизинга/кредита). Этап 1 закрыт полностью,
  кроме Э1-8 (NAP-футер) — всё ещё ждёт адреса/телефона/УНП от владельца.
- **Э3-0, черновой аудит (см. оговорку про US-геолокацию и отсутствие
  Яндекса/Wordstat в чекбоксе задачи выше — это не полноценный baseline,
  а то, что видно ограниченным набором инструментов сессии):**
  - **`site:redevelopment.pro` — 0 результатов в Google.** Сайт либо ещё
    не проиндексирован вообще, либо не виден из США. Это САМЫЙ важный
    вывод аудита: пока нет ни одной страницы сайта в индексе — весь
    остальной технический SEO (пререндер, meta, JSON-LD) не работает, пока
    Google вообще не пришёл смотреть. Резко повышает приоритет верификации
    Google Search Console (уже в «Ждём от владельца» — теперь это не
    просто «желательно», а блокер №1 для всего).
  - **«офисы в Минск Мире купить» / «помещения в Минск-Мире аренда» —
    топ-10 сплошь агрегаторы** (realt.by, domovita.by, re.kufar.by,
    megapolis-real.by, t-s.by, etagi.com), ни одного отдельного бизнес-
    центра со своим сайтом. Подтверждает расчёт плана: для этих широких
    гео-запросов реалистичный путь — размещение НА агрегаторах (Э4-3), не
    прямая конкуренция за топ своим сайтом.
  - **«аренда» без уточнения «офис» — выдача смешивается с жилой
    недвижимостью** (в топе «снять квартиру в Минск Мире» наравне с
    «аренда помещения»). Подтверждает: держать в title/копирайтинге слово
    «офисы» явно, не полагаться на голое «помещения в Минск-Мире».
  - **Важная находка про бренд:** существует настоящий, другой,
    более крупный проект с названием буквально «Деловой центр Минск Мир»
    (офисная башня + бизнес-апартаменты, отдельный от ЖК «Минск Мир»
    девелопмент — см. officelife.media, prometr.by). Это НЕ наш объект.
    Задним числом подтверждает решение владельца поправить H1 с «в Минск
    Мире» на «рядом с Минск Миром» (см. запись выше) — использовать фразу
    «деловой центр Минск Мир» как своё название нельзя, будет путаница с
    чужим проектом. Держим «деловой центр Red One» как есть.
  - **Бренд «Red One» пока не находится вообще** — ожидаемо для нового
    сайта без внешних упоминаний, актуально для Э4 (каталоги/PR).
  - **AI Overviews — не проверено**, инструмент веб-поиска сессии не
    показывает наличие/отсутствие AIO, а из США это всё равно не отражало
    бы реальность для запросов из РБ. Оставлено как открытый пункт для
    владельца (обычный поиск с телефона/компьютера из Минска) или для
    проверки после подключения GSC.
  - Яндекс, Wordstat-частотности — недоступны из этой сессии совсем,
    нужны либо от владельца, либо после подключения Яндекс.Вебмастера.
- **Яндекс.Метрика (111858495) и мета-тег подтверждения Вебмастера
  (`0ac24df252ca3634`) добавлены в `index.html`.** Владелец прислал код
  счётчика и тег напрямую. Нюанс, о котором Яндекс в готовом коде не
  предупреждает: сайт после Э2-1 сам себя открывает headless-браузером
  при каждой сборке/пересборке (`scripts/prerender.mjs`) — без доп. защиты
  каждый такой заход слал бы в Метрику фантомный визит с build-сервера.
  Решение — `?prerender=1` в URL, который использует только сам скрипт
  пререндера (в сохранённый HTML параметр не попадает, реальные посетители
  его никогда не видят), инлайн-скрипт Метрики в `index.html` проверяет
  `location.search` и не инициализируется при этом параметре.
  Осталось владельцу в самом Яндекс.Вебмастере (кода это не требует):
  нажать «Проверить» после деплоя, добавить `sitemap.xml`, выставить
  регион Минск. Цели Метрики (клик по телефону, модалка брони, бронь,
  подписание) — отдельная задача, разметка событий в коде ещё не сделана.
- **Э2-4: развилка по гео закрыта (техфундамент, без нового контента).**
  Владелец обозначил будущий второй продукт — IT-платформа компании
  (сейчас просчёт смет живёт только внутри админки, `Estimates.tsx`),
  которую планирует продавать под аудиторию из России, отдельно от
  продающих страниц объектов (`/one` — Минск/РБ). Уточнил у владельца
  масштаб работы прямо сейчас: писать контент новой страницы платформы
  ещё рано (публичного продукта за ней нет, тонкий контент без реального
  сервиса вредит SEO) — владелец согласился, ограничились техфундаментом.
  Сделано: `vercel.json`, редирект `/` → `/one` — был постоянным (308),
  стал временным (307), чтобы не закреплять корень домена намертво за
  `/one` до появления отдельной страницы платформы. Подробный план на
  потом (что делать, когда страница появится — снять редирект, отдельные
  meta/JSON-LD, куда переносить Organization-разметку, регионы по
  разделам в Яндекс.Вебмастере) записан в чекбоксе Э2-4 выше — не
  переоткрывать этот вопрос заново, план уже есть.
- **Э3-1 закрыт — гид «Район Минск Мир для бизнеса».** Владелец прислал
  выгрузку Wordstat по «минск мир» (21.07–21.08.2026, все регионы, все
  устройства, ~164 тыс. показов суммарно) — посчитал состав: запросы с
  явным офисным/коммерческим смыслом («минск мир помещение», «аренда
  помещения минск мир», «минск мир коммерческая недвижимость» и т.п.) —
  меньше 0.4% от объёма. Подавляющее большинство — про квартиры («квартиры
  минск мир» 8901, «купить квартиру минск мир» 5139 и т.д.). Вывод: биться
  за широкое «минск мир» бессмысленно, это рынок жилья, не наш. Отдельная
  фраза «район минск мир» (989/мес) — не про бизнес прицельно, но
  осмысленный объём и смысловое совпадение с темой гида — взята как
  заголовочная, без попытки залезть в жилой кластер запросов.
  Дособрал факты веб-поиском (метро Аэродромная 2024, Avia Mall 138 200 м²,
  Международный финансовый центр, школы/садики) — показал черновик текста
  владельцу до публикации (не как с FAQ, где факты были только внутренние
  и точно известные — здесь часть данных из вторичных источников, точность
  требовала подтверждения). Владелец поправил: (1) не писать точное время
  пешком до Минск Мира — реально далековато, оставить «рядом»/«по
  соседству»; (2) факты по инфраструктуре подтвердил, добавил свой факт —
  ещё 5 детских садов через дорогу от Red One (не из веб-поиска, от
  владельца); (3) цены соседей по коммерческой недвижимости — убрать
  вставку текстом совсем, это живые цифры, лучше потом сделать данными
  через cron-синки (Э3-2), не писать вручную. Ещё раньше в диалоге
  отдельно попросил не ставить ссылку с `/one` на этот гид (не отвлекать
  с продающей страницы) — учтено, ссылка только в одну сторону (гид →
  `/one`).
  Технически: новый роут `/rayon-minsk-mir` в `App.tsx` (обязательно ДО
  catch-all `/:slug`, иначе перехватило бы как поиск объекта по слагу),
  `pageMeta.ts` получил `setGenericPageMeta`/`setArticleJsonLd` —
  переиспользуемый паттерн подмены meta/JSON-LD для контентных страниц
  вне сущности "объект" (пригодится для Э3-3, кластера поддержки).
  `scripts/prerender.mjs`: список слагов теперь не только из Supabase, но
  и `STATIC_SLUGS` (добавлять туда каждый новый гид). Проверено
  скриншотами (десктоп/мобилка) — вёрстка не ломается.
  На первом реальном деплое `/rayon-minsk-mir` молча не попал в пререндер
  (та же страница у меня локально в изоляции рендерится мгновенно без
  ошибок — похоже на разовую нестабильность именно в build-контейнере
  Vercel, не баг компонента). Сделал `prerender.mjs` устойчивее: отдельная
  вкладка (`newPage()`) на каждую попытку вместо одной переиспользуемой на
  все слаги, один повтор при неудаче, таймаут 15с → 20с. После передеплоя
  сработало — проверено curl-ом: title/canonical/Article JSON-LD верные,
  `object-json-ld` корректно пустой (не протёк Red One с предыдущего
  слага), ссылка на `/one` на месте, `/one` и `/redstorage` не пострадали.
- **Гид `/rayon-minsk-mir` — первая версия признана слишком тонкой
  (владелец: «пользы буквально 0»), расширил существенно.** Владелец
  прислал PDF статьи Википедии «Минск Мир» — оттуда точные координаты
  района, обе станции метро («Ковальская Слобода» и «Аэродромная»,
  Зеленолужская линия — раньше упоминал только одну), реальные номера
  автобусных (4, 47с, 53, 56, 73, 84, 100, 107, 124, 172) и троллейбусных
  (19, 27, 59, 82) маршрутов. Указание владельца — меньше про прошлое
  (историю споров с «Итерой», смену инвесторов и т.п. из статьи
  сознательно не брал), больше про настоящее.
  Добавлено: блок статистики (4 плитки с иконками — станции метро, годы
  стройки 2015–2027, площадь Avia Mall, число маршрутов транспорта),
  отдельный FAQ-блок именно про район (4 вопроса, `FAQPage` JSON-LD) —
  для этого `FaqCard.tsx` (была намертво про Red One) разобрана на
  переиспользуемый `components/ui/FaqAccordion.tsx` + список вопросов,
  рефакторинг проверен скриншотом на `/one`, ничего не сломалось.
  **Не сделано, нужно от владельца:**
  - Карта — тот же приём, что ObjectMapWidget.tsx (Яндекс.Конструктор
    карт → «По ссылке»/«Публичная» → вкладка «Код» → только `src=`
    iframe-ссылка). Код готов (`MAP_EMBED_URL` в `DistrictGuidePage.tsx`),
    секция не рендерится, пока константа пустая — просто вставить URL,
    когда пришлёт.
  - Реальные фото района (не стоковые) — сейчас на странице фото нет
    вообще, только иконки/текст/статистика.
  - Список арендаторов района за пределами Avia Mall — владелец хочет
    проанализировать, спросил формат данных; ответил: подходит любой
    список текстом («название — категория — где примерно»), разберу сам.
- **Критический инцидент: 3 деплоя подряд падали (Error), расширенная
  версия гида ни разу не попала на прод.** Реальная причина —
  `browser.newPage()` в предыдущем «фиксе устойчивости» (запись выше)
  вызывался ВНЕ try/catch. На build-контейнере Vercel Chromium один раз
  неожиданно закрылся между слагами (redstorage успел отрендериться,
  one — уже нет: `Target page, context or browser has been closed`),
  повторный `newPage()` на мёртвом браузере уронил `npm run build`
  целиком. Прод не пострадал (Vercel продолжал отдавать последний
  удачный билд — это и есть та защита, которую сам же закладывал), но
  визуально выглядело так, будто ничего не деплоится, хотя пуши шли
  один за другим. **Нашёл только благодаря владельцу** — прислал
  скриншот Build Logs и списка Deployments из Vercel, без него
  диагностировать было бы нечем (своего доступа к логам сборки у меня
  нет). Fix: свежий `launchBrowser()` на каждую попытку целиком внутри
  try/catch (включая `newPage`/`goto`/`close`) — сбой браузера теперь
  гасится локально для одного слага, не всей сборки. Проверено на
  реальном деплое — новый билд появился за 40с (норма), гид со всем
  расширенным контентом на месте, `/one`/`/redstorage` не пострадали.
  **Вывод на будущее:** если после пуша прод не обновляется дольше
  минуты — сразу подозревать сбой сборки, не ждать молча; у владельца
  есть доступ к Vercel Deployments/Build Logs, у меня нет — при
  расхождении с ожиданием стоит попросить его посмотреть раньше, не
  после 9+ минут ожидания.
- **Гид: карта, портрет арендаторов, заготовка под фото.** Владелец
  прислал ссылку на Яндекс.Конструктор в двух нерабочих для iframe
  форматах (страница `yandex.ru/maps/?um=...` и `<script src=
  "api-maps.yandex.ru/...">`) — оба явно помечены как нерабочие в
  ObjectMapWidget.tsx/CLAUDE.md. Вытащил id конструктора из ссылки и
  собрал рабочий URL вручную по формату, который уже реально используется
  в `map_embed_url` объекта Red One (сверено прямым запросом к Supabase) —
  сам constructor-id владельца, просто в правильной обёртке. Не смог
  визуально проверить в песочнице — прокси сессии не поддерживает
  WebSocket/HTTP2, которые нужны виджету Яндекс.Карт (это ограничение
  прокси, не хост заблокирован — в статусе прокси нет записей про
  yandex.ru), тот же формат уже работает в проде на `/one`.
  Портрет арендаторов района (4 категории: сетевой бизнес, ПВЗ,
  бьюти-индустрия, крафтовый бизнес — сферы/метраж/критерии) — прислан
  владельцем, его собственный анализ, не веб-поиск. Одно название аптеки
  («In塗то») нечитаемо из-за битой кодировки при копировании — не
  включено, спросил у владельца, что имелось в виду.
  Фото района прислано владельцем прямо в чат (не файлом) — вставить
  программно нечем, своего доступа к файлу на диске у меня нет. Добавлен
  `HERO_IMAGE_URL` в `DistrictGuidePage.tsx` (пусто → рендерится
  плейсхолдер с иконкой) — попросил ссылку на фото (любой хостинг, тот
  же приём, что и `MINSK_MIR_LOGO_URL` в `ObjectLandingPage.tsx`).
- **Гид: полная переписка текста через Gemini (ProxyAPI-интеграция), с
  утверждением на каждом шаге.** Владелец счёл предыдущую версию всё ещё
  недостаточно полезной ("буквально 0 пользы") и попросил глобально
  переосмыслить страницу от А до Я, а не точечно доращивать блоки. Вместо
  того чтобы писать текст самому, впервые в этой сессии подключил Gemini
  (через ProxyAPI.ru, `gemini-3.7-flash`, оплата в рублях — владелец сам
  настраивал биллинг/ключ/домен в allowlist, см. отдельную запись про
  саму интеграцию) как соавтора текста, Claude — как архитектора структуры
  и кода. Процесс по явному ТЗ владельца (6 шагов): вычленил из
  Wordstat-выгрузки владельца запросы с коммерческим смыслом → предложил
  структуру страницы (первый вариант владелец забраковал как "слишком
  узкий, на пару блоков" — переписал под полный редизайн от А до Я, с
  явным указанием Gemini писать в стиле лендинга — конкретные короткие
  предложения, цифры, хайлайты) → владелец согласовал структуру с двумя
  правками (убрать блок 7 "лид-форма/каталог", в FAQ — только проверенные
  темы) → отправил утверждённое ТЗ в Gemini → получил полный текст на 7
  (после правки — 6) блоков → оформил в `DistrictGuidePage.tsx`.
  Gemini в черновике дважды вышел за рамки подтверждённых фактов — не
  принял молча, вынес оба на подтверждение владельцу: (1) факт
  "многоуровневые и специализированные паркинги" — подтверждён владельцем
  как реальный (в Минск Мире есть и многоуровневые, и подземные паркинги
  в жилых домах — знание не из веб-поиска, от владельца лично); (2)
  перегруппировка портрета арендаторов из исходных 4 категорий владельца
  ("сферы/критерии") в новые 4 по техническим маркерам формата помещения
  (метраж + факторы успеха под коммерческую аренду, включая новую
  категорию "офисы и клиентские сервисы", которой не было в исходном
  портрете) — одобрена владельцем как есть.
  Итог в `DistrictGuidePage.tsx`: убрана плитка "2015–2027 годы застройки"
  (снова "меньше про прошлое"), добавлена "25–45 лет — ядро аудитории";
  новые секции "Генераторы ежедневного трафика" (Avia Mall + МФЦ,
  спрос на банки/РСЦ явно сформулирован как поисковый интерес, не как
  факт присутствия — это была прямая правка в ТЗ для Gemini, чтобы не
  приписывать району несуществующие банки) и "Медицина и здоровье";
  портрет арендаторов — 4 новых карточки (`tenantProfiles`, метраж +
  критерии успеха; у категории "офисы и клиентские сервисы" пустой
  `examples` — JSX прячет пустой блок примеров, не оставляет дыру в
  вёрстке); в "Транспорт и парковка" — новая строка про парковки; карта
  (`MAP_EMBED_URL`) и FAQ теперь на реальных подтверждённых данных, а не
  плейсхолдеры. Проверено: `npx tsc -b` и `npx vite build` чисто,
  `scripts/prerender.mjs` корректно снял `/rayon-minsk-mir` (title,
  контент новых секций — на месте), скриншоты десктоп/мобилка — вёрстка
  не ломается (в т.ч. пустой `examples` у "Офисы и клиентские сервисы" не
  оставляет visual gap).
  **Остаётся открытым:** `HERO_IMAGE_URL` всё ещё пусто (фото от владельца
  пришло только как вставка в чат, не как ссылка/файл); название аптечной
  сети из исходного портрета арендаторов осталось нечитаемым из-за битой
  кодировки ("In塗то") — не уточнено, не включено в текст.

## Основные источники исследования

Google Search Central (ai-optimization-guide, special-tags, JS-rendering),
Яндекс.Вебмастер (rendering, обход по счётчикам, региональность),
Whitespark Local Search Ranking Factors 2026, Search Engine Land (итоги
core-апдейтов 2025), Vercel/MERJ (рендеринг Googlebot), Seer Interactive
(CTR при AI Overviews), reactrouter.com/how-to/pre-rendering, беларуские:
websfera.by, beseller.by, gusarov-group.by, cropas.by, barsukov.by;
площадки: realt.by, realt.onliner.by, probusiness.io, pressfeed.ru.
Полные ссылки — в результатах исследования (сессия 2026-08-22).
