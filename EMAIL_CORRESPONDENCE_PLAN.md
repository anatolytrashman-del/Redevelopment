# План: email-переписка с поставщиками в Ресерче (шаблоны, рассылка, входящие)

Инструкция для следующей сессии Claude Code (владелец, 2026-09-03: "напиши
инструкцию для себя же, я включу более простую модель для реализации").
Написана по итогам разбора кода в этой сессии — всё, что здесь названо
"уже есть", проверено по факту, а не по памяти. Читать ЦЕЛИКОМ перед
началом, потом идти по этапам по порядку, отмечая `[x]` в этом же файле.
В CLAUDE.md — только короткая запись в журнал по факту каждого этапа,
подробности не дублировать.

## 0. Что просил владелец (дословно, чтобы не потерять смысл)

> 1. Добавляем подрядчика в ресерч либо автоматическим поиском, либо вручную
> 2. Далее находим контактный email, добавляем его в карточку подрядчика
> Нужно продумать отправку первого письма и как смотреть всю переписку.
> Точно нужна группировка по подрядчику, метка с категорией подрядчика
> (например "Умные замки", чтобы в памяти не держать), ну и сама группировка
> по этому тегу. Понадобятся шаблоны — письма для поставщиков замков будут
> похожими, но всё равно нужна возможность кастомно менять каждое письмо.

Решения, уже согласованные с владельцем в этой сессии (не переспрашивать):
- **Категория = сам запрос Ресерча** (`SupplierRequest.title`, например
  "Умные замки"). Отдельного поля "категория/тег" НЕ заводить — запрос уже
  группирует поставщиков, второе поле дублировало бы его. В интерфейсе
  переписки название запроса показывать бейджем на каждом треде.
- Порядок: сначала вкладка "Переписка" + шаблоны (этапы 2–3), рассылка
  пачкой — этап 4, отдельным коммитом.

## 1. Что УЖЕ есть в коде (не переписывать, переиспользовать)

| Что | Где |
|---|---|
| Поле `email` у предложения, веб-поиск его заполняет | `src/data/supplierResearch.ts` (`SupplierOffer.email`), `src/pages/Suppliers.tsx` |
| Таблица переписки по предложению | `supplier_offer_emails` (см. схему ниже), RLS `authenticated_all` |
| Тип/маппер/API переписки | `src/data/supplierOfferEmails.ts`, `src/lib/supplierOfferEmailsApi.ts` (`fetchSupplierOfferEmails(offerId)`, `sendSupplierOfferEmail({offerId,toAddress,subject,body})`) |
| Адрес для ответов | `supplierOfferEmailAddress(offerId)` → `research+<offerId>@redevelopment.pro` — ответ поставщика матчится по id в локальной части |
| Отправка (Resend) | `api/purchase-send-email.js` — принимает `{ purchaseId \| offerId, toAddress, subject, body }`, шлёт через Resend с plus-адреса, сам пишет строку `direction:'out'` сервисным ключом. Требует `requireStaffAuth` (клиент зовёт через `authFetch`) |
| Приём (вебхук Resend) | `api/purchase-email-webhook.js` — различает `zakupki+`/`research+`, пишет `direction:'in'`, вложения через `api/_attachments.js` в бакет `object-documents`, подпись Svix проверяется по `RESEND_WEBHOOK_SECRET` |
| UI переписки сейчас | `OfferEmailModal` в конце `Suppliers.tsx` — кнопка "Написать" в `OfferDetailModal`, тема по умолчанию `Запрос цены: <название>`, лента писем + textarea + "Отправить". Видна ТОЛЬКО из карточки конкретного предложения — общей картины нет |
| Уведомления в колокольчик | `src/lib/notifications.ts` (`addNotification({title, body})`), паттерн фонового опроса — `src/lib/marketOfferDiscussionWatcher.ts` (раз в 60с, `localStorage` для "уже виденных" id, хук вызывается один раз из `AppLayout.tsx`) |

Схема `supplier_offer_emails` (проверена через Management API):
`id uuid`, `offer_id uuid NOT NULL`, `direction text NOT NULL` ('in'|'out'),
`from_address text NOT NULL`, `to_address text NOT NULL`,
`subject text NOT NULL DEFAULT ''`, `body text NOT NULL DEFAULT ''`,
`files jsonb NOT NULL DEFAULT '[]'`, `resend_message_id text NULL`,
`created_at timestamptz`. **Колонки `read_at` пока НЕТ** — см. этап 2.

Инфраструктура Resend на стороне владельца: домен верифицирован (DKIM/SPF/
MX на приём — Verified), тумблеры Sending/Receiving включены, вебхук на
`/api/purchase-email-webhook` заведён, `RESEND_WEBHOOK_SECRET` в Vercel
есть (с 29 августа). Т.е. со стороны кабинета всё готово.

## 2. Ограничения проекта, которые ЛЕГКО нарушить (читать обязательно)

- **Vercel Hobby: максимум 12 serverless-функций** (`api/*.js` без `_` в
  начале). Сейчас ровно 12. **Ни одного нового файла в `api/` заводить
  нельзя** — всё новое (шаблоны, рассылка) встраивать в
  `purchase-send-email.js` через дополнительные поля тела запроса.
- **SQL-миграции выполняем сами** через Supabase Management API (см.
  CLAUDE.md, раздел "SQL-миграции", `$SUPABASE_ACCESS_TOKEN`), в конце
  каждой DDL — `NOTIFY pgrst, 'reload schema';`. RLS на новые приватные
  таблицы — политика `authenticated_all` для роли `authenticated` (НЕ
  `anon` — это устаревший паттерн из старых записей журнала).
- **NOT NULL-ловушка**: у текстовых колонок в этом проекте почти всегда
  `NOT NULL DEFAULT ''`. Из клиента слать пустую строку как есть, НЕ
  приводить к `null` через `|| null` — ровно на этом в этой сессии упал
  `insertPledge` ("violates not-null constraint"). Перед написанием
  insert/update **сверить схему таблицы запросом к
  `information_schema.columns`**, не по памяти.
- Перед коммитом: `npx tsc -b && npx vite build`. Стейджить файлы
  поимённо, никогда `git add -A`. Перед началом работы `git fetch origin
  claude/redevelopment-platform-prototype-oodobu` — параллельная SEO-сессия
  пушит в ту же ветку, конфликт почти всегда только в CLAUDE.md
  (сохранять обе записи).
- Мок-тест UI перед пушем: временный маршрут `/__test-suppliers` в
  `App.tsx` (бэкап файла в scratchpad, `diff` после отката должен быть
  пустым), Playwright `chromium.launch({ executablePath:
  '/opt/pw-browsers/chromium' })`, импорт из
  `/opt/node22/lib/node_modules/playwright/index.mjs`, перехват
  `**/rest/v1/**` и `**/api/**` через `page.route`. Ответ на `.single()`
  запрос — одиночный объект, не массив. Живой Supabase/Resend из
  песочницы не достать.

## 3. Этапы

### Этап 1 — Проверить формат входящего письма вживую (перед всем остальным)

- [x] **Подозрение подтверждено документацией (2026-09-03, поиском —
  resend.com недоступен из песочницы напрямую даже через WebFetch, только
  WebSearch-сниппеты).** `email.received` действительно несёт только
  метаданные (`email_id`/`from`/`to`/`subject`/`attachments`-список БЕЗ
  содержимого), дословно из документации: "Webhooks do not include the
  email body, headers, or attachments, only their metadata. You must call
  the Received emails API or the Attachments API to retrieve them."
  Тело: `GET https://api.resend.com/emails/receiving/{emailId}` →
  `{ text, html, headers, ... }`. Вложения — ОТДЕЛЬНЫЙ запрос (не тот же,
  что и тело): `GET https://api.resend.com/emails/receiving/{emailId}/attachments`
  → массив `{ id, filename, content_type, download_url (валиден 1ч), ... }`
  — сопоставляется с метаданными вебхука по `id`. Оба — `Authorization:
  Bearer RESEND_API_KEY` (уже есть в Vercel, новый секрет не нужен).
- [x] **Код исправлен** — `api/_attachments.js` (новый `fetchReceivedEmailBody`
  + переписанный `extractEmailAttachments`, теперь тянет `download_url`
  отдельным запросом вместо ожидания его в самом вебхуке) и
  `api/purchase-email-webhook.js` (`body` больше не читается из
  `data.text`/`data.html` — только через `fetchReceivedEmailBody`).
  Раньше КАЖДОЕ входящее письмо сохранялось бы с пустым `body` — это не
  крайний случай, а гарантированный баг на первом же реальном ответе.
  Синтаксис (`node --check`) и логика (сухой прогон с моком `fetch` —
  оба формата ответа: плоский и `{data:...}` SDK-обёрнутый, плюс полный
  путь вложения id→download_url→байты→загрузка в бакет) проверены в
  песочнице — реальный вызов `api.resend.com` из неё недоступен.
- [ ] **Осталось — живой прогон с владельцем** (это НЕ code review, без
  него нельзя закрыть этап): из карточки любого предложения с реальным
  email нажать "Написать", отправить, затем ОТВЕТИТЬ на это письмо с той
  почты. Критерий: в `supplier_offer_emails` появилась строка
  `direction='in'` с непустым `body` (не только `resend_message_id`), и
  она видна в `OfferEmailModal` этого предложения. Если формат всё же
  окажется другим (Resend меняет API без предупреждения) — смотреть
  Vercel → Logs функции `purchase-email-webhook` (там теперь есть
  `console.error` с полным ответом api.resend.com при неудаче) и поправить
  разбор под фактический ответ.

### Этап 2 — Вкладка "Переписка" на странице "Закупки" ✅ ГОТОВО (2026-09-03)

Цель: одна страница со всеми тредами, группировка Запрос → Поставщик,
статусы и непрочитанные.

- [x] Миграция `read_at` выполнена, схема сверена (`read_at timestamptz`,
  nullable, без дефолта). `SupplierOfferEmail.readAt`/`SupplierOfferEmailRow.read_at`,
  `fromRow` в `supplierOfferEmailsApi.ts`, `markSupplierOfferEmailsRead(offerId)`
  (клиентский update, RLS `authenticated_all` — не привилегированная
  операция, отдельная серверная функция не нужна).
- [x] `fetchAllSupplierOfferEmails()` в `supplierOfferEmailsApi.ts`.
- [x] `SUPPLIER_TABS` дополнена `'Переписка'` (четвёртая), `supplierAddButton`
  для неё `undefined`.
- [x] `src/components/suppliers/SupplierCorrespondenceTab.tsx` — новый файл,
  содержит `EmailThread` (общая лента+форма ответа, теперь используется и
  вкладкой, и `OfferEmailModal`) и `SupplierCorrespondenceTab` (список
  Запрос→Поставщик слева, тред справа). Статус треда считается на лету из
  писем (`threadStatus()`), не хранится. Список отсортирован: сначала
  непрочитанные (по убыванию количества), потом по дате последнего письма.
  Предложения без email не показываются вовсе.
- [x] **Отклонение от плана**: счётчик непрочитанных НЕ встроен в подпись
  вкладки ("Переписка (N)") — `ToggleGroup` сравнивает `value === option`
  буквально строкой, динамическая подпись сломала бы подсветку активной
  вкладки и весь `tab === 'Переписка'` роутинг при каждой смене счётчика.
  Вместо этого — статичная подпись "Переписка" + отдельный красный бейдж
  рядом с `ToggleGroup` (`countUnreadSupplierEmails(emails)`).
- [x] `src/lib/supplierEmailWatcher.ts` (по образцу
  `marketOfferDiscussionWatcher.ts`, НЕ ограничен `isSuperAdmin` — Светлана
  тоже ведёт переписку), подключён в `AppLayout.tsx` рядом с
  `useMarketOfferDiscussionWatcher`.
- [x] Мок-тест (Playwright, временный `/__test-suppliers`, перехват
  `**/rest/v1/supplier_offer_emails**` GET/PATCH и `/api/purchase-send-email`):
  группировка по запросу верна, предложение без email скрыто, запрос без
  предложений-с-email не показан, статусы "Отправлено"/"Ответили" верны,
  клик по треду с непрочитанным гасит и его личный счётчик, и общий бейдж
  у `ToggleGroup` (PATCH ушёл), ответ из формы треда добавляет письмо в
  ленту (POST на `/api/purchase-send-email`, не отдельный эндпоинт).
- [ ] Мобильная раскладка (список/тред друг под другом на `< lg`) — НЕ
  проверена отдельно, только видно по коду (`lg:flex-row`/`lg:w-80`), не
  скриншотил на узком вьюпорте. Если владелец откроет "Переписку" с
  телефона и что-то не так — доработать отдельным заходом.

### Этап 3 — Шаблоны писем

- [ ] Миграция — новая таблица:
  ```sql
  create table if not exists email_templates (
    id uuid primary key default gen_random_uuid(),
    name text not null,
    subject text not null default '',
    body text not null default '',
    request_id uuid references supplier_research_requests(id) on delete set null,
    created_at timestamptz not null default now()
  );
  alter table email_templates enable row level security;
  create policy authenticated_all on email_templates
    for all to authenticated using (true) with check (true);
  notify pgrst, 'reload schema';
  ```
  `request_id` — необязательная привязка "шаблон по умолчанию для этого
  запроса"; `null` — общий шаблон.
- [ ] `src/data/emailTemplates.ts` (`EmailTemplate`/`EmailTemplateRow`) +
  `src/lib/emailTemplatesApi.ts` (`fetchEmailTemplates`, `insertEmailTemplate`,
  `updateEmailTemplate`, `deleteEmailTemplate`) — стандартная связка, см.
  CLAUDE.md "Паттерн работы с данными".
- [ ] Плейсхолдеры — чистая функция `renderEmailTemplate(template, ctx)` в
  `src/lib/emailTemplates.ts` (без React). Поддерживать ровно:
  `{компания}` → `offer.name`, `{запрос}` → `request.title`,
  `{материалы}` → список `request.items` через запятую в виде
  `название (кол-во ед.)` (та же сборка строки, что в `openWebQueryModal`
  в `Suppliers.tsx` — вынести её в общий хелпер, не копировать),
  `{контакт}` → `offer.contact`. Неизвестные `{…}` оставлять как есть,
  не падать. Регистр и пробелы внутри фигурных скобок игнорировать.
- [ ] UI управления: подвкладка/блок "Шаблоны" внутри вкладки "Переписка"
  (кнопка "Шаблоны" в шапке вкладки → модалка со списком: название,
  привязка к запросу (`Select`, "Общий" по умолчанию), тема, текст с
  подсказкой про плейсхолдеры; добавить/редактировать/удалить).
- [ ] В форме отправки (и в треде вкладки "Переписка", и в
  `OfferEmailModal`): `Select` "Шаблон" над темой. Порядок в списке:
  сначала шаблоны с `request_id` этого запроса, затем общие. Выбор →
  подставить `renderEmailTemplate(...)` в тему и текст (только если текст
  пустой или пользователь подтвердил `window.confirm` замену — не
  затирать молча то, что уже напечатали). После подстановки — обычные
  редактируемые поля, шаблон никак не "держит" текст.
- [ ] Кнопка "Сохранить как шаблон" под textarea — открывает ту же
  модалку шаблона с предзаполненными темой/текстом (подставленные
  значения обратно в плейсхолдеры не превращать — пусть сохраняется как
  есть, владелец поправит руками).
- [ ] Мок-тест: создать шаблон с `{компания}` и `{материалы}`, выбрать в
  форме — проверить подстановку, что текст после этого правится, что
  отправка уходит с итоговым (уже подставленным) текстом.

### Этап 4 — Рассылка первого письма всем поставщикам запроса

**Владелец, 2026-09-03 (важно, читать перед началом): "учти момент, чтобы
мы не попали в спам за массовую отправку одинаковых писем. А то такое
ощущение, что если улетит 20 одинаковых писем одновременно, нас забанят,
домен новый".** Реальный риск, не перестраховка — домен `redevelopment.pro`
у Resend недавно верифицирован (DKIM/SPF/DMARC настроены 2026-09, живой
трафик почти не ходил), а у почтовых провайдеров (Gmail/Yandex/mail.ru и
т.п.) есть встроенная эвристика именно на "новый домен + пачка одинаковых
писем одновременно" — типичный паттерн спам-рассылки, даже если письма
абсолютно легитимны. Это НЕ про то, что Resend забанит аккаунт (у него
свои лимиты, см. ниже отдельно) — это про то, что письма реально попадут
в спам у получателей или домен получит плохую репутацию с самого начала,
до того как накопится история легитимной переписки. Учесть при
реализации:

- [ ] **Персонализация — уже часть плана, не довесок.** Плейсхолдеры
  `{компания}`/`{материалы}`/`{запрос}`/`{контакт}` (этап 3) означают, что
  20 писем формально РАЗНЫЕ (разное приветствие, разный список позиций),
  не байт-в-байт идентичные — это уже сильно снижает риск именно
  спам-паттерна "identical bulk". Не срезать эту персонализацию ради
  простоты — не предлагать в модалке рассылки вариант "одно и то же всем
  без плейсхолдеров".
- [ ] **Задержка между отправками**, не просто последовательность.
  Голый `for...of` с `await` всё равно шлёт письма настолько быстро,
  насколько успевает ответить сеть/Resend (может быть <1с между письмами)
  — выглядит как бот. Добавить явную паузу между отправками (`await new
  Promise(r => setTimeout(r, DELAY_MS))` после каждого письма, кроме
  последнего) — `DELAY_MS` в районе 3000-5000 (3-5 секунд), не меньше.
  На 20 писем это ~1-1.5 минуты общей рассылки — нормально для модалки с
  прогресс-баром, владелец не должен ждать, приложение не блокируется
  (модалку можно закрыть, рассылка не обязана быть синхронной с открытым
  окном — если владелец предпочтёт закрыть и не ждать, доделать
  фоновую отправку через `AbortController`/пусть просто предупредим не
  закрывать вкладку, это проще и достаточно для текущего масштаба).
- [ ] **Мягкое предупреждение + разумный дефолт в самой модалке рассылки**
  (UI, не жёсткий лимит в коде — решение всё равно за владельцем): если
  получателей больше ~8-10, показать заметный (не блокирующий) баннер
  "Домен redevelopment.pro ещё нарабатывает репутацию у почтовых
  провайдеров — для лучшей доставляемости на старте рекомендуем рассылать
  небольшими партиями (5-10 писем), а не всё сразу". Не блокировать
  отправку остального — просто честно предупредить, финальное решение
  (сколько слать за раз) за владельцем, он лучше знает свою ситуацию с
  доменом на момент реализации (к тому моменту домен мог уже "нагреться"
  за счёт обычной рабочей переписки).
- [ ] Resend rate limit — по документации 2 запроса/сек на большинстве
  тарифов (сверить актуальное значение в личном кабинете/докладной
  Resend на момент реализации, могло измениться) — задержка в 3-5с между
  письмами и так далеко в пределах лимита, отдельно троттлить не нужно,
  но если решите уменьшить DELAY_MS — не опускать ниже лимита тарифа.
- [ ] Кнопка "Написать всем" на карточке запроса в Ресерче (рядом с
  "Найти в сети"). Активна, если есть хотя бы одно предложение с email,
  которому ещё не писали (нет писем `out`).
- [ ] Модалка рассылки: `Select` шаблона (обязателен) → список получателей
  с чекбоксами (по умолчанию отмечены все с email, кому ещё не писали;
  те, кому уже писали, тоже в списке, но сняты и с пометкой
  "Уже отправляли N писем") → у каждого раскрывающееся превью темы/текста
  с УЖЕ подставленными плейсхолдерами, редактируемое точечно (правки
  хранятся в локальном стейте `overrides: Record<offerId, {subject,
  body}>`) → предупреждение про домен (см. выше, если получателей много)
  → кнопка "Отправить N писем".
- [ ] Отправка — ПОСЛЕДОВАТЕЛЬНО (for…of с await, не `Promise.all`) И с
  задержкой между письмами (см. выше) — оба требования, не одно вместо
  другого. Каждому — обычный `sendSupplierOfferEmail` со своим `offerId`
  (свой `research+` адрес, ответы разлетятся по своим тредам сами).
  Прогресс "Отправлено 7 из 20" в модалке, ошибки по конкретным
  получателям собирать и показывать списком в конце, не прерывать
  рассылку из-за одной.
- [ ] Серверная часть НЕ меняется — `purchase-send-email.js` уже умеет
  всё нужное по одному письму. Новых `api/*.js` — нет.
- [ ] Мок-тест: 3 получателя, у одного замокать 500 на `/api/purchase-send-email`
  — проверить, что два ушли, ошибка показана по третьему, у отправленных в
  треде появилось письмо. Отдельно — таймингом (`Date.now()` между
  замоканными вызовами) подтвердить, что между отправками реально есть
  пауза, а не просто последовательность без задержки.

### Этап 5 — Полировка (только если останется время, отдельно спросить)

- Скрывать/сворачивать цитирование исходного письма в ответах поставщика
  (строки, начинающиеся с `>`), чтобы лента не раздувалась.
- Из веб-поиска в результат сразу писать `communicationStatus` — сейчас
  это свободное поле, можно автоматически выставлять "Письмо отправлено"
  после первой отправки. Спросить владельца, нужно ли, — поле он заводил
  для ручных пометок.

## 4. Что НЕ делать

- Не заводить отдельную таблицу/поле "категория поставщика" — категория =
  запрос (решение владельца).
- Не создавать новые файлы в `api/` (лимит 12).
- Не переносить переписку в `purchase_emails` и не объединять таблицы —
  RFQ и оформленная закупка сознательно разделены (см. комментарий в
  `data/supplierOfferEmails.ts`).
- Не открывать `supplier_offer_emails`/`email_templates` для `anon` —
  только `authenticated`.
- Не рассылать через `Promise.all` И без задержки между письмами (см.
  этап 4 — оба требования, не одно вместо другого; последовательность
  сама по себе не защищает от паттерна "пачка одинаковых писем", если
  письма улетают друг за другом без паузы).
- Не предлагать в модалке рассылки (этап 4) вариант письма без
  плейсхолдеров "одно и то же всем" — персонализация обязательна, это
  часть защиты от спам-эвристик, не просто удобство.

## 5. Журнал выполнения (дополнять сверху)

- **2026-09-03 — Этап 4 доопределён (без кода — этап ещё не начат).**
  Владелец, между реализацией этапов: "учти момент, чтобы мы не попали в
  спам за массовую отправку одинаковых писем... домен новый". Реальный
  риск (новый домен + пачка похожих писем разом = типичная спам-эвристика
  у получателей, даже для легитимной почты), не отменяет план, но меняет
  требования к этапу 4 ДО того, как он будет закодирован: обязательная
  персонализация через плейсхолдеры (уже была в этапе 3, но теперь явно
  помечена как не подлежащая упрощению), задержка 3-5с между письмами (не
  просто последовательность), мягкое UI-предупреждение при большом числе
  получателей с советом слать партиями. Подробности — прямо в чекбоксах
  этапа 4 выше.

- **2026-09-03 — Этап 2 готов.** Вкладка "Переписка" (`Suppliers.tsx` →
  `SUPPLIER_TABS`), общий компонент `EmailThread`+`SupplierCorrespondenceTab`
  (`src/components/suppliers/SupplierCorrespondenceTab.tsx`), `read_at` +
  API (`fetchAllSupplierOfferEmails`/`markSupplierOfferEmailsRead`),
  фоновый вотчер (`supplierEmailWatcher.ts`). `OfferEmailModal` переведён
  на общий `EmailThread`, свой fetch убран — единый источник данных
  (`supplierEmails` на уровне страницы). Единственное отклонение от
  исходного плана — счётчик непрочитанных НЕ в подписи вкладки (сломал бы
  `ToggleGroup`, сравнивающий `value===option` буквально), а отдельным
  бейджем рядом. Мок-тест зелёный (группировка/статусы/скрытие
  предложений без email/гашение счётчика/отправка из треда), мобильная
  раскладка не проверялась отдельно. `npx tsc -b`/`npx vite build` чистые.

- **2026-09-03 — Этап 1 (частично).** Подозрение из плана подтверждено
  документацией Resend: вебхук `email.received` не несёт тела письма —
  оно и вложения дотягиваются отдельными GET-запросами к
  `api.resend.com/emails/receiving/{id}` и `.../attachments`. Код в
  `api/_attachments.js`/`api/purchase-email-webhook.js` переписан под это,
  проверен синтаксически и сухим прогоном с моком `fetch` (реальный API
  из песочницы недоступен). **Живой прогон с владельцем ещё не сделан** —
  без него этап 1 не закрыт, см. чекбоксы выше.
