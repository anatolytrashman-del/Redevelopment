-- Заметка у записи в записной книжке почты (владелец, 2026-09-16, импорт
-- списка журналистов). Пятое поле сверх тех четырёх, что были заказаны:
-- в присланном списке у каждого контакта был хвост, которому больше негде
-- жить — телефон, телеграм, чем человек занимается и оговорки вроде
-- "личный адрес не найден, использован общий" или "перепроверить перед
-- отправкой". Без колонки половина смысла списка терялась бы при переносе.
alter table mailbox_contacts add column if not exists note text not null default '';

notify pgrst, 'reload schema';
