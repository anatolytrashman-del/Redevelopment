-- Оценка варианта контакта (чем больше, тем вероятнее это рабочий контакт
-- закупок). Закладка «Снять контакты» с 2026-09-14 отдаёт не одно значение
-- по клику, а ВЕСЬ набор со страницы разом — владелец: «мне нравится, как
-- реализовано снятие меню — всего 1 клик». Порядок вариантов в карточке
-- задаётся этим полем: ящик закупок/продаж выше общего info@, городской
-- номер выше 8-800.
--
-- Совместимо со старым кодом: колонка с умолчанием, прод её просто не видит.
alter table supplier_contact_captures
  add column if not exists rank int not null default 0;

notify pgrst, 'reload schema';
