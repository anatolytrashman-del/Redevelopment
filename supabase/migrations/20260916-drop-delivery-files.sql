-- Колонка purchase_deliveries.files больше не используется: документы
-- поставки переехали в общую таблицу purchase_documents (владелец,
-- 2026-09-16: «появляются доп. документы по поставке, надо интерфейс для их
-- загрузки и истории» — истории у jsonb-массива файлов не было, кто и когда
-- принёс бумагу, он не помнил).
--
-- Применять ТОЛЬКО после того, как код с purchase_documents доехал до прода
-- (READY): пока на проде старая сборка, она пишет в files при сохранении
-- поставки, и удаление колонки уронило бы ей сохранение. На момент удаления
-- данных в колонке нет ни одной строки — поставок в базе заведено 0.

alter table purchase_deliveries drop column if exists files;

notify pgrst, 'reload schema';
